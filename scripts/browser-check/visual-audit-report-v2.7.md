# TDSF Linux Desktop 视觉审查报告 v2.7

> 审查范围：`scripts/browser-check/screenshots/` 下 19 个页面对应的 app 截图与设计稿截图
> 审查日期：2026-07-20
> 审查人：前端视觉审查员

---

## 顶部汇总

| 项目 | 数量 |
|------|------|
| 总问题数 | 47 |
| P0（必须立即修复） | 5 |
| P1（影响设计还原） | 24 |
| P2（细节打磨） | 18 |
| 设计稿滞后 | 1 |
| 无明显差异 | 0 |

**整体结论**：应用与设计稿在全局布局、卡片密度、统计指标展示方式上存在系统性偏差。启动页差异最为严重，多处页面存在统计卡片/课程卡片横向布局与纵向堆叠不一致的问题。设置类页面整体还原度较好，但控件细节（滑块、开关、按钮分组）仍需统一。

---

## 逐页审查

### 1. boot（启动页）

**整体匹配度：低**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P0 | 背景完全不一致 | 设计稿为纯白色背景，仅左上角有黑色标题；应用为深色底+彩色斜向光带（蓝/橙/白） | 按设计稿改为纯白背景，移除光带效果 |
| P0 | 标题颜色与位置不一致 | 设计稿标题为黑色、左上角；应用为白色、居中 | 还原设计稿的黑色左上角标题 |
| P0 | "进入工作台"入口几乎缺失 | 设计稿中该入口仅显示为一条短横线，文字与箭头不可辨识；应用中为明显的白色文字+箭头 | 确认设计稿意图，若需隐藏入口则按设计稿执行，若需保留则更新设计稿 |
| P1 | 标题字重/间距差异 | 设计稿标题较细、字间距紧凑；应用标题较粗、字间距宽 | 统一标题字体与字间距 |

---

### 2. workbench（工作台）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 标题栏显示差异 | 应用顶部显示 "prod-web-01" 及地址信息；设计稿无此标题栏或更简洁 | 统一标题栏信息展示 |
| P1 | 右侧面板标题与标签样式不同 | 应用 "AI运维助手" 标题下方直接是消息；设计稿标题区与输入区边界更清晰 | 调整右侧边栏标题与内容间距 |
| P1 | 右下角悬浮按钮 | 应用右下角有一个蓝色圆形悬浮按钮；设计稿无 | 确认是否为功能按钮，若不需要则移除 |
| P2 | 终端字体颜色 | 应用中部分命令输出颜色（如红色高亮）与设计稿略有差异 | 统一终端主题色 |
| P2 | 左侧资源树缩进 | 应用资源树层级缩进略小，显得更紧凑 | 按设计稿调整缩进 |

---

### 3. tutorial（运维教程）

**整体匹配度：低**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P0 | 顶部统计卡片布局错误 | 设计稿中 12/48/3.2k 三个卡片纵向堆叠；应用中为横向排列 | 改为纵向堆叠布局 |
| P0 | 精选课程卡片列数错误 | 设计稿中课程卡片单列展示；应用中为双列并排 | 改为单列布局 |
| P1 | 缺少"推荐学习路径"区域 | 设计稿底部无"推荐学习路径"横向卡片；应用中有该区域 | 确认设计稿版本，若不需要则移除 |
| P1 | 标签行样式不同 | 应用中标签为圆角胶囊样式；设计稿中标签更接近朴素按钮 | 统一标签样式 |
| P1 | 课程卡片内进度条与按钮 | 应用中"继续学习"按钮与进度条位置和设计稿不同 | 按设计稿调整卡片内部元素位置 |
| P2 | 页面标题副标题位置 | 应用标题"运维教程"下方副标题位置与设计稿略有偏差 | 微调标题区间距 |

---

### 4. monitor（实时监控）

**整体匹配度：高（设计稿滞后，按新需求评估）**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| 设计稿滞后 | 监控指标展示方式不同 | 设计稿为旧版大数字+环形图；应用已按需求改为大数字+迷你折线图 | 不修复应用，更新 design-monitor.html 设计稿 |
| P2 | 告警条颜色 | 应用顶部告警条为红棕色背景；设计稿中无对应新样式参考 | 确认告警条颜色 token |
| P2 | 进程监控区域行高 | 应用进程列表行高略大于设计稿旧版 | 按新设计规范统一行高 |

---

### 5. history（历史决策）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 统计卡片布局不一致 | 设计稿中 247/94.3%/0.82/12s 四个指标纵向堆叠；应用中为横向排列 | 改为纵向堆叠，并附带迷你折线趋势 |
| P1 | 列表项信息密度不同 | 应用列表项显示更多字段（时间、服务器、用户、命令、耗时等）；设计稿更精简 | 按设计稿精简列表项字段 |
| P1 | 筛选标签样式不同 | 应用筛选标签为按钮组样式；设计稿中更扁平 | 统一筛选标签样式 |
| P2 | 分页器位置 | 应用分页器在右下角；设计稿未完整显示分页区域 | 确认分页器样式 |

---

### 6. knowledge（运维知识库）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 搜索栏样式不一致 | 应用搜索栏为深色圆角带图标；设计稿搜索栏更扁平、占位文字位置不同 | 统一搜索栏高度、圆角、图标 |
| P1 | 热门标签样式不同 | 应用标签为圆角胶囊；设计稿标签为朴素小按钮 | 统一标签样式 |
| P1 | 知识列表项右侧匹配度标签 | 应用右侧显示蓝色百分比标签；设计稿也有但颜色/圆角不同 | 统一匹配度标签样式 |
| P1 | 右侧边栏标题与分组 | 应用右侧有"热门知识""最近浏览""AI 知识沉淀"；设计稿分组标题样式不同 | 统一右侧边栏标题样式 |
| P2 | 知识列表项间距 | 应用列表项间距略小于设计稿 | 调整列表项 padding |

---

### 7. logs（系统日志）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 缺少"导出 CSV"按钮 | 设计稿右下角有"导出 CSV"按钮；应用中无 | 添加导出 CSV 按钮 |
| P1 | 顶部日志级别标签样式 | 应用中标签为填充按钮；设计稿为线框/扁平标签 | 统一标签样式 |
| P1 | 左侧日志源列表选中态 | 应用选中项为蓝色高亮；设计稿选中态左侧有蓝色竖条 | 按设计稿调整选中态 |
| P2 | 底部状态栏信息 | 应用底部显示"系统日志 · 1,247 条"；设计稿显示更多统计项 | 统一底部状态栏内容 |
| P2 | 日志行高亮条颜色 | 应用中 WARN/ERROR 行背景色与设计稿略有差异 | 统一日志级别背景色 token |

---

### 8. settings-general（通用设置）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 设置项标签与控件对齐 | 应用中部分标签与右侧控件（开关/输入框）垂直对齐略有偏差 | 统一设置项 label 与 control 对齐 |
| P1 | 保存/恢复默认按钮样式 | 应用"保存设置"为蓝色填充按钮；设计稿中主按钮样式可能不同 | 统一主按钮样式 |
| P2 | 分组标题字号/颜色 | 应用分组标题（如"语言与地区"）与设计稿字重略有不同 | 统一分组标题样式 |
| P2 | 输入框圆角与边框 | 应用输入框为深色填充；设计稿输入框更明显的边框 | 统一输入框样式 |

---

### 9. settings-model（模型配置）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 顶部统计卡片布局不一致 | 设计稿中 45.6x/$0.68/127次/89.3% 纵向堆叠；应用中为横向排列 | 改为纵向堆叠布局 |
| P1 | 温度参数滑块样式 | 应用滑块为细线+白色圆点；设计稿滑块更粗、填充色不同 | 统一滑块样式 |
| P1 | Thinking Effort 区域 | 应用显示为蓝色加号图标按钮；设计稿中为选项按钮组（低/中/高） | 按设计稿实现 Thinking Effort 选项 |
| P1 | API Key 输入框 | 应用 API Key 为长密码输入；设计稿显示更短或布局不同 | 统一 API Key 输入框宽度 |
| P2 | "保存所有配置"按钮位置 | 应用底部左侧有保存按钮；设计稿中按钮位置或文案不同 | 统一按钮位置与文案 |

---

### 10. settings-appearance（外观设置）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 主题模式卡片选中态 | 应用"深色模式"卡片右上角有蓝色圆点选中标记；设计稿选中态为卡片蓝色边框 | 统一选中态样式 |
| P1 | 强调色圆圈样式 | 应用强调色为实心圆；设计稿可能带白色边框或选中环 | 统一强调色选择器样式 |
| P2 | 字号/行距滑块数值显示 | 应用显示 13px/1.5；设计稿显示 13px/1.5 但布局略有不同 | 微调数值标签位置 |
| P2 | 代码高亮主题卡片 | 应用四个主题卡片为 2x2 网格；设计稿也是但边框/选中态不同 | 统一代码主题卡片边框 |

---

### 11. settings-risk（风险控制）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 命令风险评级规则表格 | 应用表格中"操作"列为图标按钮；设计稿为"编辑/删除"文字按钮 | 统一操作按钮样式 |
| P1 | 风险等级徽章颜色 | 应用中"极高/高/中/低/无"徽章颜色与设计稿略有差异 | 统一风险等级颜色 token |
| P2 | 安全防护等级卡片 | 应用选中"严格"卡片为蓝色填充；设计稿选中态可能不同 | 统一单选卡片选中态 |
| P2 | 审计日志设置项行高 | 应用设置项行高略大于设计稿 | 统一设置项行高 |

---

### 12. settings-ssh（SSH 连接）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 服务器列表操作按钮 | 应用为"连接/编辑/断开"文字按钮；设计稿为图标按钮或不同分组 | 统一操作按钮样式 |
| P1 | SSH 密钥管理卡片 | 应用密钥项右侧为红色删除图标；设计稿为"删除"文字按钮 | 统一密钥操作按钮样式 |
| P2 | 已连接服务器状态点 | 应用状态点为彩色小圆点；设计稿状态点样式略有不同 | 统一状态指示器样式 |
| P2 | 滑块数值标签 | 应用连接超时/Keep Alive 滑块右侧数值与设计稿对齐不同 | 调整数值标签对齐 |

---

### 13. settings-terminal（终端设置）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 开关颜色/尺寸 | 应用开关为蓝色圆角；设计稿开关样式可能不同 | 统一开关组件样式 |
| P1 | 滑块当前值显示 | 应用字体大小 16px/行高 1.5；设计稿显示 14px/1.4 | 确认默认值并统一显示 |
| P2 | 分组标题与说明文字间距 | 应用部分分组说明文字与标题间距略小 | 调整分组内间距 |
| P2 | 高级设置项展开状态 | 应用高级设置直接展开；设计稿可能有折叠/展开交互 | 确认是否需要折叠面板 |

---

### 14. settings-decision（决策控制）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 决策流程配置开关 | 应用中开关与风险说明文字对齐不同；设计稿更紧凑 | 统一开关与标签对齐 |
| P1 | 证据源权重滑块 | 应用滑块数值在右侧；设计稿滑块与数值布局略有不同 | 统一滑块与数值布局 |
| P2 | 风险控制策略命令列表 | 应用命令列表为纯文本；设计稿可能有代码块样式 | 统一命令列表样式 |
| P2 | 告警通知方式复选框 | 应用复选框样式与设计稿不同 | 统一复选框样式 |

---

### 15. settings-about（关于）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 信息列表布局 | 应用信息项为两列（label + value）；设计稿为更宽松的上下结构 | 统一信息列表布局 |
| P1 | 链接卡片样式 | 应用链接卡片为深色填充带图标；设计稿卡片更扁平 | 统一链接卡片样式 |
| P2 | Logo 与标题间距 | 应用 Logo 与标题间距略小于设计稿 | 调整间距 |
| P2 | 底部版权信息位置 | 应用版权信息靠左；设计稿居中 | 统一版权信息位置 |

---

### 16. history-detail（决策记录详情）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 决策记录标题格式 | 应用标题为"决策记录 #1"；设计稿为"决策记录 #DEC-2024-0718-001" | 统一标题格式为完整编号 |
| P1 | 执行命令卡片 | 应用中命令行背景与设计稿不同，且缺少"热加载 nginx 配置"说明 | 按设计稿完善命令卡片 |
| P2 | 证据溯源链步骤徽章 | 应用步骤为蓝色圆点+文字；设计稿步骤状态徽章更明显 | 统一步骤状态样式 |
| P2 | 执行结果表格 | 应用表格列宽与设计稿略有不同 | 调整表格列宽 |

---

### 17. knowledge-detail（知识详情）

**整体匹配度：高**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 顶部面包屑/标签栏 | 应用顶部显示"返回知识库"+标签；设计稿面包屑样式不同 | 统一顶部导航样式 |
| P1 | 代码块 caption/标题 | 应用代码块缺少明确的 caption 标题；设计稿代码块有"BASH"等标签 | 为代码块添加语言/标题标签 |
| P2 | 右侧目录高亮 | 应用目录高亮项与设计稿当前项不同 | 统一目录高亮样式 |
| P2 | 知识可信度圆环 | 应用显示"92%"圆环；设计稿位置/大小略有不同 | 微调圆环位置与大小 |

---

### 18. decision-detail（决策详情）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 顶部信息条布局 | 应用顶部"决策ID/时间/标签/操作按钮"排列与设计稿不同 | 统一顶部信息条布局 |
| P1 | 命令操作按钮 | 应用命令卡片中为"采纳执行/修改/拒绝"；设计稿为"采纳执行/修改/拒绝"但样式不同 | 统一命令操作按钮样式 |
| P2 | 置信度仪表盘尺寸 | 应用仪表盘略小于设计稿 | 调整仪表盘尺寸 |
| P2 | 证据源权重条形图 | 应用条形图数值在右侧；设计稿布局略有不同 | 统一条形图布局 |

---

### 19. tutorial-detail（教程学习页）

**整体匹配度：中**

| severity | 描述 | 设计稿位置 / 应用现状 | 修复建议 |
|----------|------|----------------------|----------|
| P1 | 顶部进度条样式 | 应用进度条为节点+连接线+对勾；设计稿为简洁圆点+横线 | 统一进度条样式 |
| P1 | 当前学习 badge 位置 | 应用"当前学习"蓝色 badge 位于章节标题上方；设计稿位置不同 | 按设计稿调整 badge 位置 |
| P1 | 代码块 caption | 应用代码块缺少"BASH"等语言标签；设计稿有明确 caption | 为代码块添加 caption |
| P1 | 课程目录选中态 | 应用目录选中项为蓝色背景；设计稿为左侧蓝色竖条+文字高亮 | 统一目录选中态 |
| P2 | 卡片间距 | 应用学习内容卡片与右侧卡片间距略大 | 调整卡片间距 |
| P2 | 底部进度条 | 应用底部有蓝色学习进度条；设计稿未显示或样式不同 | 确认底部进度条是否保留 |

---

## 按优先级排序的修复建议清单

### P0 - 必须立即修复

1. **boot 启动页背景与标题**：应用当前为深色光带背景+居中白色标题，设计稿为纯白背景+左上角黑色标题。需完全重构启动页视觉。
2. **boot "进入工作台"入口**：确认入口是否应显示；若按设计稿应极简，则隐藏或弱化入口。
3. **tutorial 统计卡片布局**：将横向排列改为设计稿的纵向堆叠。
4. **tutorial 精选课程卡片列数**：将双列卡片改为设计稿的单列布局。
5. **全局横向 vs 纵向统计卡片一致性**：history/settings-model 等页面也存在同样问题，需统一为设计稿的纵向堆叠+迷你趋势图风格。

### P1 - 影响设计还原（按页面聚合）

6. **workbench**：统一标题栏、右侧边栏标题间距、移除/确认右下角悬浮按钮。
7. **tutorial**：移除或确认"推荐学习路径"区域，统一标签与课程卡片内部样式。
8. **history**：统一统计卡片布局、列表项字段、筛选标签样式。
9. **knowledge**：统一搜索栏、标签、知识列表项、右侧边栏标题样式。
10. **logs**：补全"导出 CSV"按钮，统一日志级别标签与选中态。
11. **settings-general**：统一设置项对齐、按钮样式、输入框样式。
12. **settings-model**：统一统计卡片布局、温度滑块、Thinking Effort 选项、API Key 输入框。
13. **settings-appearance**：统一主题卡片选中态、强调色选择器样式。
14. **settings-risk**：统一表格操作按钮、风险等级徽章颜色。
15. **settings-ssh**：统一服务器/密钥操作按钮样式。
16. **settings-terminal**：统一开关、滑块数值显示。
17. **settings-decision**：统一开关对齐、滑块布局。
18. **settings-about**：统一信息列表布局、链接卡片样式。
19. **history-detail**：统一标题格式、命令卡片样式。
20. **knowledge-detail**：统一顶部导航、代码块 caption。
21. **decision-detail**：统一顶部信息条、命令操作按钮样式。
22. **tutorial-detail**：统一进度条、当前学习 badge、代码块 caption、目录选中态。

### P2 - 细节打磨

23. 统一所有页面的开关、滑块、复选框、按钮等控件样式。
24. 统一卡片圆角、阴影、边框颜色，检查是否使用 `--trae-*` token。
25. 统一列表项行高、间距、分页器位置。
26. 统一终端/代码块的主题色与 caption 标签。
27. 统一设置页分组标题字号与说明文字间距。
28. 统一底部状态栏/版权信息位置。
29. 检查并替换肉眼可辨的硬编码颜色（如纯黑 `#000`、纯白 `#fff`、告警红等），改为 token。
30. 更新 design-monitor.html 设计稿以匹配新版大数字+迷你折线图样式。

---

## 附录：常见系统性问题

| 问题类型 | 出现页面 | 说明 |
|----------|----------|------|
| 统计卡片横向排列 | tutorial, history, settings-model | 设计稿多为纵向堆叠+趋势图 |
| 操作按钮样式不统一 | settings-risk, settings-ssh, decision-detail | 有的用图标按钮，有的用文字按钮 |
| 滑块/开关控件差异 | settings-model, settings-terminal, settings-decision | 需要统一组件库 |
| 代码块缺少 caption | knowledge-detail, tutorial-detail | 设计稿均有语言/标题标签 |
| 列表项密度不同 | tutorial, history, knowledge | 应用更紧凑，设计稿更宽松 |

---

*报告结束*
