# TDSF-Linux Desktop 前端交付说明

> 版本：1.0.0 | 日期：2026-07-25 | 分支：feat/p0-type-declarations

## 一、项目概述

TDSF-Linux Desktop 是面向 Linux 运维初学者的人机协同可信决策桌面助手，基于 Electron + React 18 + TypeScript + Vite 构建。产品核心功能涵盖 SSH 终端、AI 辅助问答、高危命令拦截、日志分析、实时监控和运维教程六大模块，通过可视化决策内核实现 Human-in-the-Loop 的可信运维工作流。

技术栈：Electron 28（electron-vite 构建）、React 18、TypeScript 5、Vite 5、Ant Design 5、Zustand 4、Tailwind CSS 3、recharts 2、xterm.js 5、Monaco Editor。

三进程架构遵循 Electron 安全三原则：contextIsolation: true、nodeIntegration: false、sandbox: true。主进程负责 SSH2 连接、LLM 调度、SQLite 持久化、可信度算法；Preload 通过 contextBridge 暴露 55+ IPC 通道；渲染进程在沙箱中运行 React SPA，通过 IPC 与主进程通信。

## 二、代码规模

| 层               | 代码行数 | 文件数                                        | 说明         |
| ---------------- | -------- | --------------------------------------------- | ------------ |
| 渲染进程（前端） | ~70,000  | 23 页面 + 154 组件 + 48 CSS + 10 Store        | 本次交付主体 |
| 主进程           | ~70,000  | Agent + SSH + Credibility + Langfuse + Skills | 后端服务     |
| Preload          | ~3,700   | contextBridge 安全桥接                        | IPC 声明     |
| Shared 类型      | ~4,200   | 跨进程共享模型与类型                          | 契约层       |
| 合计             | ~148,000 | —                                            | 全栈         |

Git 提交记录共 156 次，从 M1（工作台）到 M5（设置页），覆盖 5 个里程碑模块的完整开发周期。

## 三、页面路由（22 条路由，21 张设计稿）

全部页面采用 HashRouter + React.lazy 懒加载，设计稿来源为 tds-linux-redesign/pages/ 下的 21 个 HTML 文件，实现逐一对照还原。

| 路由                 | 页面组件            | 设计稿                   | 模块 | 说明                                                    |
| -------------------- | ------------------- | ------------------------ | ---- | ------------------------------------------------------- |
| /boot                | BootPage            | boot.html                | M5   | 启动加载页，4 步进度同步                                |
| /workbench           | WorkbenchPage       | workbench-ai.html        | M1   | IDE 布局：FileTree + Editor + Terminal + AIPanel        |
| /monitor             | MonitorPage         | monitor.html             | M3   | 实时监控：4 KPI 环形图 + 4 时序图表 + 告警表 + 进程列表 |
| /decision            | DecisionPage        | decision-detail.html     | M2   | 可信决策内核：置信度仪表 + 证据融合 + 7 步时间线        |
| /decision/:id        | DecisionDetailPage  | decision-detail.html     | M2   | 决策详情：摘要条 + DAG + 审批流程                       |
| /history             | HistoryPage         | history.html             | M2   | 决策历史列表：卡片 + sparkline + 筛选                   |
| /history/:id         | HistoryDetailPage   | history-detail.html      | M2   | 历史详情：4 卡片归档视图                                |
| /knowledge           | KnowledgePage       | knowledge.html           | M4   | 知识库：分类网格 + 语义搜索开关                         |
| /knowledge/:id       | KnowledgeDetailPage | knowledge-detail.html    | M4   | 知识点详情：命令解释 + 语法 + 反馈持久化                |
| /tutorial            | TutorialPage        | tutorial.html            | M4   | 教程目录：分类 + 搜索 + 学习路径时间线                  |
| /tutorial/:id        | TutorialDetailPage  | tutorial-detail.html     | M4   | 教程详情：章节进度 + Markdown 渲染 + 沙箱 + 测验        |
| /logs                | LogsPage            | logs.html                | M3   | 日志查看：源切换 + 实时流 + 虚拟滚动 + AI 分析          |
| /settings            | SettingsPage        | settings.html            | M5   | 设置首页：9 卡片网格 + 字段预览                         |
| /settings/general    | GeneralSettings     | settings-general.html    | M5   | 通用设置                                                |
| /settings/ssh        | SshSettings         | settings-ssh.html        | M5   | SSH 服务器管理：4 卡片（Server/Key/Defaults/Security）  |
| /settings/model      | ModelSettings       | settings-model.html      | M5   | AI 引擎配置：7 Section 组件                             |
| /settings/decision   | DecisionSettings    | settings-decision.html   | M5   | 决策控制：6 源权重                                      |
| /settings/risk       | RiskSettings        | settings-risk.html       | M5   | 风险控制：规则编辑弹窗                                  |
| /settings/terminal   | TerminalSettings    | settings-terminal.html   | M5   | 终端设置                                                |
| /settings/appearance | AppearanceSettings  | settings-appearance.html | M5   | 外观设置                                                |
| /settings/about      | AboutSettings       | settings-about.html      | M5   | 关于页                                                  |

## 四、设计系统

### 4.1 Token 体系

设计 token 采用三层架构，确保设计稿与实现一致性：

第一层是 trae-tokens.css，定义全局 CSS 变量（--trae- 前缀），涵盖配色（bg/text/border/status 四类语义色）、字体（SF Pro Text + Microsoft YaHei + JetBrains Mono 三栈）、间距（spacer 2-48px 八级）、圆角（2/4/6/8/full 五级）、阴影（card/elevated 两级）、动画时长（instant/base/moderate/slow 四档）。品牌色 #387BFF（科技蓝），暗色基底 #1A1B1D，次级背景 #222427，卡片背景 #2A2D31。

第二层是 design-tokens.css，将设计稿 HTML 中的无前缀变量（--bg-brand 等）桥接到 --trae- 前缀变量，使设计稿 CSS 可直接复用。

第三层是 antd-tokens.ts，将 CSS 变量对应的 hex 值同步到 Ant Design ConfigProvider 的 JS token 对象，包括 colorPrimary、colorSuccess（#33C192）、colorWarning（#D29D00）、colorError（#F65A5A）等语义色，确保 AntD 组件与自定义组件颜色一致。

### 4.2 组件覆盖

global.css 对 Ant Design 全部组件做了暗色主题覆盖，包括 Modal（圆角 8px + 遮罩 blur 4px）、Message（padding 8px 16px + 语义色图标 + shadow 0 4px 16px）、Notification、Input、Button、Tag、Switch、Tooltip、Tabs 等 15+ 组件，所有值引用 --trae- token，无硬编码 hex。

### 4.3 布局规范

工作台采用 IDE 风格布局：48px ActivityRail（左侧导航栏）+ 40px TitleBar（顶部标题栏含服务器选择器）+ 200px FileTree + flex EditorArea + 560px AIPanel + 24px StatusBar。三个面板标题栏统一 40px 高度，flex align-items: center 确保基线对齐。

所有页面遵循 16px 容器内边距 + 10px header 下边距 + 1px 分割线的间距规范，section 间用 mb-3（12px）分隔。

## 五、状态管理

10 个 Zustand store 分别管理不同领域状态：

agent-store 管理 AI Agent 工作流状态（loop 步骤、决策卡片、阻塞信息）。ai-store 管理 AI 对话历史和流式响应。editor-store 管理编辑器标签页和文件树展开状态。monitor-store 管理实时监控数据（CPU/内存/磁盘/网络）和系统信息。server-store 管理 SSH 服务器配置和连接状态，支持 zustand/persist + IPC 双层持久化（敏感字段在 localStorage 中自动剥离）。settings-store 管理全局设置。terminal-store 管理 xterm.js 终端会话。theme-store 管理暗/亮主题切换。translate-store 管理命令翻译缓存。workbench-store 管理工作台布局（面板可见性、活动标签）。

## 六、近期交付内容（比赛冲刺阶段）

### 6.1 全局设计稿对齐（commit e7a3d15）

14 个文件跨 6 个页面模块逐一对照设计稿修复差异。工作台模块修复终端空状态文案（"连接 SSH 后..." → "终端待连接"）、文件树空状态文案（"未连接服务器" → "尚未连接 SSH服务器"）、状态栏 AI 标签（"AI已就绪" → "AI已激活"）、标题栏断连态按钮（条件隐藏 → 始终可见）。决策模块修复历史详情卡片文案和按钮、知识详情按钮、决策详情返回按钮隐藏、执行结果 data-dom-id。教程模块将学习路径从 3 卡片滚动器重写为 5 节点水平时间线。设置模块修复置信度仪表中心字号（48px → 40px）、终端设置新增"登录提示"开关。

### 6.2 六项视觉修复（commit 44f26f7）

第一，教程排版问题。TutorialDetailPage 原先将真实教程数据的 Markdown 字符串按空行分段渲染为纯 p 标签，导致 ## 标题、> 引用、代码块语法显示为原始文本。新增轻量 Markdown 渲染器（renderMarkdownContent 函数），支持 ## / ### 标题、> 引用块、三反引号代码块（含注释/命令语法着色）、内联 code 片段，输出带 tut-md-* CSS 类的 React 元素，对齐 tutorial-detail.html 的 article > h2/p/pre/alert 结构。

第二，可信决策页顶部间距。DecisionPage 标题字号从 24px 提升至 28px（对齐 DecisionDetailPage 和设计稿 --heading-2xl），标题与副标题间距从 gap-0.5（2px）提升至 gap-1（4px），消除拥挤感。

第三，时间选择器 UI。MonitorPage.css 为 1H/6H/24H 分段控制器添加 transition 平滑过渡（color/background 0.15s ease）、min-width: 36px 确保等宽、非活跃段 hover 态（bg-overlay-l1 + text-default）、focus-visible 轮廓（2px brand blue + offset 2px）。

第四，服务器选择器列表。WorkbenchTitlebar 服务器下拉面板从 260px 简单列表重写为 320px 设计规范面板：顶部搜索输入框（bg-overlay-l1 圆角容器 + 搜索图标 + placeholder "搜索服务器..."）、服务器列表项（Server 图标 + 名称 + host:port·username 元信息 + 状态点 + 在线/超时状态标签）、底部"添加新连接"按钮（虚线边框 + brand hover 态）。触发按钮也重新设计：状态点 + Server 图标 + IP 地址（JetBrains Mono 11px tabular-nums）+ 分隔点 + 用户名 + 旋转 ChevronDown。

第五，弹窗卡片可读性。antd-tokens.ts 新增 colorSuccess（#33C192）、colorWarning（#D29D00）、colorError（#F65A5A）三组语义色 token（含 hover/active 变体），确保 Ant Design message.success 等组件的图标颜色与设计系统一致。global.css 增强 .ant-message-notice-content（padding 8px 16px + shadow 加深 + border 升级到 l2 + flex 布局 + 图标字号 16px + 四类语义色图标覆盖）。SshSettings.css 的 .ssh-feedback 从裸 10px 绿色文字改为带背景（success-surface-l1）、边框（neutral-l1）、圆角（radius-4）、内边距（4px 10px）的 badge 组件。

第六，工作台三面板标题基线。FileTree 标题栏（.wb-filetree-header）从 32px 提升至 40px，AIPanelHeader 从 h-8（32px）提升至 h-10（40px），与 Terminal 标签栏（.term-tab-bar 40px）统一，三面板标题文字垂直居中对齐。

### 6.3 面板基线对齐（commit 1ae9df3）

工作台三面板标题栏高度统一为 40px：资源管理器（.wb-filetree-header）32px→40px，AI 运维助手（AIPanelHeader h-8→h-10）32px→40px，终端标签栏（.term-tab-bar）保持 40px。三者均使用 flex align-items: center，高度一致后文字垂直居中位置对齐，基线统一在同一水平线上。

## 七、构建与运行

开发模式：pnpm dev 启动 electron-vite dev，渲染进程热更新，主进程自动重启。开发服务器地址 localhost:9879。

生产构建：pnpm build 执行 electron-vite build，输出到 out/ 目录。pnpm build:win 执行构建 + electron-builder --win 生成 Windows 安装包。

代码质量：pnpm typecheck 执行主进程和渲染进程 TypeScript 类型检查。pnpm lint 执行 ESLint（src 目录，.ts/.tsx 文件）。pnpm test 执行 Vitest 单元测试。

设计稿预览：在 tdsf-linux-redesign 目录执行 npx serve -l 8899 可预览 21 个设计 HTML 页面，地址 localhost:8899/pages/{page-name}。

## 八、Agent 系统（后端，前端对接说明）

渲染进程通过 IPC 与主进程 Agent 系统交互。Agent 模块包含 Task Protocol（14 步任务协议，~1,920 行）、MCP 工具（25 个工具 6 大域，~1,500 行）、Credibility 可信度评估（D-S 证据理论 + PCR5 融合，~2,000 行，Demo 阶段降级为简单规则）、Langfuse 追踪（~600 行，DEV 无 Key 自动降级）、Supervisor 编排（PAOR 循环 + 审批 + 流式，~1,146 行）、Claude SDK 集成（双重包装 + 动态 import，~1,000 行）。

前端通过以下 IPC 通道与 Agent 对接：onLoopStep / onLoopDecision / onLoopDone / onLoopError / onLoopBlocked 接收决策循环事件流，loopConfirm / loopCancel 发送审批结果，onAgentStep 接收 AI 流式步骤，sandboxApprove 响应沙箱执行审批。DecisionPage 实时渲染置信度仪表（ConfidenceGauge）、命令决策终端（CommandTerminal）、7 步时间线、六源证据融合面板（EvidenceRadar + EvidenceList）。

## 九、已知限制与后续迭代

Demo 阶段以下模块走简化路径，接口保留待后续迭代：D-S 证据理论降级为简单规则（高/中/低），完整融合待 v3.1 恢复；PCR5 融合降级为简单加权；Langfuse 在 DEV 模式无 Key 时自动降级不启用；ECE 校准跳过；EU AI Act 合规跳过；CoT 熵轨迹数据收集保留但可视化跳过。

服务器选择器下拉面板的 CPU/MEM 实时统计数据尚未接入（需从 MonitorData IPC 事件聚合），当前显示连接状态标签。教程 Markdown 渲染器为轻量实现，不支持表格、列表嵌套、HTML 内嵌等高级语法，如需完整支持可后续引入 react-markdown + remark-gfm。

## 十、交付清单

本次交付包含完整前端代码（src/renderer/ 全部 23 页面 + 154 组件 + 48 CSS + 10 Store）、共享类型定义（src/shared/）、Preload IPC 声明（src/preload/）、TypeScript 配置（tsconfig.web.json 含 @main/* 路径别名）、构建配置（electron-vite + electron-builder）、设计稿参考（tdsf-linux-redesign/pages/ 21 个 HTML）。Git 分支 feat/p0-type-declarations，HEAD commit 1ae9df3，工作树干净。

---
