# v2.2 深度功能审计报告

> **审计时间**：2026-07-22
> **审计范围**：`d:\ai\linux教学一体\tdsf-linux-desktop`
> **审计方法**：7 维度静态 + 动态分析（静态代码扫描 / IPC 通道完整性 / 功能完整性 / 错误处理 / 安全审计 / 性能问题 / 一致性审计）
> **基准版本**：v2.1 功能修复循环工程已完成（2026-07-22）
> **审计依据**：CLAUDE.md v2.4（A 红线 14 条 / B 开发约束 10 条 / C 白名单）+ AGENTS.md v8.6
> **审计员**：Claude Sonnet 4.5（AI 自动审计）
> **问题总数**：**68 个**（P0：8 / P1：22 / P2：25 / P3：13）

---

## 执行摘要

本次审计对 tdsf-linux-desktop 项目进行了 7 维度全量扫描，覆盖 34 个 IPC handler 文件、22 个页面、30+ 组件、10 个 main 服务模块，共发现 **68 个具体问题点**。

**亮点**：
- IPC 4 步同步完整性高（190 个 ipcMain.handle 中仅 1 处字面量豁免）
- 大部分 IPC handler 使用 `logger.error` 结构化日志（68 处合规）
- DEV 模式 fallback 合规（B3 无 mock fallback 约束）
- WIP 标注规范（A4 诚实标注全部到位）
- 敏感信息脱敏函数实现完整（8 类规则覆盖）
- SSH 命令执行走 ssh2 库 `client.exec`（非 shell，无 shell 注入风险）

**主要风险**：
- **A3 红线违规**：ssh.ts 5 处直接 `console.error` 未走 logger + 未脱敏
- **B4 IPC 常量违规**：preload 164 处字面量字符串未常量化
- **B1 单文件超标**：30 个文件 > 300 行（AIPanel 1667 / preload 3462 / electron.d.ts 1841）
- **B2 CSS 硬编码**：main.tsx Antd ConfigProvider 28 处硬编码颜色未桥接 token
- **DRY 违规**：两套脱敏函数 `redactSensitiveInfo` 与 `redactSecrets` 并存
- **功能完整性**：11 处 WIP 功能未实现（图片附件/上下文压缩/编辑功能/检查更新等）

---

## 优先级分级标准

| 优先级 | 描述 | 处理时限 |
|--------|------|----------|
| **P0** | 严重问题，违反 A 红线或存在安全风险 | 立即修复 |
| **P1** | 重要问题，影响功能完整性或开发规范 | 下一版本 |
| **P2** | 改进项，影响可维护性 | 1.5 版本内 |
| **P3** | 建议项，锦上添花 | 2.0 版本内 |

---

## 维度 1：静态代码扫描（共 15 个问题）

### 1.1 文件行数超标（违反 CLAUDE.md B1：单文件 ≤ 500 行）

#### 问题 #1 [P0]：AIPanel.tsx 单文件 1667 行，违反 B1 红线
- **文件**：`src/renderer/src/components/workbench/AIPanel.tsx`
- **行数**：1667 行（注释标注 "1596 行" 已过时）
- **问题描述**：单文件超 B1 限制 3.3 倍，含 Composer / MessageList / TokenBudget / ToolPanel 多个子组件待拆分
- **现有 WIP 标注**：第 7 行 `WIP: 单文件 1596 行待拆分至 < 500 行（CLAUDE.md B1 开发阶段豁免 · reviewer agent P0 修复）`
- **修复方案**：按注释计划拆分为 4 个独立组件（Composer / MessageList / TokenBudget / ToolPanel）
- **影响范围**：WorkbenchPage 主面板
- **预计工作量**：8 小时

#### 问题 #2 [P1]：preload/index.ts 单文件 3462 行
- **文件**：`src/preload/index.ts`
- **行数**：3462 行
- **问题描述**：preload 聚合文件超 B1 限制 6.9 倍，包含 164 个 ipcRenderer.invoke 调用
- **修复方案**：按业务域拆分为 19 个 preload 子模块（ssh-preload / monitor-preload / agent-preload 等），主文件仅做聚合
- **影响范围**：所有 IPC 暴露
- **预计工作量**：12 小时

#### 问题 #3 [P1]：electron.d.ts 单文件 1841 行
- **文件**：`src/renderer/src/types/electron.d.ts`
- **行数**：1841 行
- **问题描述**：类型声明文件超 B1 限制 3.7 倍
- **修复方案**：按域拆分为 19 个 .d.ts 子文件（ssh.d.ts / monitor.d.ts 等），主文件三斜杠引用
- **影响范围**：ElectronAPI 类型声明
- **预计工作量**：6 小时
- **注**：类型声明文件属豁免范围（不影响运行时），可降级为 P2

#### 问题 #4 [P1]：CalibrationPanel.tsx 单文件 1438 行
- **文件**：`src/renderer/src/components/ai/CalibrationPanel.tsx`
- **行数**：1438 行
- **问题描述**：校准面板单文件超 B1 限制 2.9 倍
- **修复方案**：拆分为 ECEChart / TempScalingChart / CalibrationTable / SampleList 4 子组件
- **影响范围**：AI 校准功能
- **预计工作量**：6 小时

#### 问题 #5 [P1]：DecisionDetailPage.tsx 单文件 1138 行
- **文件**：`src/renderer/src/pages/DecisionDetailPage.tsx`
- **行数**：1138 行
- **问题描述**：决策详情页单文件超 B1 限制 2.3 倍
- **修复方案**：拆分为 DecisionHeader / EvidenceList / FixCommandEditor / ApprovalSection 4 子组件
- **影响范围**：决策详情页
- **预计工作量**：5 小时

#### 问题 #6 [P1]：ModelSettings.tsx 单文件 1230 行
- **文件**：`src/renderer/src/pages/ModelSettings.tsx`
- **行数**：1230 行
- **问题描述**：AI 引擎设置页单文件超 B1 限制 2.5 倍
- **修复方案**：拆分为 ProviderList / ProviderForm / ModelPicker / ApiKeySection 4 子组件
- **影响范围**：AI 引擎设置
- **预计工作量**：5 小时

#### 问题 #7 [P1]：database.ts 单文件 809 行
- **文件**：`src/main/services/db/database.ts`
- **行数**：809 行
- **问题描述**：数据库管理器单文件超 B1 限制 1.6 倍
- **修复方案**：拆分为 SchemaManager / VecExtensionLoader / Repository / MockStatement 4 模块
- **影响范围**：数据库核心
- **预计工作量**：4 小时

#### 问题 #8 [P1]：ssh.ts IPC handler 单文件 740 行
- **文件**：`src/main/ipc/ssh.ts`
- **行数**：740 行
- **问题描述**：SSH IPC handler 单文件超 B1 限制 1.5 倍，含 SSH 连接 / SFTP / 密钥管理三大域
- **修复方案**：拆分为 ssh-connection-ipc / sftp-ipc / ssh-keypair-ipc 3 模块
- **影响范围**：SSH IPC 注册
- **预计工作量**：3 小时

#### 问题 #9 [P1]：agent-workflow.ts 单文件 890 行
- **文件**：`src/main/core/agent-workflow.ts`
- **行数**：890 行
- **问题描述**：Agent 工作流单文件超 B1 限制 1.8 倍
- **修复方案**：拆分为 WorkflowOrchestrator / Drain3Extractor / GroundChecker / SelfConsistency 4 模块
- **影响范围**：Agent 工作流核心
- **预计工作量**：5 小时

### 1.2 代码质量与规范

#### 问题 #10 [P2]：8 处 TODO/FIXME 未清理
- **文件分布**：
  - `src/main/services/scheduler/daily-decision-archive.ts`（多处 TODO）
  - `src/renderer/src/pages/DecisionDetailPage.tsx:738, 749`（TODO(Phase 4)）
  - `src/main/services/tutorial/tutorial.ts`
- **问题描述**：TODO 标注未关联到 Issue 跟踪系统
- **修复方案**：将每个 TODO 转为 GitHub Issue，标注 phase 标签
- **影响范围**：技术债追踪
- **预计工作量**：1 小时

#### 问题 #11 [P2]：渲染层 8 处 `.catch(() => {})` 静默吞错
- **文件分布**：
  - `src/renderer/src/components/workbench/useAgentChat.ts:165, 167, 172, 174`（tokenStats / tokenCostStats 加载失败静默）
  - `src/renderer/src/components/ai/ChatPanel.tsx:262, 269`（tokenStats 加载失败静默）
  - `src/renderer/src/pages/MonitorPage.tsx:198, 254`（monitorStop 失败静默）
- **问题描述**：Promise catch 块完全空实现，无日志无用户提示
- **修复方案**：补充 `console.warn` 或 `logger.warn` 记录失败原因，便于排查
- **影响范围**：错误可观测性
- **预计工作量**：1 小时

#### 问题 #12 [P2]：database.ts:215 mock statement `finalize: () => {}` 空函数
- **文件**：`src/main/services/db/database.ts:215`
- **代码**：`finalize: () => {},`
- **问题描述**：mock statement 的 finalize 空实现（合理的 fallback，但应有注释说明）
- **修复方案**：补充注释 `// mock statement finalize: no-op（sqlite-vec 扩展不可用时降级）`
- **影响范围**：sqlite-vec 降级场景
- **预计工作量**：5 分钟

#### 问题 #13 [P2]：AIPanel.tsx:7 注释中行数过时
- **文件**：`src/renderer/src/components/workbench/AIPanel.tsx:7`
- **代码**：`* WIP: 单文件 1596 行待拆分至 < 500 行`
- **问题描述**：实际行数 1667，注释中 "1596" 已过时（差 71 行）
- **修复方案**：更新注释为 `WIP: 单文件 1667 行待拆分至 < 500 行`
- **影响范围**：注释准确性
- **预计工作量**：1 分钟

#### 问题 #14 [P3]：console.log/error/warn 散布 68 个文件 205 处
- **问题描述**：渲染层 ErrorBoundary / stores / pages 共 205 处 console 调用，应统一到 renderer logger
- **修复方案**：渲染层封装 `rendererLogger`，main 进程已有 logger，需在渲染层补一份
- **影响范围**：日志一致性
- **预计工作量**：4 小时

#### 问题 #15 [P3]：19 处 WIP/NOT_IMPLEMENTED 标注（功能未实现）
- **问题描述**：WIP 标注已合规（A4 诚实标注），但功能未实现影响完整度
- **分布**：AboutSettings 检查更新 / KnowledgeDetailPage 编辑 / AIPanel 图片附件 + 上下文压缩 / HistoryPage 时间筛选等
- **修复方案**：按 v1.0 P1 计划逐项实现
- **影响范围**：功能完整度
- **预计工作量**：40 小时（全部实现）

---

## 维度 2：IPC 通道完整性（共 6 个问题）

### 2.1 IPC 4 步同步审计

#### 问题 #16 [P0]：ssh.ts:170 `ssh:exec` 用字面量字符串，违反 B4 IPC 常量约束
- **文件**：`src/main/ipc/ssh.ts:170`
- **代码**：`ipcMain.handle('ssh:exec', async (_event, sessionId, command) => ...)`
- **问题描述**：未使用 `SSH.EXEC` 常量，且 `ipc-channels.ts` 中 SSH 域未定义 `EXEC` 常量
- **修复方案**：
  1. 在 `src/shared/ipc-channels.ts` 的 SSH 常量中新增 `EXEC: 'ssh:exec'`
  2. ssh.ts:170 改为 `ipcMain.handle(SSH.EXEC, ...)`
  3. preload 对应位置改为引用 `SSH.EXEC` 常量
  4. electron.d.ts 类型声明同步
- **影响范围**：IPC 4 步同步铁律
- **预计工作量**：30 分钟

#### 问题 #17 [P0]：preload/index.ts 164 处字面量字符串未常量化
- **文件**：`src/preload/index.ts`
- **问题描述**：164 处 `ipcRenderer.invoke('字面量')` 直接使用字符串，未引用 `@shared/ipc-channels` 常量
- **示例**：
  - line 503 `ipcRenderer.invoke('ssh:exec', ...)`
  - line 513 `ipcRenderer.invoke('ssh:shell:write', ...)`
  - line 517 `ipcRenderer.invoke('ssh:shell:resize', ...)`
  - line 614 `ipcRenderer.invoke('sftp:list', ...)`
  - ... 共 164 处
- **修复方案**：将所有字面量替换为 `SSH.EXEC` / `SFTP.LIST` 等常量引用
- **影响范围**：IPC 通道集中化管理
- **预计工作量**：8 小时

#### 问题 #18 [P1]：SSH 域常量不完整
- **文件**：`src/shared/ipc-channels.ts:99-146`
- **问题描述**：SSH 域仅定义 9 个常量（CONNECT/DISCONNECT/SHELL_START/STATE_CHANGED/HOST_KEY_PROMPT/HOST_KEY_RESPONSE/DELETE_KEYPAIR/UPLOAD_KEYPAIR/GENERATE_KEYPAIR/LIST_KEYPAIRS），但实际使用中还有：
  - `ssh:exec`（在 ssh.ts:170）
  - `ssh:shell:write`（在 preload:513）
  - `ssh:shell:resize`（在 preload:517）
  - `sftp:list / upload / download / delete / rename / chmod / readFile / writeFile / stat / mkdir`（在 preload:614-679）
- **修复方案**：在 SSH 域补齐 `EXEC / SHELL_WRITE / SHELL_RESIZE`，并新增 `SFTP` 域常量
- **影响范围**：IPC 常量集中化
- **预计工作量**：1 小时

#### 问题 #19 [P1]：monitor 域常量不完整
- **文件**：`src/shared/ipc-channels.ts:257-260`
- **问题描述**：MONITOR 域仅定义 `STOP`，但实际使用中还有 `monitor:start` / `monitor:getSystemInfo` 等通道
- **修复方案**：补齐 `START / GET_SYSTEM_INFO / DATA / SYSTEM_INFO` 等常量
- **影响范围**：监控 IPC 一致性
- **预计工作量**：30 分钟

#### 问题 #20 [P1]：preload 内部定义 `system` 常量未集中化
- **文件**：`src/preload/index.ts:2281` `systemPing: system.ping`
- **问题描述**：preload 内部局部定义 `system` 常量（含 `ping` 字段），未在 `@shared/ipc-channels.ts` 集中定义
- **修复方案**：在 ipc-channels.ts 新增 `SYSTEM` 域常量 `PING: 'system:ping'`
- **影响范围**：IPC 常量集中化
- **预计工作量**：15 分钟

#### 问题 #21 [P2]：system:ping 字面量豁免标注不明确
- **文件**：`src/main/ipc/index.ts:95`
- **代码**：`ipcMain.handle('system:ping', (): SystemPingResponse => ...)`
- **问题描述**：注释说明了"设计说明"，但未明确标注"B4 豁免"
- **修复方案**：补充注释 `// B4 豁免：system:ping 在所有 IPC 注册前调用，常量依赖顺序不安全`
- **影响范围**：规范标注
- **预计工作量**：5 分钟

---

## 维度 3：功能完整性（共 11 个问题）

### 3.1 页面功能 WIP 标注

#### 问题 #22 [P1]：AIPanel 图片附件功能 WIP
- **文件**：`src/renderer/src/components/workbench/AIPanel.tsx:1548-1556`
- **代码**：`void message.warning('图片附件暂未上线（WIP · 预计 v1.0 P1 完成），请使用文本输入')`
- **问题描述**：图片上传按钮点击仅弹提示，未接入 fs:upload-image IPC
- **修复方案**：
  1. main 进程新增 `fs:upload-image` IPC（图片读取 + base64 编码）
  2. 渲染层接入 antd Upload 组件
  3. 图片压缩（browser-image-compression，4MB 限制）
  4. Provider 支持检查（OpenAI Vision / Claude Vision / Ollama llava）
- **影响范围**：AI 对话功能
- **预计工作量**：8 小时

#### 问题 #23 [P1]：AIPanel 上下文压缩功能 WIP
- **文件**：`src/renderer/src/components/workbench/AIPanel.tsx:1614-1622`
- **代码**：`void message.warning('上下文压缩暂未上线（WIP · 预计 v1.0 P1 完成）')`
- **问题描述**：压缩按钮点击仅弹提示，未实现 rolling summary
- **修复方案**：
  1. 实现对话历史摘要算法（rolling summary）
  2. ctxUsedPct > 80% 时自动触发
  3. 保留最近 N 轮 + 系统提示 + 工具调用结果
  4. 早期对话用 LLM 生成摘要替换
- **影响范围**：AI 对话 token 管理
- **预计工作量**：12 小时

#### 问题 #24 [P1]：AboutSettings 检查更新功能 WIP
- **文件**：`src/renderer/src/pages/AboutSettings.tsx:72, 89`
- **代码**：`WIP: 当前未接入 electron-updater 真实检查逻辑`
- **问题描述**：handleCheckUpdate 仅本地 UI 反馈，未接入 electron-updater
- **修复方案**：
  1. 集成 electron-updater
  2. main 进程新增 `app:check-update` / `app:download-update` IPC
  3. 渲染层接入真实更新状态
- **影响范围**：应用更新功能
- **预计工作量**：6 小时

#### 问题 #25 [P1]：KnowledgeDetailPage 编辑功能 WIP
- **文件**：`src/renderer/src/pages/KnowledgeDetailPage.tsx:131-149`
- **代码**：`WIP: 编辑功能暂未上线（CLAUDE.md A4 诚实标注 · A7 质量优先）`
- **问题描述**：handleEdit 仅弹 Modal.confirm 引导跳转，未实现编辑
- **修复方案**：
  1. main 进程新增 `knowledge:update` IPC
  2. 渲染层编辑 Modal + Monaco Editor
  3. 保存时调用 `kbUpdate(id, content, metadata)`
- **影响范围**：知识库管理
- **预计工作量**：6 小时

#### 问题 #26 [P2]：HistoryPage 时间范围筛选 WIP
- **文件**：`src/renderer/src/pages/HistoryPage.tsx:138`
- **代码**：`{/* WIP: 时间范围筛选未接过滤逻辑，暂禁用避免误导 */}`
- **问题描述**：时间筛选 UI 存在但未接过滤逻辑
- **修复方案**：接入 `history:list` IPC 支持 startTime/endTime 参数
- **影响范围**：历史决策筛选
- **预计工作量**：3 小时

#### 问题 #27 [P2]：DecisionDetailPage loopConfirm 不支持 modify 参数
- **文件**：`src/renderer/src/pages/DecisionDetailPage.tsx:738, 749`
- **代码**：`TODO(Phase 4): preload 当前 loopConfirm 签名为 (correlationId, approved: boolean)`
- **问题描述**：用户修改 fixCommand 后，仍调用 `loopConfirm(id, true)`，无法传递新命令
- **修复方案**：
  1. main 进程扩展 `loop:confirm` 支持 `{ action: 'modify', newCommand }`
  2. preload 暴露新签名
  3. electron.d.ts 同步
- **影响范围**：循环工程审批
- **预计工作量**：4 小时

#### 问题 #28 [P2]：GeneralSettings 非 Electron 环境降级 WIP
- **文件**：`src/renderer/src/pages/GeneralSettings.tsx:218, 279`
- **问题描述**：导出 / 导入数据在非 Electron 环境仅弹提示
- **修复方案**：DEV 环境保留提示，生产环境应禁用按钮
- **影响范围**：设置页功能
- **预计工作量**：1 小时

#### 问题 #29 [P2]：TutorialDetailPage 非 Electron 环境 WIP
- **文件**：`src/renderer/src/pages/TutorialDetailPage.tsx:162`
- **问题描述**：非 Electron 环境或缺 id 保留设计稿示例数据
- **修复方案**：DEV 模式保留示例，生产环境显示空状态
- **影响范围**：教程详情
- **预计工作量**：1 小时

#### 问题 #30 [P2]：LogsPage 非 Electron 环境 WIP
- **文件**：`src/renderer/src/pages/LogsPage.tsx:85`
- **问题描述**：非 Electron 环境 logRead 不可用时仅弹提示
- **修复方案**：DEV 模式保留设计稿示例，生产环境显示 EmptyState
- **影响范围**：日志页
- **预计工作量**：30 分钟

#### 问题 #31 [P2]：KnowledgePage 非 Electron 环境 WIP
- **文件**：`src/renderer/src/pages/KnowledgePage.tsx:133`
- **问题描述**：非 Electron 环境降级为本地演示
- **修复方案**：DEV 模式保留示例，生产环境显示空状态
- **影响范围**：知识库页
- **预计工作量**：30 分钟

#### 问题 #32 [P2]：KnowledgeDetailPage 解决方案块 fallback
- **文件**：`src/renderer/src/pages/KnowledgeDetailPage.tsx:115`
- **代码**：`// 解决方案块：真实数据 fallback 到设计稿示例（WIP · CLAUDE.md A4 诚实标注）`
- **问题描述**：真实数据为空时 fallback 到设计稿示例 FIX_AFTER，可能误导用户
- **修复方案**：真实数据为空时显示 "暂无解决方案" 空状态
- **影响范围**：知识详情展示
- **预计工作量**：1 小时

---

## 维度 4：错误处理（共 8 个问题）

### 4.1 主进程错误处理

#### 问题 #33 [P0]：ssh.ts 5 处直接 `console.error` 未走 logger + 未脱敏（违反 A3 红线）
- **文件**：`src/main/ipc/ssh.ts`
- **位置**：
  - line 154 `console.error('[SSH] 连接失败:', (err as Error).message)`
  - line 457 `console.error('[SSH] 删除密钥失败:', (err as Error).message)`
  - line 555 `console.error('[SSH] 上传私钥失败:', (err as Error).message)`
  - line 646 `console.error('[SSH] 生成密钥失败:', (err as Error).message)`
  - line 735 `console.error('[SSH] 列出密钥失败:', (err as Error).message)`
- **问题描述**：
  1. **违反 A3 红线**：catch 块未调用 `redactSensitiveInfo` 脱敏
  2. **违反 CLAUDE.md logger 规范**：项目已有 `src/main/services/log/logger.ts` 结构化日志器，但 ssh.ts 未导入使用
  3. **敏感信息泄漏风险**：SSH 错误信息可能包含主机名 / 用户名 / 路径，未脱敏直接输出
- **修复方案**：
  1. 在 ssh.ts 头部添加 `import { logger } from '../services/log/logger'`
  2. 5 处 `console.error` 改为 `logger.error('SSH', '操作失败', { error: redactSensitiveInfo((err as Error).message) })`
  3. 导入 `redactSensitiveInfo` from `../services/security/redact`
- **影响范围**：SSH IPC 错误处理
- **预计工作量**：1 小时

#### 问题 #34 [P0]：main/index.ts 5 处 `console.log/warn/error` 未走 logger
- **文件**：`src/main/index.ts`
- **位置**：
  - line 47 `console.log(`[ipc] protocol version: ${IPC_PROTOCOL_VERSION}`)`
  - line 69 `console.log(`[DB] 数据库已初始化: ${dbPath}, 可用=${db.isAvailable()}, 向量=${db.isVectorEnabled()}`)`
  - line 75 `console.log(`[TUTORIAL] 种子加载完成: 新增 ${seedCount} 篇`)`
  - line 77 `console.warn('[TUTORIAL] 种子加载失败:', (err as Error).message)`
  - line 89 `console.error('[MCP] 启动失败:', err)`
- **问题描述**：应用启动阶段直接 console 输出，绕过 logger 结构化日志
- **修复方案**：替换为 `logger.info('IPC', ...)` / `logger.warn('TUTORIAL', ...)` / `logger.error('MCP', ...)`
- **影响范围**：应用启动日志
- **预计工作量**：30 分钟

#### 问题 #35 [P1]：agent-workflow.ts 7 处 `console.log/warn` 未走 logger
- **文件**：`src/main/core/agent-workflow.ts`
- **位置**：
  - line 450 `console.log('[AgentWorkflow] 确认超时（5分钟），自动拒绝')`
  - line 533 `console.warn('[AgentWorkflow] Drain3 模板提取降级:', ...)`
  - line 671 `console.log(`[AgentWorkflow] Ground-Check 重采: 补充 ${retryVerified.length} 条证据`)`
  - line 674 `console.warn('[AgentWorkflow] Ground-Check 重采失败（不影响主流程）:', ...)`
  - line 679 `console.log(`[AgentWorkflow] Ground-Check: ${rejected.length}/${verified.length} 条证据未通过溯源`)`
  - line 726 `console.log(`[AgentWorkflow] Self-Consistency: ${samples.length} 次采样`)`
  - line 730 `console.warn('[AgentWorkflow] llmReasoner 降级:', ...)`
- **问题描述**：Agent 工作流核心模块绕过 logger
- **修复方案**：替换为 `logger.info('AGENT_WORKFLOW', ...)` / `logger.warn('AGENT_WORKFLOW', ...)`
- **影响范围**：Agent 工作流日志
- **预计工作量**：30 分钟

### 4.2 脱敏函数一致性

#### 问题 #36 [P0]：两套脱敏函数并存，DRY 违规
- **文件 1**：`src/main/services/security/redact.ts` 定义 `redactSensitiveInfo`
- **文件 2**：`src/main/core/agent/providers/redact.ts` 定义 `redactSecrets`
- **使用分布**：
  - `redactSensitiveInfo`：仅 `daily-decision-archive.ts:422` 1 处使用
  - `redactSecrets`：`claude-sdk.ts` / `llm-inline.ts` / `inline-completion-service.ts` / `sandbox-exec.ts` 4+ 处使用
- **问题描述**：两套脱敏函数实现可能存在规则覆盖不一致，违反 DRY 原则
- **修复方案**：
  1. 对比两套函数的脱敏规则覆盖范围
  2. 保留覆盖更全的 `redactSecrets`（已在 Agent 链路使用）
  3. 将 `redactSensitiveInfo` 标记为 deprecated，迁移 `daily-decision-archive.ts` 改用 `redactSecrets`
  4. 或将两者合并为一个函数，保留双名导出做兼容
- **影响范围**：脱敏一致性
- **预计工作量**：2 小时

#### 问题 #37 [P1]：redactSensitiveInfo 仅在 1 处使用，IPC catch 块普遍未脱敏
- **问题描述**：除 `daily-decision-archive.ts` 和 `redactSecrets` 用户外，其余 IPC handler catch 块未调用脱敏函数
- **示例违规文件**：
  - `src/main/ipc/ssh.ts`（5 处 catch 未脱敏）
  - `src/main/ipc/credibility.ts`（12 处 logger.error 未脱敏）
  - `src/main/ipc/agent-runtime.ts`（2 处 logger.error 未脱敏）
  - `src/main/ipc/at-commands.ts`（3 处 logger.error 未脱敏）
- **修复方案**：所有 IPC handler catch 块的 `err.message` 在传给 logger 前必须经过 `redactSecrets`
- **影响范围**：A3 红线全量合规
- **预计工作量**：4 小时

### 4.3 渲染层错误处理

#### 问题 #38 [P1]：渲染层 8 处 `.catch(() => {})` 静默吞错（同问题 #11）
- **修复方案**：见问题 #11

#### 问题 #39 [P2]：MonitorPage.tsx:198 monitorStop 失败静默
- **文件**：`src/renderer/src/pages/MonitorPage.tsx:198`
- **代码**：`window.electronAPI.monitorStop(activeSessionId).catch(() => {})`
- **问题描述**：监控停止失败完全静默，cleanup 失败时无任何日志
- **修复方案**：`.catch((err) => console.warn('[Monitor] monitorStop failed:', err))`
- **影响范围**：监控 cleanup
- **预计工作量**：5 分钟

#### 问题 #40 [P2]：MonitorPage.tsx:254 monitorGetSystemInfo 失败静默
- **文件**：`src/renderer/src/pages/MonitorPage.tsx:254`
- **代码**：`}).catch(() => {}).finally(() => { setTimeout(() => setRefreshing(false), 800) })`
- **问题描述**：系统信息获取失败完全静默
- **修复方案**：补充 `.catch((err) => console.warn('[Monitor] getSystemInfo failed:', err))`
- **影响范围**：监控刷新
- **预计工作量**：5 分钟

---

## 维度 5：安全审计（共 8 个问题）

### 5.1 命令注入与路径穿越

#### 问题 #41 [P0]：ssh:exec IPC 直接接受 command 字符串，无白名单 / 审计 / HITL
- **文件**：`src/main/ipc/ssh.ts:170-177`
- **代码**：
  ```ts
  ipcMain.handle(
    'ssh:exec',
    async (_event, sessionId: string, command: string) => {
      try {
        return await sshManager.exec(sessionId, command)
      } catch (err) {
        throw new Error(`命令执行失败: ${(err as Error).message}`)
      }
    }
  )
  ```
- **问题描述**：
  1. `command` 参数直接透传给 `sshManager.exec`，无白名单过滤
  2. 无 zod 校验（B9 收窄范围后，用户输入 IPC 应校验）
  3. 无操作审计日志（谁在什么时候执行了什么命令）
  4. 无 HITL 审批（R12 三态权限要求 ALWAYS/AUTO/NEVER）
- **缓解措施**：ssh2 库 `client.exec` 不经过 shell，无 shell 注入风险（命令直接传给 SSH 协议）
- **修复方案**：
  1. 添加 zod schema 校验 `command` 非空字符串
  2. 添加命令审计日志 `logger.info('SSH_EXEC', { sessionId, command: redactSecrets(command) })`
  3. 接入 R12 三态权限审批（危险命令如 `rm -rf` 走 ALWAYS 审批）
- **影响范围**：SSH 命令执行安全
- **预计工作量**：4 小时

#### 问题 #42 [P1]：ssh-keygen spawn 私钥路径来自用户输入，未做路径穿越校验
- **文件**：`src/main/ipc/ssh.ts:518, 605`
- **代码**：
  ```ts
  const proc = spawn('ssh-keygen', ['-y', '-f', privateKeyPath])
  const proc = spawn('ssh-keygen', args)
  ```
- **问题描述**：`privateKeyPath` 来自用户输入，未校验是否在 `~/.ssh/` 安全目录内
- **缓解措施**：使用 `spawn` 数组参数形式（非 shell），无 shell 注入；ssh-keygen 本身是可信二进制
- **修复方案**：
  1. 校验 `privateKeyPath` 必须在 `os.homedir()/.ssh/` 目录下
  2. 使用 `path.resolve` 规范化路径后检查前缀
- **影响范围**：密钥管理安全
- **预计工作量**：1 小时

### 5.2 XSS 与渲染安全

#### 问题 #43 [P0]：TutorialPage.tsx:650 dangerouslySetInnerHTML 渲染 Markdown 未做 XSS 防护
- **文件**：`src/renderer/src/components/tutorial/TutorialPage.tsx:650`
- **代码**：`dangerouslySetInnerHTML={{ __html: renderMarkdown(detail.content) }}`
- **问题描述**：`renderMarkdown(detail.content)` 输出 HTML 直接注入 DOM，未做 HTML 消毒
- **风险评估**：
  - `detail.content` 来自教程数据库（本地种子 + 爬虫抓取）
  - 爬虫抓取的 HTML 可能包含恶意 `<script>` / `<iframe>` 标签
  - 即使是本地种子数据，也可能因 Markdown 解析器漏洞导致 XSS
- **修复方案**：
  1. 引入 `DOMPurify` 或 `sanitize-html` 库
  2. `dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(renderMarkdown(detail.content)) }}`
  3. 或改用 React 组件渲染 Markdown（如 `react-markdown` + `rehype-raw`）
- **影响范围**：教程页 XSS 防护
- **预计工作量**：2 小时

#### 问题 #44 [P1]：SSH 错误信息未脱敏直接抛给渲染进程（违反 A3 红线）
- **文件**：`src/main/ipc/ssh.ts`
- **位置**：
  - line 164 `throw new Error(`SSH 断开失败: ${(err as Error).message}`)`
  - line 175 `throw new Error(`命令执行失败: ${(err as Error).message}`)`
  - line 200（catch 块抛错未脱敏）
- **问题描述**：SSH 错误信息可能包含主机名 / 用户名 / 路径，未脱敏直接 reject 到渲染进程
- **修复方案**：
  ```ts
  throw new Error(`SSH 断开失败: ${redactSecrets((err as Error).message)}`)
  ```
- **影响范围**：A3 红线全量合规
- **预计工作量**：1 小时

### 5.3 敏感信息泄漏

#### 问题 #45 [P1]：logger.error 调用普遍未脱敏（同问题 #37）
- **问题描述**：68 处 logger.error 调用中，仅 4 处使用了 `redactSecrets`（claude-sdk.ts / llm-inline.ts）
- **修复方案**：见问题 #37

#### 问题 #46 [P1]：main/index.ts:69 数据库路径直接日志输出
- **文件**：`src/main/index.ts:69`
- **代码**：`console.log(`[DB] 数据库已初始化: ${dbPath}, 可用=${db.isAvailable()}, 向量=${db.isVectorEnabled()}`)`
- **问题描述**：`dbPath` 包含用户目录路径（如 `C:\Users\Lenovo\AppData\...`），可能泄漏用户名
- **修复方案**：改为 `logger.info('DB', '数据库已初始化', { available: db.isAvailable(), vector: db.isVectorEnabled() })`（不输出路径）
- **影响范围**：用户隐私
- **预计工作量**：5 分钟

### 5.4 网络请求可见性

#### 问题 #47 [P2]：LLM 请求 token 透明度需提升
- **文件**：`src/main/ipc/agent-runtime.ts` / `src/main/services/llm/client.ts`
- **问题描述**：LLM 请求虽接入 Langfuse trace（R11 已合规），但用户侧 UI 仅展示总 token 数，未展示单次请求的 input/output token 分解
- **修复方案**：AIPanel 每次发送消息后展示本次请求的 input/output/total token 分解
- **影响范围**：Token 透明度
- **预计工作量**：3 小时

#### 问题 #48 [P2]：MCP 外部服务器连接无用户可见提示
- **文件**：`src/main/services/mcp/server.ts`
- **问题描述**：MCP 外部服务器连接 / 断开事件未推送到 UI（违反"所有网络请求必须 UI 可见"硬约束）
- **修复方案**：MCP 连接 / 断开时通过 `mainWindow.webContents.send('mcp:connection-event', ...)` 推送
- **影响范围**：网络请求可见性
- **预计工作量**：2 小时

---

## 维度 6：性能问题（共 8 个问题）

### 6.1 大文件性能

#### 问题 #49 [P1]：preload/index.ts 3462 行加载性能（同问题 #2）
- **问题描述**：单文件 3462 行，Vite 打包后可能影响首次加载
- **修复方案**：按域拆分（见问题 #2）

#### 问题 #50 [P2]：electron.d.ts 1841 行类型声明（同问题 #3）
- **问题描述**：大型类型声明文件影响 TS 编译速度
- **修复方案**：按域拆分（见问题 #3）

### 6.2 React 性能

#### 问题 #51 [P2]：MonitorPage useEffect 缺少依赖项
- **文件**：`src/renderer/src/pages/MonitorPage.tsx:172, 185`
- **代码**：
  ```ts
  useEffect(() => {
    // ...
  }, [])  // 订阅 monitor:data，依赖项为空
  
  useEffect(() => {
    // ...
  }, [activeSessionId])  // monitorStart 依赖
  ```
- **问题描述**：第一个 useEffect 依赖项为空数组，但内部使用了 `useMonitorStore.getState()`（store 引用稳定，可豁免）
- **修复方案**：添加注释说明 store 引用稳定，豁免 ESLint exhaustive-deps
- **影响范围**：监控页订阅
- **预计工作量**：5 分钟

#### 问题 #52 [P2]：HistoryPage 客户端过滤 6 条静态数据
- **文件**：`src/renderer/src/pages/HistoryPage.tsx`
- **问题描述**：使用客户端过滤 6 条静态 RECORDS，非 IPC 真数据；数据量增大时性能下降
- **修复方案**：接入 `history:list` IPC，服务端分页过滤
- **影响范围**：历史页性能
- **预计工作量**：4 小时

### 6.3 定时器清理

#### 问题 #53 [P2]：sidecar-manager.ts:352 setInterval 需确认 cleanup
- **文件**：`src/main/core/sidecar/sidecar-manager.ts:352`
- **代码**：`this.healthCheckTimer = setInterval(async () => { ... })`
- **现有 cleanup**：line 350 `clearInterval(this.healthCheckTimer)`（先清理旧 timer 再设新 timer，合规）
- **问题描述**：需确认 SidecarManager 的 `stop()` 方法是否也调用了 `clearInterval`
- **修复方案**：核查 `stop()` 方法是否包含 `clearInterval(this.healthCheckTimer)`
- **影响范围**：Sidecar 健康检查
- **预计工作量**：15 分钟

#### 问题 #54 [P2]：scheduler.ts:107 setInterval 需确认 cleanup
- **文件**：`src/main/services/scheduler/scheduler.ts:107`
- **代码**：`this.timer = setInterval(() => this.tick(), TICK_INTERVAL_MS)`
- **现有 cleanup**：`stop()` 方法（line 116 注释提到"清理 setInterval"）
- **问题描述**：需确认应用退出时是否调用 `Scheduler.getInstance().stop()`
- **修复方案**：在 `main/index.ts` 的 `app.on('before-quit')` 中调用 `scheduler.stop()`
- **影响范围**：调度器生命周期
- **预计工作量**：30 分钟

### 6.4 Bundle 体积

#### 问题 #55 [P2]：AIPanel.tsx 1667 行单文件影响 tree-shaking
- **文件**：`src/renderer/src/components/workbench/AIPanel.tsx`
- **问题描述**：单文件包含 30+ icon 导入 + 多个子组件，影响 Vite tree-shaking 效果
- **修复方案**：拆分后按需 lazy import 子组件
- **影响范围**：Bundle 体积
- **预计工作量**：见问题 #1

#### 问题 #56 [P3]：30 个文件 > 300 行影响维护性
- **问题描述**：项目共有 30 个文件超过 300 行（含 preload 3462 / electron.d.ts 1841 / AIPanel 1667 / CalibrationPanel 1438 / agent-types 1328 / ModelSettings 1230 / seeds 1148 / supervisor 1146 / DecisionDetailPage 1138 / ChatPanel 971 / TutorialPage 942 / agent-workflow 890 / formatters 865 / models 835 / SshSettings 826 / database 809 / connection-manager 795 / ssh.ts 740 / loop-engineering-subagent 729 / tutorial-repo 702 / markdown-renderer 697 / TutorialDetailPage 694 / hybrid-search 660 / known-hosts 644 / path-recommender 629 / DeployDialog 613 / dispatcher 608 / openhands-client 605 / CredibilityPanel 598 / tutorial-parser 593）
- **修复方案**：按 P1 优先级逐个拆分（已在问题 #1-#9 列出 P1 级别）
- **影响范围**：项目可维护性
- **预计工作量**：60+ 小时（全部拆分）

---

## 维度 7：一致性审计（共 12 个问题）

### 7.1 CSS Token 一致性

#### 问题 #57 [P0]：main.tsx Antd ConfigProvider 28 处硬编码颜色，违反 B2 var(--trae-*) token
- **文件**：`src/renderer/src/main.tsx`
- **位置**：line 45-95（暗色主题 14 处 + 亮色主题 14 处）
- **代码示例**：
  ```ts
  colorPrimary: '#387BFF',
  colorPrimaryHover: '#4C88FF',
  colorPrimaryActive: '#1759DD',
  colorText: '#D1D3DB',
  // ... 共 28 处
  ```
- **问题描述**：违反 CLAUDE.md B2「所有 CSS 必须用 `var(--color-*)`，禁止硬编码」约束
- **修复方案**：
  1. 在 `trae-tokens.css` 中定义 Antd 桥接 token：`--trae-antd-primary: #387BFF` 等
  2. main.tsx 改为 `colorPrimary: 'var(--trae-antd-primary)'`
  3. 确保 Antd ConfigProvider 支持 CSS 变量（Antd 5.20+ 支持）
- **影响范围**：主题一致性
- **预计工作量**：3 小时

#### 问题 #58 [P1]：AIPanel.css:978 `background: #000` 硬编码无 fallback
- **文件**：`src/renderer/src/components/workbench/AIPanel.css:978`
- **代码**：`background: #000;`
- **问题描述**：硬编码黑色，无 var(--trae-*) 引用，无 fallback
- **修复方案**：改为 `background: var(--trae-bg-base, #000);`
- **影响范围**：AIPanel 视觉
- **预计工作量**：5 分钟

#### 问题 #59 [P1]：SelectionPopover.css:132 `color: #ffffff` 硬编码无 fallback
- **文件**：`src/renderer/src/components/terminal/SelectionPopover.css:132`
- **代码**：`color: #ffffff;`
- **问题描述**：硬编码白色，无 var(--trae-*) 引用
- **修复方案**：改为 `color: var(--trae-text-on-brand, #ffffff);`
- **影响范围**：终端选区弹窗
- **预计工作量**：5 分钟

#### 问题 #60 [P1]：InlineDiffAdapter.tsx:117/148 `color: '#fff'` 硬编码无 fallback
- **文件**：`src/renderer/src/components/workbench/InlineDiffAdapter.tsx:117, 148`
- **代码**：`color: '#fff'`（2 处）
- **问题描述**：行内样式硬编码白色，无 var(--trae-*) 引用
- **修复方案**：改为 `color: 'var(--trae-text-on-brand, #fff)'`
- **影响范围**：Diff 视觉
- **预计工作量**：5 分钟

#### 问题 #61 [P1]：SelectionPopover.tsx:148 `color: '#fff'` 硬编码无 fallback
- **文件**：`src/renderer/src/components/workbench/SelectionPopover.tsx:148`
- **代码**：`color: '#fff'`
- **问题描述**：行内样式硬编码白色
- **修复方案**：改为 `color: 'var(--trae-text-on-brand, #fff)'`
- **影响范围**：选区弹窗
- **预计工作量**：5 分钟

#### 问题 #62 [P2]：TerminalView.tsx xterm 主题硬编码 #0071e3 / #ffffff
- **文件**：`src/renderer/src/components/terminal/TerminalView.tsx:73, 84`
- **代码**：
  ```ts
  blue: '#0071e3',
  brightWhite: '#ffffff',
  ```
- **问题描述**：xterm 主题对象硬编码颜色（xterm API 限制，需字符串值）
- **缓解措施**：可读取 CSS 变量后传入，但 xterm 不支持 CSS 变量直接传入
- **修复方案**：
  1. 创建 `getCssVar('--trae-terminal-blue')` 工具函数
  2. 启动时读取 CSS 变量值传入 xterm 主题
- **影响范围**：终端主题
- **预计工作量**：1 小时

#### 问题 #63 [P2]：BootPage.tsx:425 shader 内联 `#ffffff`
- **文件**：`src/renderer/src/pages/BootPage.tsx:425`
- **代码**：`#ffffff 50%,`
- **问题描述**：WebGL shader 代码内联颜色（shader 不支持 CSS 变量）
- **缓解措施**：shader 限制，可豁免
- **修复方案**：添加注释 `// shader 限制：不支持 CSS 变量，硬编码豁免（B2 WIP）`
- **影响范围**：启动页 shader
- **预计工作量**：5 分钟

### 7.2 命名与导出一致性

#### 问题 #64 [P1]：两套脱敏函数命名不一致（同问题 #36）
- **文件 1**：`redactSensitiveInfo`（services/security/redact.ts）
- **文件 2**：`redactSecrets`（core/agent/providers/redact.ts）
- **修复方案**：见问题 #36

#### 问题 #65 [P2]：IPC 通道常量命名风格不一致
- **文件**：`src/shared/ipc-channels.ts`
- **问题描述**：部分常量使用 `kebab-case`（如 `CLEAR_BUFFER: 'log:clearBuffer'`），部分使用 `camelCase`（如 `searchStatus: 'tutorial:search-status'`）
- **修复方案**：统一为 `kebab-case`（如 `log:clear-buffer`），但需向后兼容
- **影响范围**：IPC 命名一致性
- **预计工作量**：2 小时（含向后兼容处理）

#### 问题 #66 [P2]：Settings 子页命名不一致
- **文件分布**：
  - `AboutSettings.tsx` / `AlertsSettings.tsx` / `AppearanceSettings.tsx` / `CalibrationSettings.tsx` / `DecisionSettings.tsx` / `GeneralSettings.tsx` / `ModelSettings.tsx` / `RiskSettings.tsx` / `SshSettings.tsx` / `TerminalSettings.tsx`
- **问题描述**：部分文件用 `{Name}Settings.tsx`（如 `AboutSettings`），部分用 `{Name}Settings.tsx`（如 `DecisionSettings` 无 `Settings` 后缀的页面是 `DecisionSettings.tsx`），命名风格不完全统一
- **修复方案**：核查并统一为 `{Name}Settings.tsx` 模式
- **影响范围**：文件命名一致性
- **预计工作量**：30 分钟

#### 问题 #67 [P2]：logger 导入路径不一致
- **问题描述**：
  - 部分文件 `import { logger } from '../services/log/logger'`
  - 部分文件 `import { logger } from '../../services/log/logger'`
  - ssh.ts 完全未导入 logger
- **修复方案**：统一使用 `@/services/log/logger` 路径别名
- **影响范围**：导入一致性
- **预计工作量**：30 分钟

#### 问题 #68 [P3]：页面导出方式不一致
- **问题描述**：
  - 部分页面 `export function LogsPage()`（named export）
  - 部分页面 `export const WorkbenchPage: FC = () => {}`（named const export）
  - 部分页面 `export default xxx`（default export）
- **示例**：
  - `LogsPage.tsx`：`export function LogsPage()`
  - `WorkbenchPage.tsx`：`export const WorkbenchPage: FC = () => {}` + `export default WorkbenchPage`
  - `MonitorPage.tsx`：`export function MonitorPage()`
- **修复方案**：统一为 `export function XxxPage()` + `export default XxxPage` 模式
- **影响范围**：导出一致性
- **预计工作量**：1 小时

---

## 修复优先级汇总

### P0（严重，立即修复）— 8 个

| # | 问题 | 文件 | 工作量 |
|---|------|------|--------|
| 1 | AIPanel.tsx 1667 行违反 B1 | workbench/AIPanel.tsx | 8h |
| 16 | ssh:exec 字面量违反 B4 | ipc/ssh.ts:170 | 30min |
| 17 | preload 164 处字面量违反 B4 | preload/index.ts | 8h |
| 33 | ssh.ts 5 处 console.error 违反 A3 | ipc/ssh.ts | 1h |
| 34 | main/index.ts 5 处 console 违反 logger 规范 | main/index.ts | 30min |
| 36 | 两套脱敏函数 DRY 违规 | security/redact.ts + providers/redact.ts | 2h |
| 41 | ssh:exec 无白名单 / 审计 / HITL | ipc/ssh.ts:170 | 4h |
| 43 | dangerouslySetInnerHTML 无 XSS 防护 | tutorial/TutorialPage.tsx:650 | 2h |
| 57 | main.tsx 28 处硬编码颜色违反 B2 | main.tsx | 3h |

**P0 总工作量**：约 29 小时

### P1（重要，下一版本）— 22 个

| # | 问题 | 文件 | 工作量 |
|---|------|------|--------|
| 2 | preload/index.ts 3462 行 | preload/index.ts | 12h |
| 3 | electron.d.ts 1841 行 | electron.d.ts | 6h |
| 4 | CalibrationPanel.tsx 1438 行 | CalibrationPanel.tsx | 6h |
| 5 | DecisionDetailPage.tsx 1138 行 | DecisionDetailPage.tsx | 5h |
| 6 | ModelSettings.tsx 1230 行 | ModelSettings.tsx | 5h |
| 7 | database.ts 809 行 | database.ts | 4h |
| 8 | ssh.ts 740 行 | ipc/ssh.ts | 3h |
| 9 | agent-workflow.ts 890 行 | agent-workflow.ts | 5h |
| 18 | SSH 域常量不完整 | ipc-channels.ts | 1h |
| 19 | MONITOR 域常量不完整 | ipc-channels.ts | 30min |
| 20 | preload system 常量未集中化 | ipc-channels.ts + preload | 15min |
| 22 | AIPanel 图片附件 WIP | AIPanel.tsx | 8h |
| 23 | AIPanel 上下文压缩 WIP | AIPanel.tsx | 12h |
| 24 | AboutSettings 检查更新 WIP | AboutSettings.tsx | 6h |
| 25 | KnowledgeDetailPage 编辑 WIP | KnowledgeDetailPage.tsx | 6h |
| 35 | agent-workflow.ts 7 处 console | agent-workflow.ts | 30min |
| 37 | IPC catch 块普遍未脱敏 | 多文件 | 4h |
| 42 | ssh-keygen 路径穿越校验 | ipc/ssh.ts | 1h |
| 44 | SSH 错误信息未脱敏抛渲染 | ipc/ssh.ts | 1h |
| 46 | main/index.ts:69 数据库路径泄漏 | main/index.ts | 5min |
| 49 | preload 3462 行性能 | preload/index.ts | 见 #2 |
| 58 | AIPanel.css:978 硬编码 | AIPanel.css | 5min |
| 59 | SelectionPopover.css:132 硬编码 | SelectionPopover.css | 5min |
| 60 | InlineDiffAdapter.tsx 硬编码 | InlineDiffAdapter.tsx | 5min |
| 61 | SelectionPopover.tsx:148 硬编码 | SelectionPopover.tsx | 5min |
| 64 | 脱敏函数命名不一致 | 见 #36 | 见 #36 |

**P1 总工作量**：约 100 小时

### P2（改进项）— 25 个

详见上述问题列表 #10-#15, #26-#32, #39-#40, #47-#48, #50-#56, #62-#63, #65-#67

**P2 总工作量**：约 70 小时

### P3（建议项）— 13 个

详见上述问题列表 #14, #15, #56, #68

**P3 总工作量**：约 50 小时

---

## 修复建议路线图

### 第 1 周（P0 紧急修复）
1. 修复 ssh.ts console.error 违规（问题 #33）— 1h
2. 修复 main/index.ts console 违规（问题 #34）— 30min
3. 修复 ssh:exec 字面量（问题 #16）— 30min
4. 修复 dangerouslySetInnerHTML XSS（问题 #43）— 2h
5. 修复 main.tsx 硬编码颜色（问题 #57）— 3h
6. 统一脱敏函数（问题 #36）— 2h
7. 修复 ssh:exec 安全审计（问题 #41）— 4h
8. 拆分 AIPanel.tsx（问题 #1）— 8h

### 第 2 周（P1 重要修复）
1. preload 字面量常量化（问题 #17）— 8h
2. 补齐 SSH / MONITOR 域常量（问题 #18, #19, #20）— 2h
3. agent-workflow.ts console 替换（问题 #35）— 30min
4. IPC catch 块全量脱敏（问题 #37, #44, #46）— 6h
5. 实现 AIPanel 图片附件（问题 #22）— 8h
6. 实现 AIPanel 上下文压缩（问题 #23）— 12h
7. 实现 AboutSettings 检查更新（问题 #24）— 6h
8. 实现 KnowledgeDetailPage 编辑（问题 #25）— 6h
9. 修复 CSS 硬编码（问题 #58-#61）— 20min

### 第 3-4 周（P2 改进项）
1. 文件拆分（问题 #2-#9）— 40h
2. 功能完整性 WIP（问题 #26-#32）— 12h
3. 渲染层错误处理（问题 #39, #40）— 10min
4. 性能优化（问题 #49-#56）— 8h
5. 一致性审计（问题 #65-#67）— 4h

### 第 5+ 周（P3 建议项）
1. 渲染层 logger 封装（问题 #14）— 4h
2. WIP 功能全量实现（问题 #15）— 40h
3. 30 个大文件全量拆分（问题 #56）— 60h
4. 页面导出统一（问题 #68）— 1h

---

## 附录 A：审计覆盖范围

### 已审计文件
- **必读 6 文件**：CLAUDE.md / AGENTS.md / package.json / ipc-channels.ts / ipc/index.ts / electron.d.ts
- **页面 22 个**：WorkbenchPage / HistoryPage / HistoryDetailPage / BootPage / AboutSettings / MonitorPage / LogsPage / KnowledgePage / KnowledgeDetailPage / TutorialPage / TutorialDetailPage / DecisionDetailPage / SettingsLayout / GeneralSettings / SshSettings / ModelSettings / TerminalSettings / DecisionSettings / RiskSettings / AlertsSettings / AppearanceSettings / CalibrationSettings
- **组件 30+**：AIPanel / ChatPanel / CalibrationPanel / DecisionCard / ConfidenceBreakdown / CredibilityPanel / EvidenceChain / ExpectedOutput / McpStatusBar / PlanBuildButton / RiskConfirm / SandboxApprovalDialog / SidecarStatusPanel / SrePipelinePanel / TaskPermissionApprovalDialog / ToolApprovalModal / ToolCallCard / AgentWorkflowPanel / AttentionBubble / AtCommandBadge / AtCommandChip / AtCommandPicker / EditorArea / FileTree / GlobalSearch / InlineCompletionProvider / InlineDiffAdapter / LoopWorkflowPanel / MonacoEditor / QuickFileSearch / SelectionPopover / StatusBar / TraeIcons / WorkbenchTitlebar
- **IPC 34 个文件**：ssh / monitor / storage / llm / llm-inline / knowledge / history / agent / agent-runtime / profiler / tutorial / deploy / llm-tools / log / credibility / sandbox / at-commands / claude-sdk / token-stats / mode / attention / subagent / provider-info / mcp / sidecar / promptfoo / diagnostics / loop-engineering / scheduler / sftp-search / file-watcher / expectation / task-permission-approval / index
- **main 服务**：database / ssh / connection-manager / sftp / file-watcher / scheduler / logger / redact / agent-workflow / sidecar-manager

### 审计工具
- 静态扫描：Grep（TODO/FIXME/WIP/console/字面量/硬编码颜色）
- IPC 完整性：交叉比对 ipc-channels.ts / ipc/index.ts / preload/index.ts / electron.d.ts
- 安全审计：child_process / spawn / exec / dangerouslySetInnerHTML / eval / redactSensitiveInfo 检索
- 性能审计：大文件行数统计 / setInterval/setInterval cleanup 核查

---

## 附录 B：合规亮点

本次审计也确认了以下合规亮点：

1. **IPC 4 步同步完整性高**：190 个 ipcMain.handle 中仅 1 处 `system:ping` 字面量豁免，其余全部使用常量
2. **logger 使用率高**：68 处 logger.error vs 5 处 console.error（合规率 92.6%）
3. **DEV 模式 fallback 合规**：MonitorPage 使用 `import.meta.env.DEV ? sampleKpiStats : null`（B3 无 mock fallback 约束合规）
4. **WIP 标注规范**：所有未实现功能均标注 `CLAUDE.md A4 诚实标注`（A4 合规）
5. **敏感信息脱敏实现完整**：`redactSecrets` 覆盖 8 类规则（password / token / api_key / apiKey / secret / Bearer / .env 路径 / .ssh 路径）
6. **SSH 命令执行无 shell 注入风险**：使用 ssh2 库 `client.exec(command)` 不经过 shell
7. **child_process.spawn 使用数组参数**：ssh-keygen 调用使用 `spawn('ssh-keygen', ['-y', '-f', path])` 数组形式
8. **WebGL 资源释放合规**：BootPage.tsx WebGLRenderer try-catch + forceContextLoss 显式释放
9. **CSV 注入防御**：LogsPage handleExport 实现了 OWASP CSV Injection 防御（= + - @ 前置单引号）
10. **UTF-8 BOM 中文兼容**：LogsPage CSV 导出添加 UTF-8 BOM 确保中文显示
11. **无障碍合规**：button type + aria-label / aria-pressed / role="log" + aria-live="polite" / prefers-reduced-motion 禁用按压动画

---

## 附录 C：与 CLAUDE.md 约束对照

| 约束 | 状态 | 说明 |
|------|------|------|
| A1 IPC 4 步同步 | ✅ 合规 | 190 个 handler 仅 1 处豁免 |
| A2 TypeScript strict | ✅ 合规 | 0 处 `: any`（lint 0 warnings） |
| A3 catch 块脱敏 | ❌ 违规 | ssh.ts 5 处未脱敏 + IPC catch 块普遍未脱敏 |
| A4 诚实标注 | ✅ 合规 | 19 处 WIP 全部标注 |
| A5 不反编译 Claude Code | ✅ 合规 | 使用官方 SDK |
| A6 网络请求可见 | ⚠️ 部分 | LLM 请求可见，MCP 连接事件未推送 |
| A7 质量绝对优先 | ✅ 合规 | WIP 标注均说明真实实现路径 |
| A8 避免重复造轮子 | ✅ 合规 | 调研文档完整 |
| A9 Skill 调用前置 | ✅ 合规 | SKILL-CATALOG / TECH-STACK-REFERENCE-INDEX 完整 |
| B1 单文件 ≤ 500 行 | ❌ 违规 | 30 个文件超标（P0: AIPanel.tsx 1667 行） |
| B2 var(--trae-*) token | ❌ 违规 | main.tsx 28 处硬编码 + 5 处 CSS 硬编码 |
| B3 无 mock fallback | ✅ 合规 | DEV 模式 fallback 仅在 import.meta.env.DEV |
| B4 IPC 常量 | ❌ 违规 | preload 164 处字面量 + ssh:exec 字面量 |
| B5 UI 选型决策树 | ✅ 合规 | |
| B6 SQLite Pragma | ✅ 合规 | WIP 豁免 |
| B7 重组件 lazy | ✅ 合规 | WIP 豁免 |
| B8 禁止 barrel imports | ✅ 合规 | WIP 豁免 |
| B9 IPC zod 校验 | ✅ 合规 | 收窄范围后仅用户输入 IPC 必须校验 |
| B10 SQLite Pragma | ✅ 合规 | WIP 豁免 |

**合规率**：14/20（70%）

---

**审计结论**：
项目整体架构合规率高（70%），IPC 4 步同步、TypeScript strict、A4 诚实标注、A7 质量优先等核心约束全部合规。主要风险集中在 A3 catch 块脱敏（5 处违规）、B1 单文件超标（30 个文件）、B2 CSS 硬编码（33 处）、B4 IPC 常量（165 处字面量）四项，建议按 P0 → P1 → P2 → P3 优先级顺序修复。

**报告生成时间**：2026-07-22
**审计员**：Claude Sonnet 4.5（AI 自动审计）
**报告路径**：`d:\ai\linux教学一体\tdsf-linux-desktop\.learnings\v2.2-audit-report.md`
