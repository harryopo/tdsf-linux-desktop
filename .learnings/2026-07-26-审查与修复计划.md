# 2026-07-26 桌面端审查与修复执行计划

> **作者**: ai-20260720171157-6577
> **触发**: 用户「当前的桌面端进展怎么样了，你去检查一下项目进度，再看看还有哪些没有完善的地方，进行修复审查」
> **分支**: master（领先 origin/master 16 commits）
> **比赛截止**: 2026-07-30

---

## 1. 当前状态

### 1.1 门禁五绿状态

| 门禁 | 命令 | 状态 | 备注 |
|------|------|------|------|
| TypeScript Node | `pnpm typecheck:node` | ✅ exit 0 | 2026-07-26 心跳验证 |
| TypeScript Web | `pnpm typecheck:web` | ✅ exit 0 | 2026-07-26 心跳验证 |
| ESLint | `pnpm lint` | ✅ exit 0 | 0 errors / 0 warnings |
| Vitest | `pnpm test` | ✅ 1366/1366 | 62 test files passed in 9.69s |
| electron-vite build | `pnpm build` | ✅ PASS | built in 14.81s |
| electron-builder --win | `pnpm build:win` | ⏭️ SKIP | 缺 Windows SDK，待 push 后 CI 跑 |

### 1.2 未提交修改概览（16 个）

```
M .ai-coordination.json                                        +170 (AI session 记录)
M docs/handoff/AI_HANDOVER_PROMPT_V2.md                        +77/-X (交接文档)
M src/main/services/skills/builtin/diagnose-oom-killer.md      +18/-X (skill 文档)
M src/renderer/src/components/settings/model/ModelConfigSection.tsx  +X/-109
M src/renderer/src/components/settings/model/constants.ts      +0/-34
M src/renderer/src/components/terminal/SelectionPopover.css   (无 diff，仅 CRLF)
M src/renderer/src/components/terminal/SelectionPopover.tsx   +16
M src/renderer/src/components/terminal/translator.ts          +12
M src/renderer/src/components/tutorial/CourseList.tsx         +X/-X
M src/renderer/src/components/tutorial/types.ts               +4
M src/renderer/src/components/workbench/FileTree.tsx          +36
M src/renderer/src/pages/ModelSettings.tsx                    +X/-34
M src/renderer/src/pages/TutorialPage.tsx                     +X/-121
M src/renderer/src/utils/evidence-to-input.ts                 +0/-6
M tests/e2e/screenshots-acceptance/01-home-terminal.png       (被 .gitignore 屏蔽但已追踪)
```

### 1.3 最近 10 个 commits（master 顶端）

```
c6ae7fc feat(workbench): global search panel + live command extraction + dead code cleanup
0ae56ab chore(git): untrack playwright-report/index.html (.gitignore L44 规则生效)
958b1a5 chore(coordination): [ai-kimi-20260725-backend] 清理前端 AI session 所有权登记
50ed0b8 fix(frontend): [ai-20260720171157-6577] 修复 ModelSettings exportModelStats IPC 名称不一致
b5c52bb feat(scheduler): [ai-kimi-20260725-backend] 调度器任务补充单测与导出修复
be0ae44 fix(e2e): 修 2 个 demo-9-steps 真实 BUG（让主路径 E2E 全过 9/9）
718bfff feat(frontend): P0-3 新建 CalibrationSettings 组件接入校准 6 通道 [ai-20260720171157-6577]
62a4c60 docs(handoff): [ai-kimi-20260725-backend] P1后端开发计划与多AI交接建议
17bef97 chore(coordination): [ai-kimi-20260725-backend] 声明后端模块所有权
53df89a docs(agents+competition): [ai-kimi-20260725-backend] 启用有限多AI模式 + 提交比赛PPT
```

### 1.4 比赛前 P0 死 UI 补齐完成度（摘要验证）

| 项目 | commit | 状态 |
|------|--------|------|
| 全局搜索面板 | `c6ae7fc` | ✅ 已完成 |
| 布局切换 | `c6ae7fc` 死代码清理 | ⏭️ 暂不实现（无后端支撑） |
| edit-knowledge 弹窗 | `68e21e5` v2.2 R 阶段 | ✅ 已完成 |
| AIPanel 工具面板 6 个操作 | v2.2 R 阶段 | ✅ 已完成 |
| Monitor sample-data fallback | 已回滚 | ✅ 已完成 |
| Live command extraction | `c6ae7fc` | ✅ 已完成 |

---

## 2. 16 个未提交修改分类与处置

### 2.1 A 组 · 核心功能修复（必须保留 · 5 项 · 7 文件）

| 文件 | 改动摘要 | 风险评估 |
|------|---------|---------|
| `ModelConfigSection.tsx` | 删除 MODELS 卡片选择 UI，新增通用模型名输入框 | ✅ 安全（grep 验证无其他引用） |
| `ModelSettings.tsx` | 配套 props 简化（删除 onSwitchModel/modelOptions/loadingProviders） | ✅ 同上 |
| `constants.ts` | 删除 `MODELS: ModelOption[]` 和 `ModelOption` 类型 | ✅ 已被 grep 验证无引用 |
| `FileTree.tsx` | ① 修复点击两次才能展开（react-arborist isOpen 语义） ② 后台预取一级子目录（限并发 4 + 世代号） | ✅ 增量优化，回退路径完整 |
| `CourseList.tsx` | 「加载更多」分页（PAGE_SIZE=12），切换分类/搜索时重置 | ✅ UI 增强 |
| `TutorialPage.tsx` | ① 删除 v2.5 EmbeddingBanner 临时 hack ② 合并 IPC + localStorage 进度数据 ③ 真实计算精选课程进度 | ✅ 净化，符合"降级保留原则" |
| `types.ts` | 导出 `computeFeaturedProgress` | ✅ 新增导出 |
| `evidence-to-input.ts` | 指纹函数回滚 cotEntropyTrajectory 逻辑（grep 验证 fingerprint 唯一调用方） | ✅ 增量回滚，无回归 |

**注**：EmbeddingBanner 组件本身保留在 `src/renderer/src/components/tutorial/EmbeddingBanner.tsx`（CLAUDE.md L78「降级≠完全切除」原则）。

### 2.2 B 组 · 词库 v1.3.0 兼容字段（建议保留 · 3 项）

| 文件 | 改动摘要 | 风险评估 |
|------|---------|---------|
| `translator.ts` | DictEntry 增加 `syntax?` 和 `detail?` 可选字段 | ✅ 向后兼容（optional） |
| `SelectionPopover.tsx` | 增加"语法"和"详细"显示 section（仅在字段存在时触发） | ✅ 条件渲染，无词库数据时不显示 |
| `SelectionPopover.css` | CRLF 转换无逻辑变化 | ✅ 自动 CRLF 修复 |

**保留理由**：
- 字段都是可选（`?`），门禁 0 errors
- 当前词库虽未填充（v1.4.0/v1.5.0 已合入），但 selection popover 提前准备好 UI 框架
- 后续 v1.6+ 词库可直接用上，无需再改 UI

### 2.3 C 组 · AI 协作 + 文档（保留 · 3 项）

| 文件 | 改动摘要 | 风险评估 |
|------|---------|---------|
| `.ai-coordination.json` | session claim/release 记录 | ✅ 自动维护 |
| `AI_HANDOVER_PROMPT_V2.md` | 交接文档更新 | ✅ 文档 |
| `diagnose-oom-killer.md` | skill 文档微调 | ✅ 文档 |

### 2.4 D 组 · E2E 截图（需 git rm --cached · 1 项）

`tests/e2e/screenshots-acceptance/01-home-terminal.png` 已被 `.gitignore` L48 屏蔽但 git 仍追踪。
**处置**：`git rm --cached` 解除追踪，符合"文件跟踪管理：需忽略的已跟踪文件使用 git rm --cached 解除跟踪"硬约束。

---

## 3. 执行步骤

### Step 1 · 解除 E2E 截图追踪

```bash
git rm --cached tests/e2e/screenshots-acceptance/01-home-terminal.png
```

### Step 2 · Batch 1 commit（ModelSettings 简化）

```bash
git add src/renderer/src/components/settings/model/ModelConfigSection.tsx \
        src/renderer/src/components/settings/model/constants.ts \
        src/renderer/src/pages/ModelSettings.tsx
git commit -m "fix(model-settings): [ai-20260720171157-6577] 简化模型配置 - 删除 MODELS 预设卡片，改为通用模型名输入"
```

### Step 3 · Batch 2 commit（教程 + 文件树 + 指纹）

```bash
git add src/renderer/src/components/tutorial/CourseList.tsx \
        src/renderer/src/components/tutorial/types.ts \
        src/renderer/src/components/workbench/FileTree.tsx \
        src/renderer/src/pages/TutorialPage.tsx \
        src/renderer/src/utils/evidence-to-input.ts
git commit -m "feat(tutorial+workbench): [ai-20260720171157-6577] 课程分页 + 文件树预取+点击修复 + 进度合并 + 指纹回滚"
```

### Step 4 · Batch 3 commit（词库 v1.3.0 兼容）

```bash
git add src/renderer/src/components/terminal/SelectionPopover.css \
        src/renderer/src/components/terminal/SelectionPopover.tsx \
        src/renderer/src/components/terminal/translator.ts
git commit -m "feat(translate): [ai-20260720171157-6577] SelectionPopover 增加 syntax/detail 字段（v1.3.0 兼容，向后兼容可选）"
```

### Step 5 · Batch 4 commit（AI 协作 + 文档 + 截图解除追踪）

```bash
git add .ai-coordination.json \
        docs/handoff/AI_HANDOVER_PROMPT_V2.md \
        src/main/services/skills/builtin/diagnose-oom-killer.md
git rm --cached tests/e2e/screenshots-acceptance/01-home-terminal.png
git commit -m "chore(coordination+docs+gitignore): [ai-20260720171157-6577] AI session 同步 + 文档 + 解除 E2E 截图追踪"
```

### Step 6 · 推送

```bash
git push origin master
```

CI 自动跑 `pnpm build:win` 生成 NSIS 安装包。

### Step 7 · 更新 PROGRESS.md + LEARNINGS.md

在 PROGRESS.md 增加「2026-07-26 桌面端审查与修复」章节。
在 LEARNINGS.md 增加 LRN-20260726-001 ~ 003 三条经验。

---

## 4. 验证清单

执行完所有 commit + push 后必须跑：

```bash
pnpm typecheck:node   # 0 errors
pnpm typecheck:web    # 0 errors
pnpm lint             # 0 errors / 0 warnings
pnpm test             # 1366/1366 passed
pnpm build            # PASS
```

5 绿全过才能算完成。

---

## 5. 已知风险

1. **build:win 仍需 CI 验证**：本地缺 Windows SDK，push 后由 GitHub Actions windows-latest 跑
2. **词库 B 组字段未被使用**：保留是"向前兼容"设计，lint 通过但属于"潜在死代码"
3. **`.ai-coordination.json` 体积增大**：单 AI 模式下不强制，commit 后会进一步增长
4. **16 个未推送 commit 风险**：push 可能因远端保护规则失败，需 force-with-lease（避免 hard reset）

---

## 6. 不在本次范围

- Phase 8 之后的循环工程
- v3.0+ 终端 Agent（独立项目 tdsf-terminal-agent）
- 词库 v1.6+ 数据补充
- 比赛 PPT 自动化（已就绪）
- 新功能开发（仅修复 + 优化）

---

*计划生成：2026-07-26 · 作者：ai-20260720171157-6577*
