# 方案书 v0.9.2：Agent 架构设计深度调研

> **方案版本**：v0.9.2（v9.0 路线第 4 阶段，v0.9.1 4-Agent 协作模式完成后的深度调研）
> **方案日期**：2026-07-19
> **作者**：架构师 agent（基于千问参考.md 的 Harmony 工程体系 + 5 个开源项目 clone + v0.9.1 实施经验）
> **前置依赖**：v0.9.0 自检闭环 + v0.9.0 三项深度加固 + v0.9.1 4-Agent 协作 + risk-engine-ast 拆分
> **本方案性质**：架构设计深度调研方案书（非实施方案），不修改任何代码文件

---

## 0. 摘要

本方案书围绕 **"agent 架构设计"** 这一用户最关心的核心命题，回答三个问题：

1. **千问参考.md 的 Harmony 工程体系如何映射到 tdsf-linux-desktop？** —— 把"生产力 = 模型能力 × Harmony 工程能力（相乘关系）"的核心理念，落实到本体（Ontology）驱动 + Skill 系统（DAG 工作流引擎）+ 六层架构 + 控制论思维 + 数字员工"人才流水线"模式上，并与项目已有的 risk-engine-ast（AST 风险评估）+ sandbox:execute IPC 强制审批 + 4 个 Claude Subagents（architect / backend / api / reviewer）+ Mastra 框架（已选定）融合。

2. **VSCode 开源集成的可行性？** —— 用户最关心的问题。结论是 **路径 C（@monaco-editor/react，当前已落地）+ 借鉴路径 B（VS Code 扩展 API 设计思想）**：不 fork VS Code、不强依赖 code-server/Theia，保留 Electron 30 + React 18 自主架构；同时把 VS Code 1.90+ 的 Language Model API（`vscode.lm.*`）和 Cline 的"扩展即 agent tool"模式作为内部插件机制的设计参考。

3. **主流 VS Code 扩展型 Agent 的架构借鉴清单？** —— 主 agent 正在并行 clone 5 个开源项目（Cline ✅ / Kilo Code / Continue.dev / Aider + 已分析的 MetaGPT / OpenHands / Mastra / crewAI）。本方案书为每个项目预先规划了"应该重点分析哪些模块"的清单，源码分析章节明确标注"待主 agent 调度专门 agent 分析"。

**关键决策**：
- 推荐路径 **C（@monaco-editor/react）+ 借鉴路径 B（VS Code 扩展 API 设计）**，不动 VS Code 本体
- 架构演进 5 阶段（v0.9.2 → v0.9.3 → v0.9.4 → v0.9.5 → v1.0）合理且可回滚
- 千问参考.md 的"控制论思维"在 tdsf-linux-desktop 已有雏形（risk-engine-ast + assessCommandRisk 双层防御 + sandbox:execute IPC 强制审批），下一步是补强"独立验证"和"思维外显"

**硬约束对齐**：
- ✅ IDE 工作台基于现有 SftpManager 扩展（不引入 code-server/Theia）
- ✅ Agent 架构用 TS 原生框架（Mastra + Vercel AI SDK 7 + Claude Agent SDK），不引入 Python 进程通信
- ✅ 可信度算法有论文支撑（D-S + PCR5 + 43 篇论文，已在 22 号调研报告归档）
- ✅ @命令支持鼠标划选注入（v0.9 预留接口，v1.0 实施 8 类）
- ✅ 运维 Agent 每步执行有人工审批闸门（sandbox:execute IPC 已强制审批）
- ✅ 不反编译 Claude Code（用官方 `@anthropic-ai/claude-agent-sdk` 合法集成）
- ✅ 所有网络请求 UI 可见 + 敏感文件默认 redact + 本地优先 + Token 消耗透明
- ✅ 质量绝对优先 + 开源源码全量分析（5 个开源项目已 clone 到 `opensource-reference/`）

---

## 1. 背景与目标

### 1.1 用户最关心的痛点：Agent 架构设计

用户在千问参考.md 中明确表达了三个核心诉求：

1. **从被动响应到主动治理**：Agent 不能只是"问 → 答"的命令生成器，而要具备情境意识（Situational Awareness）—— 知道"我在哪台机器、用户刚才试了什么、当前日志报什么"。
2. **可解释、可预测、可信任**：AI 本质是概率模型，黑盒和幻觉不可避免；学生用户因为"不知道敲下去会发生什么"而不敢操作，这是**"认知摩擦与信任赤字"**。解决方案是**意图翻译层 + 沙箱预演 + 置信度可视化**。
3. **教学赋能**：从"工具"升级为"导师"——告诉学生"这条命令的作用是...，敲下去你应该看到 ...，如果没看到请检查 ..."，让学生从"不敢敲 → 感性认识 → 理性认知"。

### 1.2 个人开发 AI 编程辅助的困难

作为个人开发者，独立推进一个 Electron 30 + React 18 + TS + Ant Design 5 + Zustand + ssh2 + xterm.js + Mastra + Vercel AI SDK 7 + Claude Agent SDK 的大型项目，存在以下客观困难：

1. **架构复杂度高**：跨进程三层（main / preload / renderer）+ IPC 4 步同步铁律 + Agent 编排（Supervisor + Subagent）+ 沙箱（OpenHands REST API）+ 可信度算法（D-S + PCR5）—— 任何一处跳步都会埋雷。
2. **开源生态变化快**：Gemini CLI 2026-05-19 闭源化、Roo Code 2026-05-15 归档、Grok Build 2026-07-12 数据丑闻 —— 半年前调研的结论可能已失效。
3. **质量与速度的张力**：用户硬约束"质量绝对优先 + 开源源码必须全量分析"，与个人开发者的时间精力形成根本张力。需要靠 4-Agent 协作模式（architect → backend/api → reviewer）+ Claude Subagents 文件模式 + Trae IDE Task 工具来放大产能。
4. **AI 编程辅助的边界**：Claude Code / Cursor / Trae / Cline 等工具能加速编码，但**架构决策、跨模块边界、IPC 4 步同步、可信度算法**等"高阶判断"必须由人主导，AI 辅助。

### 1.3 VSCode 开源集成的契机

用户多次表达"IDE 工作台类似 Trae"的诉求。市面上 VS Code 扩展型 Agent（Cline 5M+ 安装量、Kilo Code 是 Roo Code 升级路径、Continue.dev 多模型动态调度、Aider 终端优先）已经验证了"VS Code 扩展 + LLM Agent"模式的产品可行性。tdsf-linux-desktop 的契机在于：

1. **不 fork VS Code**：fork 维护成本极高（VS Code 频繁更新），且会破坏现有 Electron 30 + React 18 + Ant Design 5 自主架构。
2. **不强依赖 code-server/Theia**：code-server 进程外启动 + 300MB+ 包体增量 + 与现有 ssh2/SftpManager 重复；Theia EPL-2.0 + InversifyJS DI 学习曲线陡 + 重构成本高（15 号调研报告已排除）。
3. **借鉴 VS Code 扩展 API 设计**：当前 `@monaco-editor/react` 已是 VS Code 同源体验，再借鉴 VS Code 1.90+ 的 `vscode.lm.*` Language Model API + Cline 的"扩展即 agent tool"模式，可在不动 VS Code 本体的前提下，构建 tdsf-linux-desktop 的内部插件机制。

---

## 2. 千问参考.md 的 Harmony 工程体系映射

### 2.1 生产力公式：模型能力 × Harmony 工程能力

千问参考.md 的核心理念：

> AI 模型能力（无论开源或闭源）正趋于同质化，真正的差异化竞争力在于"Harmony"（驾驭/约束）工程体系。生产力 = 模型能力 × Harmony 工程能力（**相乘关系，非相加**）。

**映射到 tdsf-linux-desktop**：

| 维度 | 模型能力（A） | Harmony 工程能力（B） | 生产力（A × B） |
|------|-------------|---------------------|----------------|
| 命令生成 | DeepSeek V4 / 通义千问 3 / Claude Sonnet 4.5 生成 shell 命令 | risk-engine-ast AST + 正则双层防御 + sandbox:execute IPC 强制审批 + 用户审批闸门 | 高置信度命令 + 风险预警 + 教学解释 |
| 故障诊断 | LLM 推理 + RAG 检索 Runbook | 本地上下文感知引擎（静态画像 + 动态历史 + 实时日志）+ 6 源证据融合（D-S + PCR5） | 现场aware 诊断 + 可信度评分 + 证据链 |
| 代码修改 | LLM 生成 diff / whole / search-replace | SftpManager 流式读写 + Monaco diff 预览 + 用户审批 + git 回滚 | 安全变更 + 可回滚 + 可审计 |
| 教学辅导 | LLM 解释命令原理 | 知识库（运维手册 + 历史故障 + 错题本）+ 预期回显预测 + 风险可视化 | 从"不敢敲"到"理性认知" |

**关键启示**：模型能力再强（A = 1.0），如果没有 Harmony 工程能力（B = 0），生产力仍然是 0。这就是为什么 tdsf-linux-desktop 必须坚持"质量绝对优先 + 开源源码全量分析 + IPC 4 步同步铁律 + 每步人工审批闸门"——这些是 B 的核心组成。

### 2.2 六层架构在 tdsf-linux-desktop 的落地

千问参考.md 的六层架构：知识库 / 规则 / 约束 / Skill / 模型路由 / IO 编排。

| 层级 | 千问参考.md 定义 | tdsf-linux-desktop 落地 | 实施状态 |
|------|----------------|----------------------|---------|
| L1 知识库 | 本体（Ontology）+ RAG 向量库 | `src/main/core/knowledge/`（待建）+ Runbook + 历史故障 + 错题本（Dexie IndexedDB） | ⏳ 待实施 |
| L2 规则 | 确定性业务规则 | `risk-engine-rules.ts`（高/中危命令规则）+ IPC 命名规范 + Provider 工厂 | ✅ 已实施 |
| L3 约束 | 护栏（Guardrails）| 硬约束清单（16 条）+ IPC 4 步同步铁律 + 单文件 ≤ 500 行 + 单函数圈复杂度 ≤ 15 + ESLint 0 错误 | ✅ 已实施 |
| L4 Skill | DAG 工作流引擎 + Meta-Skill 编排 | Mastra `WorkflowAgent` + 8 个 Subagent（v0.9 设计，v1.0 实施）+ MCP 双向网关 | ⏳ 部分实施（4 个 Claude Subagents 已就绪，8 个运行时 Subagent 待实施） |
| L5 模型路由 | Provider 抽象 + 多模型调度 | `src/main/core/agent/providers/`（8 个 Provider：deepseek/qwen/volcengine-ark/anthropic/claude-sdk/google/ollama/openai-compatible）+ 思考强度三档（fast/standard/deep）| ⏳ 部分实施（Provider 工厂已就绪，思考强度待 UI 接入） |
| L6 IO 编排 | IPC 编排 + 工具调用 + 流式输出 | IPC 通道（agent:* / sandbox:* / token:* / provider:* / confidence:* / at:*）+ Vercel AI SDK 7 `useChat` 流式 + xterm.js 终端数据推送 | ✅ IPC 已就绪，UI 待接入 |

### 2.3 本体（Ontology）驱动设计

千问参考.md：

> 本体作为结构化的知识库，为 Agent 提供领域知识和确定性约束。采用 JSON-LD 等对大模型友好的格式进行序列化，并通过"切片"和"投影"机制，将大型本体分解为可落地的小场景应用。

**映射到 tdsf-linux-desktop**：

1. **本体范围**：Linux 运维领域本体，包含
   - 实体（Entity）：服务器 / 用户 / 进程 / 文件 / 服务 / 端口 / 包 / 配置 / 日志
   - 关系（Relation）：用户→进程（拥有） / 进程→端口（监听） / 服务→配置（依赖） / 文件→日志（写入）
   - 属性（Property）：进程的 PID/CPU/内存 / 文件的权限/大小/mtime / 服务的状态/启动时间
   - 约束（Constraint）：根目录不可删除 / 系统包不可随意卸载 / 生产数据库不可直接 DROP

2. **JSON-LD 序列化**（PoC 草案，待用户决策是否启动）：
   ```json-ld
   {
     "@context": "https://tdsf-linux-desktop/ontology/linux-ops.jsonld",
     "@type": "Command",
     "name": "systemctl restart nginx",
     "affects": { "@id": "service:nginx", "@type": "Service" },
     "riskLevel": "medium",
     "requiresApproval": true,
     "sideEffects": ["短暂的 502 错误", "活跃连接断开"],
     "recoveryCommand": "systemctl start nginx"
   }
   ```

3. **切片与投影**：
   - **切片（Slice）**：按场景切分本体（如"网络诊断切片" / "磁盘空间切片" / "服务管理切片"），每个切片只包含相关实体和关系
   - **投影（Projection）**：根据用户当前操作上下文，动态投影相关切片到 LLM 上下文（如用户在排查 nginx 故障，投影 nginx 服务本体 + 相关进程 + 端口 + 日志路径）

4. **与 GB/T 48320-2026 对齐**：千问参考.md 提到平台已实现 95% 的中国国家标准《GB/T 48320-2026 本体标准》，tdsf-linux-desktop 作为教学项目，可参考该标准的术语和结构，但不强制对齐（待用户决策）。

### 2.4 Skill 系统（DAG 工作流引擎）

千问参考.md：

> 将业务能力标准化、模块化为可复用的 Skill。通过 Meta-Skill 进行编排，形成基于有向无环图（DAG）的工作流引擎，实现复杂任务的自动化执行。

**映射到 tdsf-linux-desktop 的 8 个 Subagent 标准化**：

| Skill / Subagent | 输入 | 输出 | DAG 节点（依赖关系） |
|-----------------|------|------|-------------------|
| 编程 Subagent | 远程文件路径 + 修改意图 | diff + 修改后的文件内容 | 依赖：知识库 Subagent（最佳实践） / 历史回溯 Subagent（历史决策） |
| 思考 Subagent | 问题 + 上下文 | 推理链 + 方案 | 依赖：搜索 Subagent（联网资料） / 知识库 Subagent（Runbook） |
| 运行 Subagent | shell 命令 | 执行结果 + 风险评估 | 依赖：编程 Subagent（生成命令） / risk-engine-ast（风险评估） |
| 搜索 Subagent | 查询关键词 | 文档 / 答案 | 独立（无依赖） |
| Skill Subagent | skill 名称 + 参数 | skill 执行结果 | 依赖：MCP 网关（skill 协议） |
| 方法论 Subagent | 故障场景 | 方法论步骤 | 依赖：知识库 Subagent（方法论库） |
| 历史回溯 Subagent | 当前问题 | 历史决策 + 成功率 | 依赖：Dexie 历史检索 |
| 知识库 Subagent | 查询 | RAG 检索结果 | 独立（无依赖） |

**Meta-Skill 编排（DAG 工作流）**：

```
用户输入"nginx 启动失败"
      ↓
[Supervisor: Plan] 拆解任务
      ↓
┌─────────────────────────────────────────────┐
│ 并行：                                       │
│  [运行 Subagent] systemctl status nginx     │
│  [知识库 Subagent] RAG 检索 nginx 故障排查   │
│  [历史回溯 Subagent] 检索历史决策            │
└─────────────────────────────────────────────┘
      ↓
[思考 Subagent] 综合分析 → 推理链
      ↓
[Supervisor: Reflect] 评估质量
      ↓
[编程 Subagent] 生成修复命令
      ↓
[risk-engine-ast] 风险评估
      ↓
[Supervisor: Act] 生成"执行计划单"
      ↓
[用户审批闸门] 用户确认 / 修改 / 拒绝
      ↓
[运行 Subagent] 执行 + 观察回显
      ↓
[Supervisor: Observe] 与预期回显对比
      ↓
[Supervisor: Reflect] 完成 / 重试 / 升级
      ↓
[知识库 Subagent] 更新知识库（Post-mortem）
```

### 2.5 控制论思维：确定性规则 + AI 不确定性互补

千问参考.md：

> 强调在系统中引入确定性规则，与 AI 的不确定性形成互补。例如，在邮件生成等场景中，通过脚本进行确定性检查，确保输出质量。

**映射到 tdsf-linux-desktop**：

| AI 不确定性 | 确定性规则互补 | 实施状态 |
|-----------|-------------|---------|
| LLM 生成 shell 命令可能含危险操作 | risk-engine-ast（tree-sitter-bash AST）+ risk-engine-rules（高/中危命令规则）+ assessCommandRisk 双层防御 | ✅ 已实施（v0.9.0 + v0.9.1 拆分） |
| LLM 可能"幻觉"不存在的命令 | 命令白名单（仅允许已知命令）+ `which` 检查 | ⏳ 待实施 |
| LLM 可能修改错误文件 | SftpManager 路径白名单 + 敏感文件 redact（.env / .ssh/ / *_key） | ✅ 已实施 |
| LLM 可能"绕过"用户审批 | sandbox:execute IPC 层强制审批（不依赖 UI 自觉）+ 30 秒超时 | ✅ 已实施 |
| LLM 可能上传敏感数据 | HC-1 网络日志可见 + 敏感文件 redact + 本地优先（默认 Ollama / 国内 Provider） | ✅ 已实施 |
| LLM 决策不可解释 | 可信度算法（D-S + PCR5 + 6 源证据融合）+ React Flow DAG 可视化 | ⏳ v1.1 实施 |
| LLM 可能"重复犯错" | 故障复盘自动化 + 错题本 + 自我修正 | ⏳ 待实施 |

**关键启示**：tdsf-linux-desktop 的 risk-engine-ast 已经体现了"控制论思维"的雏形——AST 解析是确定性规则，LLM 生成命令是不确定性，两者互补。下一步是补强"独立验证"（用不同 LLM 或不同链路验证）和"思维外显"（推理链可视化）。

### 2.6 数字员工"人才流水线"模式：Build → Hire → Onboard → Deploy

千问参考.md：

> 数字员工的构建遵循"人才流水线"模式：构建（Build）→ 雇佣（Hire）→ 实习（Onboard）→ 上岗（Deploy）。其中，"雇佣"和"实习"阶段对应企业的客制化过程，将通用能力适配到具体业务规则中。

**映射到 tdsf-linux-desktop 的 Copilot 模式三阶段**：

| 阶段 | 千问参考.md | tdsf-linux-desktop | 实施版本 |
|------|-----------|-------------------|---------|
| Build（构建） | 通用 Agent 能力 | Mastra + Vercel AI SDK 7 + 8 个 Subagent 骨架 | v0.9 ~ v1.0 |
| Hire（雇佣） | 客制化到具体业务 | 本体（Ontology）注入 + 运维知识库 RAG + 硬约束护栏 | v1.0 ~ v1.1 |
| Onboard（实习） | 在沙箱中验证 | OpenHands Sandbox + 数字孪生 + 用户审批闸门 + 复盘 | v1.1 ~ v1.2 |
| Deploy（上岗） | 生产环境运行 | Copilot 模式阶段三（全自动低风险场景） | v1.2+ |

**关键启示**：不要试图一开始就做全能 Agent。从 Copilot 模式阶段一（辅助：只查询和诊断，人不执行）起步，逐步演进到阶段二（半自动：生成修复脚本，人 Review 后执行），最后到阶段三（全自动：低风险场景无人值守）。

---

## 3. 运维 Agent 五层架构映射

千问参考.md 提出的运维 Agent 五层架构，与 tdsf-linux-desktop 的现状和规划高度契合。

### 3.1 感知层（Perception & Ingestion Layer）

**千问参考.md 定义**：Agent 的"眼睛"和"耳朵"，负责全量收集数据。

**tdsf-linux-desktop 落地**：

1. **静态画像注入**：
   - 每次对话自动附带 `uname -a` / `df -h` / `free -m` / `cat /etc/os-release` / `whoami` / `pwd`
   - 实施位置：`src/main/core/context/static-profile.ts`（待建）
   - 触发时机：用户首次连接 SSH 会话时采集，缓存到 Zustand `serverStore`
   - 注入策略：作为 system prompt 的固定段落，每次 Agent 调用自动附加

2. **动态历史回溯**：
   - 自动读取 `.bash_history` 或当前 Shell Session 的前 N 条命令（默认 N=20）
   - 实施位置：`src/main/core/context/dynamic-history.ts`（待建）
   - 数据来源：xterm.js 终端会话历史 + SSH `history` 命令
   - 注入策略：作为 user prompt 的前缀，让 LLM 知道"你刚才试过了什么"

3. **实时日志挂载**：
   - 允许用户通过快捷键（如 `Ctrl+L`）将终端最近 50 行报错日志作为 Context 发送给 AI
   - 实施位置：`src/renderer/src/components/ai/ChatPanel.tsx`（修改）+ `src/main/core/context/log-mount.ts`（待建）
   - 数据来源：xterm.js 终端 buffer + SFTP `tail -f` 实时日志流
   - 注入策略：作为 `@log` 命令的 payload（v0.9 预留接口，v1.0 实施 8 类 @命令）

4. **多模态数据接入**（扩展）：
   - Metrics（指标）：`/proc/loadavg` / `vmstat` / `iostat`（v1.2+ 监控面板）
   - Logs（日志）：syslog / journalctl / 应用日志
   - Traces（链路追踪）：待定（v1.3+）
   - Events（事件流）：变更事件（发布 / 配置修改），因为 80% 的故障源于变更

5. **实时告警收敛**：
   - 输入端初步清洗，将海量告警聚合为"故障场景"
   - 实施位置：`src/main/core/perception/alert-aggregator.ts`（待建，v1.2+）

### 3.2 大脑编排层（Orchestrator & Reasoning Layer）

**千问参考.md 定义**：Agent 的核心，对应 Orchestrator 角色。

**tdsf-linux-desktop 落地**：

1. **意图识别与路由**：
   - 判断用户是想要"查询状态" / "诊断根因" / "执行修复" / "学习教学"
   - 实施位置：`src/main/core/agent/supervisor.ts`（v0.9 设计，待实施）
   - 实现方式：LLM zero-shot 意图分类 + 规则兜底（关键词匹配）

2. **RAG 增强检索**：
   - 挂载运维知识库（Runbook / 历史故障复盘 / 架构图 / 命令手册）
   - 实施位置：`src/main/core/knowledge/rag-engine.ts`（待建）
   - 技术栈：Mastra RAG（pgvector / Qdrant / Elasticsearch）+ hybrid search（向量 + BM25 keyword）

3. **思维链规划**：
   - 对于复杂故障，Agent 生成步骤计划
   - 示例：先查数据库连接 → 再查应用日志 → 最后检查网络策略
   - 实施位置：`src/main/core/agent/supervisor.ts` 的 `plan(task)` 方法
   - 借鉴：Cline 的 plan-and-act 架构 + Grok Build 的 Plan-Search-Build 节奏 + Claude Code 的四层编排

4. **Plan-Act-Observe-Reflect 循环（PAOR）**：
   - v0.9 方案书 §3.2 已设计，本方案书不重复
   - 与 Grok Build 的 Plan-Search-Build 三阶段相比，多了 Observe 和 Reflect 阶段
   - 理由：运维场景对"观察结果"和"反思质量"要求更高，不能盲执行

### 3.3 专家技能层（Skill/Tool Layer）

**千问参考.md 定义**：Agent 的"手脚"，对应 FE/BE Agent 或 Architecture Agent 的执行能力。

**tdsf-linux-desktop 落地**：

1. **8 个特化 Subagent**（v0.9 方案书 §3.1 已设计）：
   - 编程 Subagent / 思考 Subagent / 运行 Subagent / 搜索 Subagent
   - Skill Subagent / 方法论 Subagent / 历史回溯 Subagent / 知识库 Subagent
   - 每个 Subagent 实现统一的 `execute(task) → result` 接口

2. **SRE 诊断 Agent**（运维领域特化）：
   - 擅长分析监控图表，识别异常波峰，调用 APM 工具定位慢调用
   - 主要工具：`@metric` 命令 + `monitor:getMetrics` IPC + Recharts 可视化

3. **DBA 专家 Agent**：
   - 专门处理数据库锁表 / 慢 SQL / 主从延迟等问题
   - 主要工具：`ssh:exec` 执行 `SHOW PROCESSLIST` / `EXPLAIN` / `pt-query-digest`

4. **K8s/Infra Agent**：
   - 负责容器重启 / 节点隔离 / 扩缩容等基础设施操作
   - 主要工具：`ssh:exec` 执行 `kubectl` / `docker` 命令

5. **安全合规 Agent**：
   - 在执行任何高危操作前，进行权限校验和安全审计
   - 主要工具：risk-engine-ast + assessCommandRisk + 敏感文件 redact 检查
   - 这是硬约束"运维 Agent 每步执行必须有人工审批闸门"的最后一道防线

### 3.4 执行与沙箱层（Execution & Sandbox Layer）

**千问参考.md 定义**：保障生产环境安全，必须引入沙箱机制。

**tdsf-linux-desktop 落地**：

1. **人机协同确认**：
   - 对于写操作（如重启服务 / 回滚版本 / 修改配置），Agent 必须生成"执行计划单"
   - 执行计划单包含：命令 / 影响范围 / 风险等级 / 预期回显 / 回滚方案
   - 等待人类审批（30 秒超时自动拒绝）
   - 实施位置：`src/main/ipc/sandbox.ts` 的 `sandbox:approval-request` 推送 + `src/renderer/src/components/ai/SandboxApprovalDialog.tsx`（v0.9.1 P0-1 待实施）

2. **只读/读写分离**：
   - 默认情况下 Agent 只有只读权限（查询 / 诊断 / 解释）
   - 只有经过授权才能获取写权限（修改 / 执行 / 重启）
   - 实施方式：Provider 工厂的 `capabilities` 字段 + IPC 通道的 `requiresApproval` 标志

3. **模拟演练**：
   - 在执行高风险变更前，先在沙箱中模拟执行结果
   - 实施方式：
     - v0.9：进程级沙箱 + 权限白名单（借鉴 Trae/Claude Code 的 auto-allow）
     - v1.3：Docker 容器沙箱（借鉴 OpenHands 的 REST API 沙箱，已在 25 号调研报告分析）
   - 命令预演：对危险命令（如 `rm` / `mkfs`）自动附加 `-n` 或 `--dry-run` 参数

4. **沙箱架构演进**：
   - v0.9 ~ v1.0：进程级 + AST 风险评估 + IPC 强制审批
   - v1.1 ~ v1.2：OpenHands Docker 沙箱（已 clone 到 `opensource-reference/OpenHands/`）+ session_api_key 句柄模式
   - v1.3+：数字孪生（在测试环境模拟生产环境配置）

### 3.5 记忆与进化层（Memory & Evolution Layer）

**千问参考.md 定义**：对应 Feedback Bus 和 Evaluator。

**tdsf-linux-desktop 落地**：

1. **故障复盘自动化**：
   - 每次故障处理结束后，自动生成 Post-mortem 报告
   - Post-mortem 包含：故障现象 / 根因 / 解决方案 / 时间线 / 经验教训
   - 自动更新知识库（向量化 + RAG 检索）
   - 实施位置：`src/main/core/memory/postmortem.ts`（待建，v1.2）

2. **自我修正**：
   - 如果某次诊断失败，记录错误路径
   - 下次遇到类似场景时避开该路径
   - 实施方式：错题本（Dexie IndexedDB）+ 历史回溯 Subagent 检索

3. **强化学习驱动进化**（长期）：
   - 通过 Feedback Bus 收集执行过程与结果的反馈
   - 对 Agent 进行过程激励和结果激励
   - 不断优化其行为策略，避免重复犯错
   - 实施位置：`src/main/core/memory/feedback-bus.ts`（待建，v1.3+）

4. **记忆存储**：
   - 短期记忆：Mastra Memory（thread-scoped + working memory）
   - 长期记忆：Dexie（IndexedDB）+ 向量库（pgvector / Qdrant）
   - 跨会话记忆：Mastra Memory Threads + semantic recall

---

## 4. 用户痛点深度剖析与解决方案

千问参考.md 在第 159 行明确表达了用户痛点：

> 我之前遇到的问题就是我把 shell 终端截图给 ai 看，ai 给出代码，我再一个个敲，这其中就有一种置信度的问题，因为有时候 ai 给的代码运行了还是出错解决不了问题，因为 ai 无法感知系统环境和系统日志信息，也无法知道除了截图外的历史命令...学生用 ai，最大的问题就是他没学过这个命令而不敢敲...ai 本质是概率，带来的黑盒和幻觉是不可避免的，所以人机的信任会低。

### 4.1 痛点：缺乏情境意识（Lack of Situational Awareness）

**官方术语描述**：AI 无法感知当前的系统状态（OS 版本、已装软件、网络环境），导致生成的建议是"真空中的理论解"，而非"现场的实操解"。

**用户原话映射**：用户提到"AI 无法感知系统环境和系统日志信息，也无法知道除了截图外的历史命令"。

**解决方案：本地上下文感知引擎**：

1. **静态画像注入**（§3.1.1 已详述）：
   - 每次对话自动附带 `uname -a` / `df -h` / `free -m` 等基础信息
   - 让 AI 知道"你在哪台机器、什么 OS、多少磁盘和内存"

2. **动态历史回溯**（§3.1.2 已详述）：
   - 自动读取 `.bash_history` 或当前 Shell Session 的前 N 条命令
   - 让 AI 知道"你刚才试过了什么"，避免重复建议

3. **实时日志挂载**（§3.1.3 已详述）：
   - 允许用户通过 `Ctrl+L` 将终端最近 50 行报错日志作为 Context 发送给 AI
   - 让 AI 知道"刚才报了什么错"

4. **@命令 8 类**（v0.9 方案书 §4 已设计）：
   - `@log` 注入日志片段 / `@cmd` 注入命令 + 预测结果 / `@file` 注入远程文件内容
   - `@metric` 注入监控指标 / `@decision` 注入历史决策 / `@kb` 注入知识库条目
   - `@skill` 调用 skill / `@server` 注入服务器信息
   - **必须支持鼠标划选注入**（硬约束），不能只靠手动输入

### 4.2 痛点：认知摩擦与信任赤字（Cognitive Friction & Trust Deficit）

**官方术语描述**：由于 AI 输出缺乏因果解释（Why）和结果预测（What if），用户处于高认知负荷状态，产生对自动化系统的防御心理。

**用户原话映射**：用户提到"学生最大的问题就是他没学过这个命令而不敢敲"。

**解决方案：思维外显 + 置信度可视化**：

1. **意图翻译层**：
   - AI 输出必须包含一段自然语言解释
   - 示例："这条命令的作用是重启 nginx 服务，它会先停止当前所有 nginx 进程（包括你正在处理的连接），然后重新加载配置文件。执行期间会有 1-3 秒的 502 错误。"
   - 实施位置：`src/main/core/agent/intent-translator.ts`（待建）

2. **沙箱预演机制（Dry-Run Simulation）**：
   - 对于危险命令（如 `rm` / `mkfs`），Agent 自动附加 `-n` 或 `--dry-run` 参数进行模拟运行
   - **关键创新点**：利用 LLM 预测回显
   - 示例："执行后，你应该会看到 `Active: active (running) since ...`，如果没有看到，请检查 `journalctl -u nginx` 日志"
   - 实施位置：`src/main/core/agent/predictor.ts`（待建）+ `src/main/core/sandbox/dry-run.ts`（待建）

3. **置信度可视化**：
   - 每条建议旁标注置信度标签（如 `[High Confidence]` / `[Warning: Destructive]`）
   - 6 源证据融合（D-S + PCR5）+ React Flow DAG 可视化
   - 实施位置：`src/main/core/credibility/`（v0.9.0 已建目录骨架）+ `src/renderer/src/components/ai/CredibilityPanel.tsx`（待建）
   - 论文支撑：22 号调研报告（D-S 1967 + PCR5 2004 + Verbalized Confidence 2024-2025 + Self-Consistency 2022 + 43 篇论文）

4. **风险可视化**：
   - 用颜色编码（绿/黄/红）标记命令风险（low / medium / high）
   - 提示潜在影响（"会影响 5 个活跃连接" / "会删除 100 个文件"）
   - 实施位置：`src/renderer/src/components/ai/RiskBadge.tsx`（待建）

### 4.3 痛点：缺乏反馈回路验证（Absence of Feedback Loop Verification）

**官方术语描述**：传统 AI 交互是单向的（Prompt → Answer），缺少"预演 → 确认 → 执行 → 修正"的闭环，导致错误成本极高。

**用户原话映射**：用户提到"有时候 ai 给的代码运行了还是出错解决不了问题"。

**解决方案：预演-确认-执行-修正闭环**：

```
1. 预演（Dry-Run）
   ↓
2. 确认（User Approval）
   ↓
3. 执行（Execute）
   ↓
4. 修正（Incremental Fix）
```

**实施细节**：

1. **预演**：
   - AI 生成 1-3 个方案，每个方案包含：
     - 命令代码块
     - **原理简述**（教学属性）
     - **预期后果**（风险预警）
     - **预期回显**（"你应该看到 ..."）
   - 高风险命令自动附加 `--dry-run` 模拟运行

2. **确认**：
   - 用户选择方案 A/B/C，或者要求 AI"再解释详细点"
   - 人工审批闸门（30 秒超时自动拒绝）

3. **执行**：
   - 用户执行后，Agent 观察回显
   - 与预期回显对比，判断是否成功

4. **修正**：
   - 如果出错，直接将新报错贴回
   - AI 基于之前的上下文进行**增量修正**（不重新生成完整方案，而是在原方案基础上调整）

5. **千问参考.md 的"独立验证"原则**：
   - 验证必须与推理使用不同链路或不同 Agent
   - 例如：用 DeepSeek V4 生成命令，用 Claude Sonnet 4.5 进行审核
   - 前端验证应直接通过实际执行（在沙箱中）测试，而非仅分析代码
   - 实施位置：`src/main/core/agent/independent-verifier.ts`（待建，v1.1+）

### 4.4 痛点：不可解释的黑盒决策

**官方术语描述**：用户只看到了"怎么做"（How），却不知道"为什么这么做"（Why），阻碍了从模仿到理解的转化。

**用户原话映射**：用户提到"如果说 ai 可以通过知识库，运维经验，上下文环境感知，告诉他这样敲的原因，以及预测敲后的结果，我觉得是非常好的"。

**解决方案：意图翻译层 + 推理链可视化**：

1. **意图翻译层**（§4.2.1 已详述）：
   - AI 输出包含"为什么这么做"的自然语言解释
   - 引用知识库依据（"根据 Runbook 第 3 步 ..."）
   - 引用上下文证据（"你的系统是 Ubuntu 22.04，nginx 版本 1.18，所以 ..."）

2. **推理链可视化**：
   - 用 React Flow 展示从问题到解决方案的推理路径
   - 节点：问题 → 假设 → 验证 → 结论 → 方案
   - 边：证据支持 / 证据反驳 / 逻辑推导
   - 实施位置：`src/renderer/src/components/ai/ReasoningChain.tsx`（待建）

3. **可信度透明化**（硬约束）：
   - 每个决策展示 6 源证据权重 + 公式
   - 6 源：日志证据 / 知识库匹配 / AI 模型置信度 / 人工决策 / 历史对话 / 最佳实践
   - 冲突度 K > 0.3 时高亮警告"证据冲突，建议人工复核"
   - 实施位置：`src/main/core/credibility/` + `src/renderer/src/components/ai/CredibilityPanel.tsx`

4. **教学引导**：
   - 提供命令原理解释、风险提示、操作建议（如"建议先备份"）
   - 错题本功能：将错误转化为学习机会
   - 实施位置：`src/main/core/teaching/error-book.ts`（待建，v1.2+）

---

## 5. 主流开源 Agent 架构对比分析

### 5.1 商业 IDE Agent 对比（Cursor / Trae / Codex / Claude Code）

| 项目 | 类型 | 架构特点 | 可借鉴点 | 不可借鉴点 |
|------|------|---------|---------|-----------|
| **Cursor** | 闭源 VS Code fork | proprietary Agent + Codebase Indexing + Tab 模型 | 上下文管理 / Tab 自动补全 | 闭源，不能直接用代码 |
| **Trae** | 闭源 VS Code fork | `.trae/agents` 目录 + hooks 机制 + Task 工具 + subagent | **.trae/agents 目录最接近 tdsf-linux-desktop 的 .claude/agents/ 设计** | 闭源 |
| **Codex CLI** | OpenAI 开源 CLI | CLI 优先 + sandbox exec | CLI 交互模式 | 不适合桌面端 |
| **Claude Code** | 闭源 + 官方 SDK | Bun + TS + React/Ink + Agent Loop + 四层多 Agent 编排 + 200K-1M 上下文 + 5 层 compaction | Agent Loop / 上下文 compaction / 四层编排 / 工具系统 | 闭源核心；用 `@anthropic-ai/claude-agent-sdk` 合法集成（不反编译，硬约束） |

**关键启示**：
- **Trae 的 `.trae/agents` 目录 + hooks 机制**最接近 tdsf-linux-desktop 的设计（项目已有 `.claude/agents/` 4 个 subagent 定义）
- **Claude Code 的四层多 Agent 编排**（Task / Background / Agent Teams / Coordinator）是 Supervisor-Subagent 模式的最佳实践
- **Cursor 的上下文管理**值得借鉴，但闭源无法直接参考代码

### 5.2 开源 VSCode 扩展型 Agent 对比

| 项目 | License | Star | 架构特点 | 集成难度 | 推荐度 |
|------|---------|------|---------|---------|--------|
| **Cline** | Apache-2.0 | 5M+ 安装 | plan-and-act 架构 + tool_use API + MCP 集成 + Checkpointing | 🟡 中（VS Code 扩展机制） | ⭐⭐⭐⭐⭐ |
| **Kilo Code** | Apache-2.0 | ~10k | Roo Code 升级路径 + Code/Plan/Debug/Ask + subagents + Agent Manager 跨 git worktrees | 🟡 中 | ⭐⭐⭐⭐ |
| **Continue.dev** | Apache-2.0 | ~25k | 多模型动态调度（router / fallback）+ Continue Hub 自托管 + VS Code + JetBrains 双端 + RAG codebase indexing | 🟡 中 | ⭐⭐⭐⭐ |
| **Aider** | Apache-2.0 | ~30k | 终端优先 + git 沙箱回滚 + RepoMap + 多种 edit format（whole/diff/udiff/search-replace）+ 本地模型 + BYOK | 🟢 低（CLI） | ⭐⭐⭐ |

**关键启示**：
- **Cline 是最值得深度分析的项目**：5M+ 安装量验证了产品可行性，plan-and-act 架构 + tool_use API + MCP 集成 + Checkpointing 都是 tdsf-linux-desktop 需要的核心能力
- **Kilo Code 是 Roo Code 的维护升级路径**：Roo Code 2026-05-15 归档后，Kilo Code 接棒；Code/Plan/Debug/Ask 多模式切换 + subagents 设计值得借鉴
- **Continue.dev 的多模型动态调度**：router / fallback 机制对 tdsf-linux-desktop 的 Provider 工厂有参考价值
- **Aider 的 git 沙箱回滚 + RepoMap**：对 tdsf-linux-desktop 的"故障复盘 + 自我修正"有参考价值

### 5.3 开源 Agent 框架对比（已在 v0.9.1 调研）

**已选定**（v0.9 方案书 §9 决策 1）：
- ✅ **Mastra v1.34 + Vercel AI SDK 7**：Apache 2.0 / TS 原生 / DirectChatTransport 杀手级能力 / supervisor agents + ACP / 双向 MCP / 18+ 国内 provider / 25,900 stars
  - 源码已 clone 到 `opensource-reference/mastra/`
  - 源码分析报告：`idea-to-dev-output/24-源码分析-Mastra框架.md`

**已分析但不引入**：
- ❌ **MetaGPT**：MIT / Python / SOP 范式 —— 借鉴 SOP 思想到 `.claude/agents/`，但不引入 Python 框架（硬约束）
- ❌ **crewAI**：MIT / Python / Role-based 协作 —— 借鉴 Role-based 设计，但不引入
- ❌ **LangGraph**：MIT / Python / Stateful multi-agent —— 不引入
- ❌ **AutoGen**：MIT / Python / **已进入 maintenance mode**（2025-late）—— 不再推荐新项目
- ✅ **OpenHands**：MIT / Python + TypeScript / SandboxService + REST API —— 沙箱已实施（v0.9.0 warmupSessionKeyCache + sessionKeyMap 句柄模式）
  - 源码已 clone 到 `opensource-reference/OpenHands/`
  - 源码分析报告：`idea-to-dev-output/25-源码分析-OpenHands沙箱.md`

### 5.4 红线项目（已排除）

| 项目 | 排除原因 |
|------|---------|
| **Gemini CLI** | 2026-05-19 闭源化（21 号调研报告红线） |
| **Roo Code** | 2026-05-15 归档（21 号调研报告红线） |
| **AGNT** | custom license（非标准开源协议） |
| **Hermes** | Python + Rust 混合（不引入 Python） |
| **Coder** | AGPL-3.0（License 红线） |
| **judge0** | GPL-3.0（License 红线） |

---

## 6. 待补充：5 个开源项目源码分析清单（主 agent 正在 clone）

> **重要说明**：本章节为"待主 agent 调度专门 agent 分析"的预留清单。源码分析必须遵循硬约束"开源源码必须全量分析"，不能仅凭 README/文档判断。每个项目的分析结果将作为 v0.9.4 阶段的输入。

### 6.1 Cline 源码分析（待主 agent 调度专门 agent 分析）

**项目状态**：✅ 已 clone 到 `opensource-reference/cline/`

**项目快照**（基于 LS 结果）：
- License：Apache-2.0（已确认 `apps/vscode/LICENSE`）
- 技术栈：TypeScript + Biome + Bun + gRPC + protobuf
- 架构特点：
  - `apps/vscode/` 是 VS Code 扩展主体
  - `apps/cli/` 是 CLI 版本（bin/cline）
  - `sdk/` 是独立 SDK（含 SdkController / SdkSessionLifecycle / SdkCompactionCoordinator 等协调器）
  - `proto/cline/` 含 17 个 .proto 文件（account / browser / checkpoints / commands / file / hooks / mcp / models / slash / state / task / ui / web / worktree 等）
  - `src/core/controller/` 含 account / browser / commands / file / grpc-recorder / mcp / models / slash / state / task / ui / web / worktree 等 controller
  - `src/core/hooks/` 含 HookProcess / HookProcessRegistry / hook-factory 等
  - `src/core/mentions/` 含 @命令实现（index.ts + index.test.ts）
  - `src/core/storage/` 含 StateManager / disk / state-migrations / skill-directories 等
  - `src/core/task/tools/subagent/` 含 AgentConfigLoader / SubagentToolName
  - `src/hosts/vscode/hostbridge/` 含 diff / env / window 三个 host bridge
  - `src/hosts/vscode/terminal/` 含 OSC 633 Parser（终端集成协议）
  - `src/integrations/editor/` 含 DiffViewProvider / EditPreview
  - `src/integrations/terminal/` 含 constants / types
  - `src/sdk/` 含 30+ 个 SdkXxxCoordinator 协调器（compaction / diff-edit / followup / interaction / mcp / message / mode / provider-change / session-event / session-history / session-lifecycle / task-control / task-start / tool-policies 等）

**重点分析模块**（待主 agent 调度专门 agent 分析）：

- [ ] **plan-and-act 架构核心实现**
  - 位置：`src/core/task/` + `src/sdk/sdk-mode-coordinator.ts` + `src/sdk/sdk-task-start-coordinator.ts`
  - 重点：Plan 模式和 Act 模式如何切换、Plan 阶段如何拆解任务、Act 阶段如何执行
  - 借鉴目标：tdsf-linux-desktop 的 Supervisor Agent Plan-Act-Observe-Reflect 循环（PAOR）

- [ ] **工具系统（tool_use API）**
  - 位置：`src/core/task/tools/` + `src/sdk/sdk-tool-policies.ts`
  - 重点：工具如何定义、如何注册、如何调用、tool_use API 的协议
  - 借鉴目标：tdsf-linux-desktop 的 8 个 Subagent 工具系统

- [ ] **MCP 集成方式**
  - 位置：`src/core/controller/mcp/` + `src/sdk/sdk-mcp-coordinator.ts` + `proto/cline/mcp.proto`
  - 重点：MCP Server 如何注册、如何发现工具、如何调用、tool auto-approve 机制
  - 借鉴目标：tdsf-linux-desktop 的 MCP 双向网关（v0.9 方案书 §3.1）

- [ ] **VS Code 扩展机制（如何注入 editor / terminal / file system）**
  - 位置：`src/hosts/vscode/` + `src/integrations/editor/` + `src/integrations/terminal/`
  - 重点：如何创建 DiffView、如何与 VS Code Terminal 集成（OSC 633 Parser）、如何操作文件系统
  - 借鉴目标：tdsf-linux-desktop 的 IDE 工作台（@monaco-editor/react + xterm.js）

- [ ] **Browser tool / Terminal tool / File system tool 实现**
  - 位置：`src/core/controller/browser/` + `src/integrations/terminal/` + `src/core/controller/file/`
  - 重点：Browser tool 如何驱动 Playwright、Terminal tool 如何执行命令并捕获回显、File system tool 如何读写文件
  - 借鉴目标：tdsf-linux-desktop 的运行 Subagent + 编程 Subagent

- [ ] **Context management（@mention / file context）**
  - 位置：`src/core/mentions/` + `src/core/controller/file/openMention.ts`
  - 重点：@mention 如何解析、如何注入文件上下文、如何处理大文件
  - 借鉴目标：tdsf-linux-desktop 的 8 类 @命令（v0.9 方案书 §4）

- [ ] **Checkpointing / undo 机制**
  - 位置：`src/sdk/sdk-checkpoints.ts` + `proto/cline/checkpoints.proto`
  - 重点：如何创建 checkpoint、如何 undo、如何与 git 集成
  - 借鉴目标：tdsf-linux-desktop 的故障复盘 + 自我修正（v1.2+）

- [ ] **gRPC + protobuf 架构**
  - 位置：`proto/cline/` + `src/core/controller/grpc-handler.ts` + `src/hosts/vscode/hostbridge/hostbridge-grpc-handler.ts`
  - 重点：为什么用 gRPC 而不是直接 IPC、protobuf 如何定义接口、hostbridge 如何工作
  - 借鉴目标：tdsf-linux-desktop 是否需要在 main / preload / renderer 三层间引入 gRPC（待评估，目前用 Electron IPC）

- [ ] **Hooks 机制**
  - 位置：`src/core/hooks/` + `src/sdk/hooks-adapter.ts`
  - 重点：Hooks 如何定义、如何触发、taskstart/taskcomplete/taskcancel/taskresume 等 hook 的实现
  - 借鉴目标：tdsf-linux-desktop 是否需要 Hooks 机制（类似 Trae 的 .trae/hooks/）

- [ ] **Subagent 系统**
  - 位置：`src/core/task/tools/subagent/` + `src/core/controller/file/createSkillFile.ts` + `src/core/storage/skill-directories.ts`
  - 重点：Subagent 如何定义（AgentConfigLoader）、如何调用（SubagentToolName）、与 Skill 文件的关系
  - 借鉴目标：tdsf-linux-desktop 的 8 个运行时 Subagent + 4 个 Claude Subagents（.claude/agents/）

- [ ] **Worktree 管理**
  - 位置：`src/core/controller/worktree/` + `proto/cline/worktree.proto`
  - 重点：如何创建 / 切换 / 合并 / 删除 git worktree、并行 subagent 如何隔离
  - 借鉴目标：tdsf-linux-desktop 的并行 Subagent 隔离（v1.0+）

- [ ] **Compaction 机制（5 层上下文压缩）**
  - 位置：`src/sdk/sdk-compaction.ts` + `src/sdk/sdk-compaction-coordinator.ts`
  - 重点：何时触发 compaction、如何压缩、如何保留重要节点
  - 借鉴目标：tdsf-linux-desktop 的上下文管理（v0.9 方案书 §3.3 的 5 层 compaction）

### 6.2 Kilo Code 源码分析（待）

**项目状态**：⏳ 待主 agent clone 到 `opensource-reference/kilo-code/`

**项目背景**（基于 21 号调研报告）：
- License：Apache-2.0
- 是 Roo Code 的维护升级路径（Roo Code 2026-05-15 归档后，Kilo Code 接棒）
- 架构特点：Code/Plan/Debug/Ask 多模式 + subagents + Agent Manager 跨 git worktrees 并行

**重点分析模块**（待主 agent 调度专门 agent 分析）：

- [ ] **Code/Plan/Debug/Ask 多模式切换**
  - 重点：4 种模式如何切换、每种模式的 prompt 模板、模式间的状态传递
  - 借鉴目标：tdsf-linux-desktop 的思考强度三档（fast / standard / deep）+ 多模式 Subagent 调度

- [ ] **Subagent 系统（如何定义 / 调度）**
  - 重点：Subagent 如何定义（YAML / Markdown / JSON）、如何调度、与主 Agent 的通信协议
  - 借鉴目标：tdsf-linux-desktop 的 8 个运行时 Subagent 设计

- [ ] **Agent Manager 跨 git worktrees 并行实现**
  - 重点：如何并行启动多个 subagent、每个 subagent 独立 worktree、如何同步状态
  - 借鉴目标：tdsf-linux-desktop 的并行 Subagent 策略（独立子任务并行，依赖子任务串行）

- [ ] **MCP 集成**
  - 重点：MCP Server 注册、工具发现、调用协议
  - 借鉴目标：tdsf-linux-desktop 的 MCP 双向网关

- [ ] **与 Roo Code 的差异**
  - 重点：Kilo Code 相比 Roo Code 的改进点、新增特性、向后兼容性
  - 借鉴目标：评估是否值得从 Roo Code 思路迁移到 Kilo Code 思路

### 6.3 Continue.dev 源码分析（待）

**项目状态**：⏳ 待主 agent clone 到 `opensource-reference/continue-dev/`

**项目背景**（基于 21 号调研报告）：
- License：Apache-2.0
- ~25k stars
- 架构特点：多模型动态调度（router / fallback）+ Continue Hub 自托管 + VS Code + JetBrains 双端 + RAG codebase indexing + Tab autocomplete / chat / edit / action 多模式

**重点分析模块**（待主 agent 调度专门 agent 分析）：

- [ ] **多模型动态调度（router / fallback）**
  - 重点：router 如何根据任务类型选择模型、fallback 如何在主模型失败时切换、模型路由表的设计
  - 借鉴目标：tdsf-linux-desktop 的 Provider 工厂 + 思考强度三档 + 模型路由（编程用 DeepSeek-Coder，思考用通义千问，兜底用 Claude）

- [ ] **Continue Hub 自托管机制**
  - 重点：Continue Hub 如何部署、如何管理用户、如何计量计费、如何与 IDE 集成
  - 借鉴目标：tdsf-linux-desktop 是否需要类似的自托管后端（教学场景可能不需要，但值得关注）

- [ ] **VS Code + JetBrains 双端适配**
  - 重点：如何抽象 IDE API、如何同时支持两个 IDE、双端代码复用率
  - 借鉴目标：tdsf-linux-desktop 是否需要支持 JetBrains（当前只支持 Electron + VS Code 同源体验）

- [ ] **RAG 索引（codebase indexing）**
  - 重点：如何对整个 codebase 建立向量索引、如何增量更新、如何检索
  - 借鉴目标：tdsf-linux-desktop 的知识库 RAG（运维 Runbook + 历史故障 + 命令手册）

- [ ] **Tab autocomplete / chat / edit / action 多模式**
  - 重点：4 种模式如何切换、Tab autocomplete 的低延迟实现、chat 的流式输出、edit 的 diff 应用、action 的工具调用
  - 借鉴目标：tdsf-linux-desktop 的 ChatPanel + IDE 工作台集成

### 6.4 Aider 源码分析（待）

**项目状态**：⏳ 待主 agent clone 到 `opensource-reference/aider/`

**项目背景**（基于 21 号调研报告）：
- License：Apache-2.0
- ~30k stars
- 架构特点：终端优先 + git 沙箱回滚 + RepoMap（仓库结构理解）+ 多种 edit format（whole / diff / udiff / search-replace）+ 本地模型 + BYOK

**重点分析模块**（待主 agent 调度专门 agent 分析）：

- [ ] **git 沙箱回滚机制**
  - 重点：如何在每次修改前创建 git commit、如何回滚、如何与 git stash 集成
  - 借鉴目标：tdsf-linux-desktop 的故障复盘 + 自我修正（v1.2+）+ 配置文件修改的回滚机制

- [ ] **RepoMap（仓库结构理解）**
  - 重点：如何用 tree-sitter 解析代码结构、如何生成 RepoMap、如何压缩到 LLM 上下文
  - 借鉴目标：tdsf-linux-desktop 的本体（Ontology）驱动设计（将 Linux 运维领域知识结构化）

- [ ] **多种 edit format（whole / diff / udiff / search-replace）**
  - 重点：4 种 edit format 的适用场景、如何选择、如何应用、如何处理冲突
  - 借鉴目标：tdsf-linux-desktop 的编程 Subagent（远程文件修改）

- [ ] **本地模型 + BYOK 双模式**
  - 重点：如何同时支持本地 Ollama 和 BYOK（Bring Your Own Key）、如何切换、如何降级
  - 借鉴目标：tdsf-linux-desktop 的本地优先（默认 Ollama / 国内 Provider，海外 API 需显式开启）+ Provider 工厂

### 6.5 已分析项目复用（MetaGPT / OpenHands / Mastra / crewAI）

> 这 4 个项目已在 v0.9 / v0.9.1 阶段完成源码分析，本方案书只引用结论，不重复分析。

#### 6.5.1 MetaGPT（已分析）

- **源码位置**：`opensource-reference/MetaGPT/`
- **License**：MIT
- **借鉴点**：SOP 范式（Product Manager → Architect → Engineer → QA Engineer 装配线）
- **已应用到 tdsf-linux-desktop**：
  - `.claude/agents/` 4 个 subagent（architect / backend / api / reviewer）借鉴 MetaGPT SOP
  - v0.9.1 方案书 §1.3 明确："借鉴 SOP 范式但不引入 Python 框架"
- **不引入原因**：Python 框架，违反硬约束"Agent 架构必须用 TS 原生框架"

#### 6.5.2 OpenHands（已分析 + 已实施）

- **源码位置**：`opensource-reference/OpenHands/`
- **License**：MIT
- **借鉴点**：SandboxService + REST API 沙箱
- **已应用到 tdsf-linux-desktop**：
  - v0.9.0 三项深度加固：warmupSessionKeyCache + sessionKeyMap 句柄模式 + sandbox:create/list/delete/execute IPC
  - v0.9.1 4-Agent 协作：sandbox:approval-request 强制审批 + assessCommandRisk 危险度识别
- **不引入 Python 部分**：只复用 OpenHands 的 SandboxService REST API，不在主进程引入 Python

#### 6.5.3 Mastra（已分析 + 已选定）

- **源码位置**：`opensource-reference/mastra/`
- **License**：Apache-2.0
- **借鉴点**：DirectChatTransport + SubAgent + Tool approval + Memory + RAG + Eval
- **已应用到 tdsf-linux-desktop**：
  - v0.9 方案书 §9 决策 1：选定 Mastra v1.34 + Vercel AI SDK 7 作为 Agent 框架
  - 源码分析报告：`idea-to-dev-output/24-源码分析-Mastra框架.md`
- **下一步**：v0.9.3 渲染层 UI 接入后，v0.9.4 + v0.9.5 全面落地 Mastra Agent Runtime

#### 6.5.4 crewAI（已分析）

- **源码位置**：`opensource-reference/crewAI/`
- **License**：MIT
- **借鉴点**：Role-based 协作（role / goal / backstory 模式）
- **已应用到 tdsf-linux-desktop**：
  - v0.9.1 方案书 §1.3 明确："借鉴 Role-based 协作但不引入 Python 框架"
- **不引入原因**：Python 框架，违反硬约束

---

## 7. VSCode 开源集成可行性分析（用户明确要求）

> 本章节是用户最关心的核心命题之一。基于 15 号调研报告（WebIDE 与 SSH 文件管理集成方案）+ Cline 源码结构 + 项目硬约束，给出三路径对比和推荐方案。

### 7.1 三种集成路径对比

#### 路径 A：fork VS Code（microsoft/vscode，MIT）→ 改造为 tdsf-linux-desktop

**做法**：fork microsoft/vscode 仓库，移除 VS Code 的 Azure / Copilot 等云端功能，集成 tdsf-linux-desktop 的 ssh2 / SftpManager / xterm.js / Mastra Agent Runtime。

**优点**：
- 集成难度极高但获得完整 VS Code 体验
- 兼容所有 VS Code 扩展（5M+ 扩展生态）
- Monaco 编辑器 + 终端 + 文件树 + 命令面板 + 设置页全部原生
- Language Server Protocol 原生支持

**缺点**：
- ❌ 维护成本极高：VS Code 每月发布新版本，每次 merge 上游改动都是噩梦
- ❌ 包体巨大：VS Code 完整包体 300MB+，加上 Electron 30 自身 200MB，总包体 500MB+
- ❌ 架构冲突：VS Code 自带 Extension Host 进程 + Main Process + Renderer Process 三层，与 tdsf-linux-desktop 的 Electron 三层架构冲突
- ❌ 重构现有 renderer：tdsf-linux-desktop 已有 React 18 + Ant Design 5 + Zustand 架构，fork VS Code 后需要全部推倒重来
- ❌ 与现有 SftpManager 重复：VS Code 自带 Remote SSH 扩展，会与 tdsf-linux-desktop 的 SftpManager 形成两套 SSH 栈

**结论**：❌ 不推荐

#### 路径 B：开发 VS Code 扩展（vscode-extension-api）→ 不动 VS Code 本体

**做法**：开发一个 VS Code 扩展，安装到用户的 VS Code 中，提供 tdsf-linux-desktop 的 AI Agent / SSH / 沙箱 / 可信度等功能。

**优点**：
- 集成难度中等
- 兼容所有 VS Code 扩展
- 不需要维护 VS Code 本体
- 利用 VS Code 的 Language Model API（`vscode.lm.*`，1.90+ 原生 AI API）

**缺点**：
- ⚠️ 灵活性受限：VS Code 扩展 API 有沙箱限制，不能直接访问 Node.js API
- ⚠️ 用户必须先安装 VS Code：tdsf-linux-desktop 作为独立桌面应用的产品定位被削弱
- ⚠️ 与 tdsf-linux-desktop 的 Electron 30 + React 18 + Ant Design 5 架构不一致
- ⚠️ 教学场景：学生用户需要先装 VS Code 再装扩展，门槛高
- ⚠️ 仍有部分功能受限：VS Code 扩展不能修改 VS Code UI 的核心部分（如标题栏 / 状态栏样式）

**结论**：⚠️ 可选（但不作为主路径）

#### 路径 C：用 @monaco-editor/react（当前方案）→ 在 Electron 内自建 IDE 体验

**做法**：在 Electron 30 + React 18 + Ant Design 5 框架内，用 `@monaco-editor/react` 提供代码编辑器，用 xterm.js 提供终端，用 SftpManager 提供远程文件管理，自建 IDE 体验。

**优点**：
- 集成难度低（已实施 v0.8 IDE MVP）
- 灵活性高：完全自主架构，可定制任何 UI
- 维护成本低：不需要 merge VS Code 上游
- 性能可控：Electron 30 单进程，无 VS Code Extension Host 额外进程
- 与现有架构一致：React 18 + Ant Design 5 + Zustand + ssh2 + xterm.js
- 教学场景：学生用户一键安装，无需额外配置

**缺点**：
- ⚠️ 需要自建 IDE 体验（文件树 / Tab / 命令面板 / 设置页等）
- ⚠️ 不兼容 VS Code 扩展生态
- ⚠️ Monaco 编辑器是 VS Code 同源，但不是完整 VS Code

**结论**：✅ 当前方案（推荐继续）

### 7.2 三路径对比表

| 维度 | 路径 A fork VS Code | 路径 B VS Code 扩展 | 路径 C @monaco-editor/react（当前） |
|------|--------------------|--------------------|----------------------------------|
| 集成难度 | 极高 | 中 | 低（已实施） |
| 灵活性 | 极高 | 受限 | 高 |
| 维护成本 | 极高（VS Code 频繁更新） | 低 | 中 |
| 性能 | 原生 | 原生 | Electron |
| 包体 | 500MB+ | 0（依赖用户 VS Code） | 200MB（Electron 标准） |
| VS Code 扩展兼容 | ✅ 完全兼容 | ✅ 完全兼容 | ❌ 不兼容 |
| 与现有架构一致 | ❌ 推倒重来 | ❌ 不一致 | ✅ 完全一致 |
| 教学场景门槛 | 高（包体大） | 高（需先装 VS Code） | 低（一键安装） |
| 与 SftpManager 关系 | ❌ 重复 | ❌ 重复 | ✅ 复用 |
| 硬约束对齐 | ❌ 违反"基于现有 SftpManager 扩展" | ❌ 违反 | ✅ 完全对齐 |
| 推荐 | ❌ 不推荐 | ⚠️ 可选 | ✅ 当前方案 |

### 7.3 推荐方案：路径 C + 借鉴路径 B 的扩展机制

**结论**：
- **主路径**：继续走路径 C（@monaco-editor/react + 自建 IDE 体验），这是 v0.8 IDE MVP 已落地的方案
- **借鉴**：从路径 B（VS Code 扩展 API）借鉴扩展机制设计思想，构建 tdsf-linux-desktop 的内部插件机制
- **借鉴**：从 Cline 的"扩展即 agent tool"模式借鉴，让 8 个 Subagent 成为可插拔的"内部扩展"

**理由**：
1. **硬约束对齐**：路径 C 完全对齐"IDE 工作台必须基于现有 SftpManager 扩展，不引入 code-server/Theia"
2. **架构一致**：路径 C 与现有 Electron 30 + React 18 + Ant Design 5 + Zustand 架构完全一致
3. **教学场景**：路径 C 一键安装，无需用户额外配置 VS Code
4. **维护成本**：路径 C 维护成本中等，不需要 merge VS Code 上游
5. **扩展性**：借鉴路径 B 的 VS Code 扩展 API 设计，为 tdsf-linux-desktop 设计内部插件机制，未来可支持第三方扩展

### 7.4 VS Code 扩展机制借鉴清单

从 VS Code 扩展 API 借鉴以下设计思想到 tdsf-linux-desktop 的内部插件机制：

| VS Code 扩展 API | 借鉴设计 | tdsf-linux-desktop 落地 |
|----------------|---------|----------------------|
| `vscode.workspace.fs` | 文件系统抽象（readFile / writeFile / stat / delete） | SftpManager 已有类似 API，可对外暴露为内部插件接口 |
| `vscode.window.createTerminal` | 终端创建 API | xterm.js + SSH 会话管理已实现，可包装为 `tdsf.terminal.create()` |
| `vscode.window.activeTextEditor` | 活跃编辑器 API | Monaco Editor 实例管理 + Tab 系统 |
| `vscode.commands.registerCommand` | 命令注册 API | 命令面板（Ctrl+Shift+P）+ 内部命令注册表 |
| `vscode.window.showInputBox` / `showQuickPick` | 输入框 / 快速选择 API | Ant Design 5 Modal / Select 已有，可包装 |
| `vscode.workspace.onDidChangeTextDocument` | 文档变更事件 | Monaco Editor `onDidChangeModelContent` |
| `vscode.LanguageModelChat`（1.90+） | Language Model API | Provider 工厂 + Mastra Agent 已有，可包装为 `tdsf.ai.chat()` |
| `vscode.lm.selectChatModels` | 模型选择 API | Provider 工厂的 `listProviders()` + `setDefault()` |
| `vscode.ChatRequestHandler` | Chat 请求处理 | ChatPanel + Agent Runtime 的 `agent:chat` IPC |
| `vscode.ChatParticipant` | Chat 参与者 | 8 个 Subagent 注册为 Chat Participant |
| `vscode.tasks` | 任务系统 | 任务面板（v1.0+）+ 故障复盘自动化 |
| `vscode.debug` | 调试 API | 远程调试（v1.3+） |

**关键启示**：
- **`vscode.lm.*` Language Model API**（VS Code 1.90+ 原生 AI API）是 VS Code 官方的 AI 集成方案，tdsf-linux-desktop 可借鉴其设计思想，在 Provider 工厂 + Mastra Agent 基础上包装一层 `tdsf.ai.*` 内部 API
- **`@monaco-editor/react` 已是 VS Code 同源体验**：Monaco Editor 是 VS Code 的核心编辑器组件，用 `@monaco-editor/react` 已经获得了 VS Code 90% 的编辑体验
- **Cline 的"扩展即 agent tool"模式**值得借鉴：把 8 个 Subagent 设计为可插拔的"内部扩展"，每个 Subagent 注册自己的工具和命令

---

## 8. tdsf-linux-desktop Agent 架构演进建议

### 8.1 当前架构（v0.9.1 现状）

**已实施**：
1. **Claude Subagents 文件模式 + Trae IDE Task 工具双层实施**（v0.9.1）
   - `.claude/agents/` 4 个 subagent：architect / backend / api / reviewer
   - YAML frontmatter + Markdown 系统提示
   - Claude Code CLI 可通过 `/agents` 命令调用
   - Trae IDE 内通过 `general_purpose_task` subagent 模拟 4 个角色

2. **risk-engine-ast + sandbox:execute IPC 已就绪**（v0.9.0 + v0.9.1）
   - `src/main/core/risk-engine-ast.ts`（236 行，主入口）
   - `src/main/core/risk-engine-rules.ts`（282 行，类型 + 高/中危命令规则）
   - `src/main/core/risk-engine-ast-utils.ts`（224 行，AST 遍历工具）
   - tree-sitter-bash WASM + 6 类绕过覆盖（命令拼接 / 引号拼接 / 命令替换 / 进程替换 / 变量展开 / Base64 混淆）
   - AST 失败时降级到正则方案（Postel's Law）
   - sandbox:execute IPC 层强制审批：waitForSandboxApproval() + sandbox:approval-request 推送 + 30 秒超时 + assessCommandRisk 危险度识别（low/medium/high）

3. **Mastra 框架已选定**（v0.9 方案书 §9 决策 1）
   - Mastra v1.34 + Vercel AI SDK 7 组合
   - 8 个 Provider 工厂骨架：deepseek / qwen / volcengine-ark / anthropic / claude-sdk / google / ollama / openai-compatible
   - ClaudeSdkProvider 不实现 LanguageModelV2 契约，通过专属 IPC 通道调用

4. **IPC 4 步同步已就绪**（v0.9.0）
   - claude-sdk:generate / stream / cancel
   - sandbox:approve + sandbox:approval-request
   - token:records
   - provider:set-default
   - credibility:assess / dag
   - at:list / resolve / parse

5. **sessionKeyMap 句柄模式**（v0.9.0 三项深度加固）
   - session_api_key 不出主进程
   - sandbox:create / list 抹除返回值中的 key
   - sandbox:execute 从 Map 查找
   - sandbox:delete 清理 Map
   - warmupSessionKeyCache 预热缓存

**待实施**：
- ⚠️ 渲染层 UI 未接入（审批弹窗 / Token 监控面板 / ChatPanel 工具栏增强）—— v0.9.1 P0 任务，等用户设计稿
- ⚠️ 8 个运行时 Subagent 骨架未实施 —— v1.0
- ⚠️ 本地上下文感知引擎（静态画像 + 动态历史 + 实时日志）未实施 —— v1.0
- ⚠️ 思维外显 + 置信度可视化未实施 —— v1.1
- ⚠️ 可信度算法（D-S + PCR5 + 6 源证据融合）未实施 —— v1.1
- ⚠️ 本体（Ontology）驱动设计未实施 —— v1.0+ 待用户决策
- ⚠️ Skill 系统 DAG 工作流引擎未实施 —— v1.0+ 待用户决策

### 8.2 演进路线（5 个阶段）

| 阶段 | 版本 | 目标 | 关键交付物 | 依赖 |
|------|------|------|-----------|------|
| **阶段 1** | v0.9.2 | Agent 架构设计深度调研方案书（本方案书） | 本方案书 + 5 个开源项目源码分析清单 | 无 |
| **阶段 2** | v0.9.3 | 渲染层 UI 接入 | 审批弹窗 + Token 监控面板 + ChatPanel 工具栏增强 | v0.9.2 + 用户设计稿 |
| **阶段 3** | v0.9.4 | 5 个开源项目源码分析完成 + 借鉴实施 | Cline / Kilo Code / Continue.dev / Aider 源码分析报告 + 借鉴点落地 | v0.9.2 + 主 agent 调度专门 agent |
| **阶段 4** | v0.9.5 | 千问参考.md 的 Harmony 工程体系全面落地 | 本体（Ontology）+ Skill 系统（DAG 工作流引擎）+ 六层架构 + 控制论思维 + 本地上下文感知引擎 + 思维外显 + 置信度可视化 | v0.9.3 + v0.9.4 |
| **阶段 5** | v1.0 | 稳定版发布 | 8 个运行时 Subagent + 8 类 @命令 + 可信度算法 + 知识库 RAG + 教学辅导 | v0.9.5 |

**演进原则**：
1. **质量绝对优先**：每个阶段必须三重验证（typecheck:node + typecheck:web + build）+ 跳步检查 + reviewer agent 7 维审查
2. **可回滚**：每个版本必须可回滚到上一版本，不引入破坏性变更
3. **不跳步**：v0.9.3 渲染层 UI 接入是 v0.9.4 / v0.9.5 的前置依赖，不能跳过
4. **等用户决策**：本体（Ontology）+ Skill 系统（DAG 工作流引擎）需要用户决策后才能启动

### 8.3 架构改进建议

基于千问参考.md 的 Harmony 工程体系 + 5 个开源项目源码分析清单 + v0.9.1 实施经验，提出 8 项架构改进建议：

#### 8.3.1 引入本体（Ontology）：用 JSON-LD 描述运维领域知识

**目标**：将 Linux 运维领域知识结构化为 JSON-LD 本体，为 Agent 提供确定性约束。

**实施位置**：`src/main/core/knowledge/ontology/`（待建）
- `linux-ops.jsonld`：本体主文件
- `slices/`：场景切片（网络诊断 / 磁盘空间 / 服务管理 / 用户管理 / 包管理 / 安全合规）
- `projector.ts`：投影引擎（根据上下文动态投影相关切片）

**优先级**：P1（待用户决策）

#### 8.3.2 Skill 系统落地：将 8 个 Subagent 标准化为 DAG 工作流

**目标**：将 8 个 Subagent 标准化为可复用的 Skill，用 Mastra `WorkflowAgent` 编排为 DAG 工作流。

**实施位置**：`src/main/core/agent/skills/`（待建）
- `program-skill.ts`：编程 Skill
- `reason-skill.ts`：思考 Skill
- `run-skill.ts`：运行 Skill
- `search-skill.ts`：搜索 Skill
- `skill-skill.ts`：Skill 调用 Skill（Meta-Skill）
- `method-skill.ts`：方法论 Skill
- `history-skill.ts`：历史回溯 Skill
- `knowledge-skill.ts`：知识库 Skill
- `dag-orchestrator.ts`：DAG 工作流编排器

**优先级**：P1（待用户决策）

#### 8.3.3 六层架构分层：知识库 / 规则 / 约束 / Skill / 模型路由 / IO 编排

**目标**：将 tdsf-linux-desktop 的代码按六层架构分层，确保各层职责清晰、可独立演进。

**实施位置**：
- L1 知识库：`src/main/core/knowledge/`（待建）
- L2 规则：`src/main/core/risk-engine-rules.ts`（已实施）
- L3 约束：硬约束清单 + IPC 4 步同步铁律（已实施）
- L4 Skill：`src/main/core/agent/skills/`（待建）
- L5 模型路由：`src/main/core/agent/providers/`（已实施骨架）
- L6 IO 编排：`src/main/ipc/` + `src/preload/index.ts`（已实施）

**优先级**：P2（重构性工作，待 v1.0 稳定后启动）

#### 8.3.4 控制论思维：risk-engine-ast + assessCommandRisk 已有确定性规则，加强独立验证

**目标**：在已有的 AST + 正则双层防御基础上，引入"独立验证"原则。

**实施位置**：
- `src/main/core/agent/independent-verifier.ts`（待建）
- 验证策略：用 DeepSeek V4 生成命令，用 Claude Sonnet 4.5 进行审核
- 沙箱预演：高风险命令在 OpenHands Docker 沙箱中模拟执行

**优先级**：P2（v1.1+）

#### 8.3.5 本地上下文感知引擎：实现静态画像 + 动态历史 + 实时日志三件套

**目标**：解决"AI 不知道我在哪、我刚试了什么、刚才报了什么"的痛点。

**实施位置**：
- `src/main/core/context/static-profile.ts`（待建）
- `src/main/core/context/dynamic-history.ts`（待建）
- `src/main/core/context/log-mount.ts`（待建）

**优先级**：P1（v1.0）

#### 8.3.6 思维外显机制：意图翻译层 + 沙箱预演 + 置信度可视化（D-S + PCR5）

**目标**：解决"AI 黑盒决策，学生不敢敲"的痛点。

**实施位置**：
- `src/main/core/agent/intent-translator.ts`（待建）
- `src/main/core/agent/predictor.ts`（待建）
- `src/main/core/sandbox/dry-run.ts`（待建）
- `src/main/core/credibility/`（v0.9.0 已建目录骨架）
- `src/renderer/src/components/ai/CredibilityPanel.tsx`（待建）
- `src/renderer/src/components/ai/ReasoningChain.tsx`（待建）
- `src/renderer/src/components/ai/RiskBadge.tsx`（待建）

**优先级**：P2（v1.1，依赖可信度算法 D-S + PCR5）

#### 8.3.7 人机协同决策流：诊断 → 提案 → 决策 → 执行与反馈（已有审批闸门）

**目标**：将运维 Agent 的执行流程标准化为"诊断 → 提案 → 决策 → 执行与反馈"四阶段。

**实施位置**：
- 诊断阶段：`src/main/core/agent/supervisor.ts` 的 `diagnose(task)` 方法
- 提案阶段：`src/main/core/agent/supervisor.ts` 的 `propose(diagnosis)` 方法
- 决策阶段：`src/main/ipc/sandbox.ts` 的 `sandbox:approval-request` 强制审批（已实施）
- 执行与反馈阶段：`src/main/core/agent/supervisor.ts` 的 `executeAndObserve(decision)` 方法

**优先级**：P1（v1.0，部分已实施）

#### 8.3.8 Copilot 模式三阶段：辅助（v0.9.x）→ 半自动（v1.0）→ 全自动低风险场景（v1.1+）

**目标**：不要试图一开始就做全能 Agent，从 Copilot 模式阶段一起步，逐步演进。

**阶段一（辅助，v0.9.x ~ v1.0）**：
- 只做查询和诊断，给出建议，人不执行
- 8 个 Subagent 骨架就绪 + 本地上下文感知引擎 + 思维外显

**阶段二（半自动，v1.0 ~ v1.1）**：
- 生成修复脚本，人 Review 后执行
- 可信度算法（D-S + PCR5）+ 独立验证 + 沙箱预演

**阶段三（全自动低风险场景，v1.1+）**：
- 针对低风险场景（如磁盘清理、非核心服务重启）实现无人值守
- 高风险场景仍需人工审批

**优先级**：P3（长期演进）

---

## 9. 风险与回滚方案

### 9.1 风险识别

#### 风险 1：开源项目 License 变更

**案例**：
- Gemini CLI 2026-05-19 闭源化（21 号调研报告红线）
- Roo Code 2026-05-15 归档（21 号调研报告红线）
- Grok Build 2026-07-12 数据丑闻后 7-15 紧急开源（19 号调研报告）

**影响**：
- 已选定的开源项目（Mastra / Vercel AI SDK 7 / Claude Agent SDK / OpenHands）可能突然变更 License
- 已 clone 的源码可能无法继续使用

**应对**：
- 定期（每季度）复查已选定项目的 License 状态
- 维护备选方案清单（如 Mastra → VoltAgent）
- 已 clone 的源码保留在 `opensource-reference/`，即使上游变更 License，已 clone 的版本仍可使用

#### 风险 2：Mastra 框架活跃度下降

**影响**：
- Mastra 是 tdsf-linux-desktop 的核心 Agent 框架，活跃度下降会导致 bug 无人修复、新特性无人开发

**应对**：
- 监控 Mastra GitHub star 数、commit 频率、issue 响应时间
- 维护备选方案：VoltAgent（基于 Vercel AI SDK 7）或自研
- 保持与 Vercel AI SDK 7 的直接集成（Mastra 失效时可降级为纯 AI SDK 7）

#### 风险 3：Claude SDK ESM 兼容性问题（已修复）

**影响**：
- `@anthropic-ai/claude-agent-sdk@0.3.211` 是 ESM-only（sdk.mjs），Electron 主进程 CommonJS require 失败

**应对**（已实施）：
- type-only import 保留编译期类型检查
- 运行时 `await import()` 动态加载 ESM 模块
- 两全其美：编译期类型安全 + 运行时 ESM 兼容

#### 风险 4：渲染层 UI 集成跳步陷阱

**影响**：
- v0.9.0 主进程 IPC 全部就绪但渲染层 UI 是空白，这是最大的跳步陷阱
- v0.9.1 P0 任务（审批弹窗 + Token 监控面板）必须落地，否则 v0.9.3+ 无法启动

**应对**：
- v0.9.3 阶段必须先完成渲染层 UI 接入，再启动 v0.9.4
- reviewer agent 跳步检查：主进程 IPC + 渲染层 UI 是否完整接入
- 等用户设计稿，但先用通用 Ant Design 5 风格实施 P0

#### 风险 5：5 个开源项目源码分析工作量超预期

**影响**：
- Cline 单项目代码量已超 40000 字符（LS 工具截断），完整源码分析工作量巨大
- 5 个项目并行分析可能导致主 agent 上下文爆炸

**应对**：
- 主 agent 调度专门 agent 并行分析（每个项目一个 agent）
- 每个专门 agent 只负责 1 个项目，输出结构化源码分析报告
- 源码分析报告归档到 `idea-to-dev-output/28-源码分析-Cline.md` 等编号文件

#### 风险 6：本体（Ontology）+ Skill 系统过度工程化

**影响**：
- 本体（Ontology）和 Skill 系统是千问参考.md 的核心理念，但 tdsf-linux-desktop 作为教学项目，可能不需要如此复杂的架构
- 过度工程化会拖慢开发进度

**应对**：
- 本体（Ontology）和 Skill 系统的启动需要用户决策（§11 待用户决策）
- 先做 PoC（概念验证），验证可行后再全面落地
- 保留降级方案：如果本体（Ontology）过度复杂，可降级为简单的 JSON 配置文件 + RAG 检索

### 9.2 回滚方案

**每个版本的回滚策略**：

1. **三重验证**：每个版本必须通过 typecheck:node + typecheck:web + build 三重验证才能发布
2. **跳步检查**：reviewer agent 必须完成跳步检查清单（主进程 IPC + 渲染层 UI + 编译通过 + 运行时跑通 + 类型定义 + 类型引用 + 单元测试 + 归档补充）
3. **reviewer agent 7 维审查**：安全 / 性能 / 正确性 / 可维护性 / 测试 / 可访问性 / 文档
4. **Git tag**：每个版本发布时打 Git tag（v0.9.2 / v0.9.3 / ...），可随时回滚到任意版本
5. **阶段门禁**：v0.9.3 渲染层 UI 接入未完成，不启动 v0.9.4；v0.9.4 源码分析未完成，不启动 v0.9.5

---

## 10. 实施任务清单

### 10.1 P0（阻塞类，必须完成）

- [ ] **P0-1**：等待 5 个开源项目源码分析完成（Cline ✅ 已 clone / Kilo Code ⏳ / Continue.dev ⏳ / Aider ⏳ + MetaGPT/OpenHands/Mastra/crewAI 已分析）
  - 主 agent 调度专门 agent 并行分析
  - 每个项目输出结构化源码分析报告，归档到 `idea-to-dev-output/28-源码分析-{项目名}.md`
  - 优先级：P0
  - 阻塞：v0.9.4 阶段

- [ ] **P0-2**：渲染层 UI 接入（审批弹窗 + Token 监控面板）
  - 实施位置：`src/renderer/src/components/ai/SandboxApprovalDialog.tsx` + `src/renderer/src/components/ai/TokenMonitorPanel.tsx`
  - 依赖：v0.9.1 P0-1 / P0-2（主进程 IPC 已就绪）+ 用户设计稿
  - 优先级：P0
  - 阻塞：v0.9.3 阶段

### 10.2 P1（重要类，强烈建议完成）

- [ ] **P1-1**：本体（Ontology）驱动设计 PoC
  - 实施位置：`src/main/core/knowledge/ontology/`
  - 范围：Linux 运维领域本体（JSON-LD）+ 切片机制 + 投影引擎
  - 优先级：P1（待用户决策）
  - 阻塞：v0.9.5 阶段

- [ ] **P1-2**：Skill 系统 DAG 工作流引擎设计
  - 实施位置：`src/main/core/agent/skills/`
  - 范围：8 个 Skill 标准化 + DAG 编排器 + Meta-Skill
  - 优先级：P1（待用户决策）
  - 阻塞：v0.9.5 阶段

- [ ] **P1-3**：本地上下文感知引擎实施
  - 实施位置：`src/main/core/context/`（static-profile.ts + dynamic-history.ts + log-mount.ts）
  - 范围：静态画像注入 + 动态历史回溯 + 实时日志挂载
  - 优先级：P1
  - 阻塞：v1.0 阶段

### 10.3 P2（优化类，可选）

- [ ] **P2-1**：思维外显 + 置信度可视化
  - 实施位置：`src/main/core/agent/intent-translator.ts` + `src/main/core/agent/predictor.ts` + `src/main/core/credibility/` + `src/renderer/src/components/ai/CredibilityPanel.tsx` + `src/renderer/src/components/ai/ReasoningChain.tsx` + `src/renderer/src/components/ai/RiskBadge.tsx`
  - 范围：意图翻译层 + 沙箱预演 + 6 源证据融合（D-S + PCR5）+ React Flow DAG 可视化
  - 优先级：P2
  - 阻塞：v1.1 阶段

- [ ] **P2-2**：人机协同决策流 UI
  - 实施位置：`src/renderer/src/components/ai/DecisionFlow.tsx`
  - 范围：诊断 → 提案 → 决策 → 执行与反馈四阶段 UI
  - 优先级：P2
  - 阻塞：v1.0 阶段（部分已实施：sandbox:approval-request 强制审批）

### 10.4 P3（长期演进）

- [ ] **P3-1**：Copilot 模式三阶段演进
  - 阶段一（辅助，v0.9.x ~ v1.0）：只查询和诊断
  - 阶段二（半自动，v1.0 ~ v1.1）：生成修复脚本，人 Review 后执行
  - 阶段三（全自动低风险场景，v1.1+）：磁盘清理 / 非核心服务重启等无人值守
  - 优先级：P3
  - 阻塞：v1.1+ 阶段

- [ ] **P3-2**：六层架构分层重构
  - 实施位置：`src/main/core/` 整体重构
  - 范围：L1 知识库 / L2 规则 / L3 约束 / L4 Skill / L5 模型路由 / L6 IO 编排
  - 优先级：P3
  - 阻塞：v1.0 稳定后

---

## 11. 待用户决策

在启动 v0.9.3 及后续阶段前，请用户确认以下 5 项决策：

### 决策 1：是否启动 v0.9.2 源码分析阶段（5 个开源项目）

**推荐**：✅ 启动

**理由**：
- 5 个开源项目（Cline / Kilo Code / Continue.dev / Aider + 已分析的 MetaGPT/OpenHands/Mastra/crewAI）是 v0.9.4 借鉴实施的基础
- 硬约束"开源源码必须全量分析"要求不能仅凭 README/文档判断
- 主 agent 可调度专门 agent 并行分析，不阻塞 v0.9.3 渲染层 UI 接入

**待用户确认**：
- [ ] 是否同意启动 5 个开源项目源码分析？
- [ ] 是否同意主 agent 调度专门 agent 并行分析（每个项目一个 agent）？
- [ ] 是否同意源码分析报告归档到 `idea-to-dev-output/28-源码分析-{项目名}.md`？

### 决策 2：是否引入本体（Ontology）驱动设计（JSON-LD）

**推荐**：⚠️ 先做 PoC，再决定是否全面落地

**理由**：
- 千问参考.md 的本体（Ontology）驱动是核心理念，但 tdsf-linux-desktop 作为教学项目，可能不需要如此复杂
- JSON-LD 对大模型友好，但需要额外的学习成本和维护成本
- 建议先做 PoC（v0.9.5 阶段），验证可行后再全面落地

**待用户确认**：
- [ ] 是否同意启动本体（Ontology）驱动设计 PoC？
- [ ] 是否参考 GB/T 48320-2026 本体标准？
- [ ] 是否同意 JSON-LD 作为序列化格式？

### 决策 3：Skill 系统是否落地为 DAG 工作流引擎

**推荐**：⚠️ 先做 PoC，再决定是否全面落地

**理由**：
- Skill 系统（DAG 工作流引擎）是千问参考.md 的核心理念，但 8 个 Subagent 的 DAG 编排复杂度较高
- Mastra `WorkflowAgent` 已提供 DAG 工作流能力，可直接复用
- 建议先做 PoC（v0.9.5 阶段），验证可行后再全面落地

**待用户确认**：
- [ ] 是否同意启动 Skill 系统 DAG 工作流引擎 PoC？
- [ ] 是否同意 8 个 Subagent 标准化为 Skill？
- [ ] 是否同意 Mastra `WorkflowAgent` 作为 DAG 编排器？

### 决策 4：本地上下文感知引擎的实施优先级

**推荐**：✅ P1 优先级（v1.0 阶段实施）

**理由**：
- 本地上下文感知引擎（静态画像 + 动态历史 + 实时日志）是解决"AI 缺乏情境意识"痛点的核心方案
- 用户在千问参考.md 第 159 行明确表达了这个痛点
- 实施难度中等，可在 v1.0 阶段与 8 个 Subagent 同步实施

**待用户确认**：
- [ ] 是否同意本地上下文感知引擎为 P1 优先级？
- [ ] 是否同意 v1.0 阶段实施？
- [ ] 是否同意 `Ctrl+L` 快捷键触发实时日志挂载？

### 决策 5：Copilot 模式三阶段的时间线

**推荐**：
- 阶段一（辅助）：v0.9.x ~ v1.0（与 8 个 Subagent + 本地上下文感知引擎同步）
- 阶段二（半自动）：v1.0 ~ v1.1（与可信度算法 + 独立验证同步）
- 阶段三（全自动低风险场景）：v1.1+（与沙箱预演 + 数字孪生同步）

**理由**：
- 不要试图一开始就做全能 Agent
- 阶段一建立用户信任，阶段二引入自动化，阶段三扩大自动化范围
- 高风险场景始终需要人工审批（硬约束）

**待用户确认**：
- [ ] 是否同意 Copilot 模式三阶段的时间线？
- [ ] 是否同意阶段三只覆盖低风险场景（磁盘清理 / 非核心服务重启）？
- [ ] 是否同意高风险场景始终需要人工审批？

---

## 附录 A：千问参考.md 核心理念速查

### A.1 生产力公式

> 生产力 = 模型能力 × Harmony 工程能力（相乘关系，非相加）

### A.2 Harmony 工程关键技术

1. **本体（Ontology）驱动**：JSON-LD 序列化 + 切片 + 投影
2. **Skill（技能）系统**：Meta-Skill 编排 + DAG 工作流引擎
3. **六层架构**：知识库 / 规则 / 约束 / Skill / 模型路由 / IO 编排
4. **控制论思维**：确定性规则 + AI 不确定性互补
5. **数字员工"人才流水线"**：Build → Hire → Onboard → Deploy

### A.3 运维 Agent 五层架构

1. **感知层**（Perception & Ingestion Layer）：多模态数据接入 + 实时告警收敛 + 上下文关联
2. **大脑编排层**（Orchestrator & Reasoning Layer）：意图识别 + RAG 增强 + 思维链规划
3. **专家技能层**（Skill/Tool Layer）：SRE 诊断 / DBA / K8s / 安全合规 Agent
4. **执行与沙箱层**（Execution & Sandbox Layer）：人机协同确认 + 只读/读写分离 + 模拟演练
5. **记忆与进化层**（Memory & Evolution Layer）：故障复盘 + 自我修正

### A.4 用户痛点官方术语

1. **缺乏情境意识**（Lack of Situational Awareness）
2. **认知摩擦与信任赤字**（Cognitive Friction & Trust Deficit）
3. **缺乏反馈回路验证**（Absence of Feedback Loop Verification）
4. **不可解释的黑盒决策**（Unexplainable Black-box Decision Making）

### A.5 Copilot 模式三阶段

1. **阶段一（辅助）**：只做查询和诊断，给出建议，人不执行
2. **阶段二（半自动）**：生成修复脚本，人 Review 后执行
3. **阶段三（全自动）**：针对低风险场景实现无人值守

---

## 附录 B：v0.9.x 版本演进路线图

```
v0.9.0（已完成）
  ├─ 自检闭环（P-1 ~ P-10 修复）
  ├─ 三项深度加固（ESM 修复 / warmupSessionKeyCache / tree-sitter-bash AST）
  └─ IPC 全部就绪（sandbox:execute 强制审批 + token:records + provider:set-default）

v0.9.1（已完成）
  ├─ 4 Agent 协作开发模式（architect / backend / api / reviewer）
  ├─ .claude/agents/ 4 个 subagent 定义
  ├─ risk-engine-ast 单元测试（46 个测试）
  ├─ warmupSessionKeyCache 集成测试（9 个测试）
  └─ risk-engine-ast.ts 拆分为 3 文件（rules / ast-utils / ast）

v0.9.2（本方案书）
  ├─ Agent 架构设计深度调研方案书
  ├─ 千问参考.md 的 Harmony 工程体系映射
  ├─ VSCode 开源集成可行性分析（路径 C + 借鉴路径 B）
  ├─ 5 个开源项目源码分析清单（待主 agent 调度专门 agent）
  └─ 8 项架构改进建议

v0.9.3（待启动，等用户设计稿）
  ├─ 渲染层审批弹窗组件（SandboxApprovalDialog.tsx）
  ├─ Token 监控面板接入 tokenRecords
  └─ ChatPanel 工具栏增强

v0.9.4（待启动，依赖 v0.9.2 源码分析）
  ├─ Cline 源码分析完成 + 借鉴实施
  ├─ Kilo Code 源码分析完成 + 借鉴实施
  ├─ Continue.dev 源码分析完成 + 借鉴实施
  └─ Aider 源码分析完成 + 借鉴实施

v0.9.5（待启动，依赖 v0.9.3 + v0.9.4）
  ├─ 本体（Ontology）驱动设计 PoC
  ├─ Skill 系统 DAG 工作流引擎 PoC
  ├─ 本地上下文感知引擎实施
  ├─ 思维外显 + 置信度可视化
  └─ 人机协同决策流 UI

v1.0（稳定版发布）
  ├─ 8 个运行时 Subagent
  ├─ 8 类 @命令
  ├─ 可信度算法（D-S + PCR5 + 6 源证据融合）
  ├─ 知识库 RAG
  └─ 教学辅导
```

---

## 附录 C：硬约束对齐清单

| # | 硬约束 | 本方案书对齐 |
|---|-------|------------|
| HC-1 | IDE 工作台必须基于现有 SftpManager 扩展，不引入 code-server/Theia | §7 推荐路径 C（@monaco-editor/react），不引入 code-server/Theia |
| HC-2 | Agent 架构必须用 TS 原生框架（Mastra / Vercel AI SDK / Claude Agent SDK），不引入 Python 进程通信 | §5.3 已选定 Mastra + Vercel AI SDK 7 + Claude Agent SDK，不引入 Python |
| HC-3 | 可信度算法必须有论文支撑（D-S + PCR5），不能拍脑袋 | §4.2 + §4.4 引用 22 号调研报告（43 篇论文） |
| HC-4 | @命令必须支持鼠标划选注入 | §4.1 + §3.1 引用 v0.9 方案书 §4.2 鼠标划选注入交互 |
| HC-5 | 运维 Agent 每步执行必须有人工审批闸门 | §3.4 + §4.3 sandbox:execute IPC 强制审批 + 30 秒超时 |
| HC-6 | 不反编译 Claude Code | §5.1 用官方 `@anthropic-ai/claude-agent-sdk` 合法集成 |
| HC-7 | 所有网络请求必须 UI 可见 | §3.4 + §4.4 HC-1 网络日志可见 |
| HC-8 | 敏感文件默认 redact | §3.4 + §4.4 敏感文件 redact（.env / .ssh/ / *_key） |
| HC-9 | 本地优先（默认 Ollama / 国内 Provider，海外 API 需显式开启） | §5.3 + §8.3 默认 Ollama / DeepSeek / 通义千问，海外 API 显式开启 |
| HC-10 | Token 消耗必须透明 | §3.4 + §4.4 token:records IPC + Token 监控面板（v0.9.3 P0-2） |
| HC-11 | 所有新组件/页面 CSS 必须用 `var(--color-*)` | §8.3 不涉及 CSS，但实施时遵循 |
| HC-12 | 主色使用低饱和靛蓝 `#4f46e5`（亮）/ `#818cf8`（暗） | §8.3 不涉及颜色，但实施时遵循 |
| HC-13 | 卡片 hover 仅允许一种变化：阴影 | §8.3 不涉及卡片，但实施时遵循 |
| HC-14 | 质量绝对优先 | §9.2 三重验证 + 跳步检查 + reviewer agent 7 维审查 |
| HC-15 | 开源源码必须全量分析 | §6 5 个开源项目源码分析清单 + 已 clone 到 opensource-reference/ |

---

## 附录 D：参考资料

### D.1 千问参考.md
- 路径：`d:\ai\linux教学一体\参考资料\千问参考.md`
- 核心理念：Harmony 工程体系 + 运维 Agent 五层架构 + 用户痛点深度剖析

### D.2 已有调研文档（位于 `d:\ai\linux教学一体\idea-to-dev-output\`）
- 15 号：开源项目调研-WebIDE与SSH文件管理集成方案.md（IDE 选型，路径 C 的依据）
- 16 号：开源项目调研-AIAgent编码架构.md（Agent 框架对比）
- 17 号：方案书-v9.0-AI原生运维IDE.md（v9.0 主方案书）
- 18 号：开源项目调研-AI编程沙箱方案.md（沙箱选型）
- 19 号：开源调研-Grok-Code-Agent架构.md（Grok Build）
- 20 号：开源调研-Claude-Code源码与集成可行性.md（Claude Code SDK）
- 21 号：开源调研-高新Agent架构选型.md（Mastra + Vercel AI SDK 7）
- 22 号：可信度算法论文支撑调研.md（D-S + PCR5 + 43 篇论文）
- 23 号：方案书-v0.9-Agent架构与AI集成.md（v0.9 方案书）
- 24 号：源码分析-Mastra框架.md（Mastra 源码已分析）
- 25 号：源码分析-OpenHands沙箱.md（OpenHands 源码已分析）
- 26 号：方案书-v0.9修订版-质量优先实施.md（v0.9 修订版）
- 27 号：调研-Bash命令解析库选型-危险命令识别.md（tree-sitter-bash）

### D.3 最新方案书
- 路径：`d:\ai\linux教学一体\tdsf-linux-desktop\docs\方案书-v0.9.1-4Agent协作开发模式.md`
- 内容：4 Agent 协作开发模式（architect / backend / api / reviewer）

### D.4 项目硬约束
- 路径：`c:\Users\Lenovo\.trae-cn\memory\projects\-d-ai-linux----\project_memory.md`
- 内容：Hard Constraints + Engineering Conventions + Lessons Learned + v0.9.0/v0.9.1 实施记录

### D.5 已实施代码
- `src/main/core/risk-engine-ast.ts`（236 行，AST 风险评估主入口）
- `src/main/core/risk-engine-rules.ts`（282 行，类型 + 高/中危命令规则）
- `src/main/core/risk-engine-ast-utils.ts`（224 行，AST 遍历工具）
- `.claude/agents/architect.md`（架构师 subagent 定义）
- `.claude/agents/backend.md`（后端工程师 subagent 定义）
- `.claude/agents/api.md`（跨进程接口工程师 subagent 定义）
- `.claude/agents/reviewer.md`（代码审查 subagent 定义）

### D.6 开源项目（位于 `d:\ai\linux教学一体\opensource-reference\`）
- `cline/`（Cline，Apache-2.0，已 clone，待源码分析）
- `kilo-code/`（Kilo Code，Apache-2.0，待 clone）
- `continue-dev/`（Continue.dev，Apache-2.0，待 clone）
- `aider/`（Aider，Apache-2.0，待 clone）
- `MetaGPT/`（MetaGPT，MIT，已分析）
- `OpenHands/`（OpenHands，MIT，已分析 + 已实施沙箱）
- `mastra/`（Mastra，Apache-2.0，已分析 + 已选定）
- `crewAI/`（crewAI，MIT，已分析）

---

## 附录 E：本方案书与用户输入的对应关系

| 用户输入要点 | 方案书对应章节 |
|------------|-------------|
| 千问参考.md 的 Harmony 工程体系映射 | §2 千问参考.md 的 Harmony 工程体系映射 |
| 运维 Agent 五层架构 | §3 运维 Agent 五层架构映射 |
| 用户痛点深度剖析与解决方案 | §4 用户痛点深度剖析与解决方案 |
| 主流开源 Agent 架构对比分析 | §5 主流开源 Agent 架构对比分析 |
| 5 个开源项目源码分析清单 | §6 待补充：5 个开源项目源码分析清单 |
| VSCode 开源集成可行性分析（用户最关心） | §7 VSCode 开源集成可行性分析 |
| tdsf-linux-desktop Agent 架构演进建议 | §8 tdsf-linux-desktop Agent 架构演进建议 |
| 风险与回滚方案 | §9 风险与回滚方案 |
| 实施任务清单 | §10 实施任务清单 |
| 待用户决策 | §11 待用户决策 |
| 不重复已有调研 | §5.3 + §6.5 已分析项目复用，只引用结论不重写 |
| 聚焦三个核心 | §2 + §7 + §6 三个核心章节 |
| 质量绝对优先 | §9.2 三重验证 + 跳步检查 + reviewer agent 7 维审查 |
| 诚实标注（源码分析章节） | §6 所有未完成项目明确标"待主 agent 调度专门 agent 分析" |
| 必须保留所有硬约束 | 附录 C 硬约束对齐清单（15 条全部对齐） |
| 输出语言：中文 | 全文中文 |
| 不修改任何代码文件 | 本方案书为纯 markdown 输出，未修改任何代码文件 |

---

**方案书结束**

> 本方案书为 v0.9.2 阶段的深度调研方案书，不修改任何代码文件。后续 v0.9.3（渲染层 UI 接入）+ v0.9.4（5 个开源项目源码分析）+ v0.9.5（Harmony 工程体系全面落地）将基于本方案书展开。
>
> 等待用户对 §11 待用户决策的 5 项决策确认后，启动下一阶段。
