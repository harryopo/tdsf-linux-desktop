# v2.3 UI 与功能平衡修复规划

> 用户要求：保证质量优先，不为了拆分大文件而拆分，继续修复 UI 和功能的平衡问题
> 基础：CLAUDE.md v2.5 + AGENTS.md v8.7（B1 单文件行数放宽）
> 目标：修复渲染层 WIP 功能 + UI 交互问题 + 功能完整性

---

## 一、当前 WIP 问题清单（从源码扫描）

### 高优先级（用户可感知的功能缺失）

| 文件 | 行号 | WIP 内容 | 影响 | 修复方案 |
|------|------|----------|------|----------|
| HistoryPage.tsx | 138 | 时间范围筛选未接过滤逻辑 | 历史记录页筛选无效 | 接入真实过滤逻辑或移除禁用 |
| GeneralSettings.tsx | 218/279 | 非 Electron 环境降级为提示 | 设置项在 Electron 下应真实生效 | 确认 Electron 环境下真实调用 IPC |
| KnowledgePage.tsx | 133 | 非 Electron 环境降级为本地演示 | 知识库页面非真实数据 | Electron 环境下接真实 kbList IPC |
| LogsPage.tsx | 85 | 非 Electron 环境降级为静态数据 | 日志页静态数据 | Electron 环境下接真实 logQuery IPC |
| TutorialDetailPage.tsx | 162/324 | 非 Electron 环境示例数据 | 教程详情非真实 | Electron 环境下接真实 tutorial IPC |
| DecisionDetailPage.tsx | 738/749 | loopConfirm 新命令参数待扩展 | 决策修复命令无法修改 | 扩展主进程 loopConfirm 支持 newCommand |
| AIPanel.tsx/Composer.tsx | - | 上下文压缩 WIP | AI 对话长上下文 | 实现上下文压缩 |

### 中优先级（UI/UX 细节）

| 文件 | 行号 | WIP 内容 | 影响 | 修复方案 |
|------|------|----------|------|----------|
| AboutSettings.tsx | 14/64/75 | APP_BUILD_TIME/INSTALL_PATH 示例值占位 | 关于页信息不准确 | 从 Electron app.getVersion()/getPath 获取 |
| KnowledgeDetailPage.tsx | 78/124 | 非 Electron 环境示例数据 | 知识详情 fallback | 已接 kbGet，确认真实数据链路 |
| MonitorPage.tsx | 297 | 无告警时灰色占位 | 监控页空状态 | 设计稿空状态实现 |
| ChatPanel.tsx | - | 需要检查具体 WIP | AI 对话面板 | 读取源码确认 |

---

## 二、Phase T · UI 与功能平衡修复（8 Task）

### T.1 HistoryPage 时间范围筛选接真实过滤
- 读取 HistoryPage.tsx L138 附近
- 实现时间范围筛选器 onChange → 过滤 historyData
- 如果无后端时间过滤，前端按 timestamp 过滤

### T.2 GeneralSettings 真实 IPC 接入
- 读取 GeneralSettings.tsx L218/L279
- 确认 Electron 环境下调用正确的 IPC（appearance/general/decision 设置保存）
- 移除不必要的 WIP 提示，或改为仅在非 Electron 环境显示

### T.3 KnowledgePage 真实数据接入
- 读取 KnowledgePage.tsx L133
- Electron 环境下调用 `kbList` IPC（或其他已有通道）
- 移除本地演示 fallback

### T.4 LogsPage 真实数据接入
- 读取 LogsPage.tsx L85
- Electron 环境下调用 `log:query` IPC
- 移除静态数据 fallback

### T.5 TutorialDetailPage 真实数据接入
- 读取 TutorialDetailPage.tsx L162
- Electron 环境下调用 tutorial 详情 IPC
- 移除设计稿示例数据 fallback

### T.6 DecisionDetailPage loopConfirm 支持新命令
- 读取 DecisionDetailPage.tsx L738-L749
- 扩展 `src/main/ipc/loop-engineering.ts` 的 loopConfirm handler 支持 newCommand 参数
- 同步更新 preload 和 electron.d.ts 类型

### T.7 AIPanel 上下文压缩实现
- 读取 AIPanel.tsx/Composer.tsx 的上下文压缩 WIP
- 实现简单策略：消息数超过阈值时，只保留系统提示 + 最近 N 条 + 摘要
- 或调用已有 LLM 总结接口

### T.8 AboutSettings 真实应用信息
- 读取 AboutSettings.tsx 和 about-settings.constants.ts
- 从 Electron 获取真实版本号、构建时间、安装路径
- 新增 IPC 通道 `app:get-info`（4 步同步）

---

## 三、Phase U · UI 交互细节修复（6 Task）

### U.1 检查所有页面滚动问题
- 用户之前反馈部分页面无法上下滑动
- 检查所有 Page 组件的 overflow 设置
- 确保内容区 `overflow-y: auto` 且高度正确

### U.2 检查所有按钮点击反馈
- 用户反馈模块点击无动效
- 检查 antd Button 和自定义按钮的 active/hover 状态
- 添加合适的 transition/micro-interaction

### U.3 检查所有 Modal/Dialog 关闭行为
- 确保 ESC 关闭、点击蒙层关闭一致
- 检查关闭后状态清理

### U.4 检查表单验证和错误提示
- 所有表单提交前验证
- 错误信息用 message.error 提示用户

### U.5 检查空状态和 loading 状态
- 列表为空时显示设计稿空状态
- 数据加载中显示 Spin/Skeleton

### U.6 检查响应式布局
- 窗口尺寸调整时布局不崩
- 最小窗口尺寸约束

---

## 四、执行顺序

1. **Phase T**（功能真实化）→ 用户可感知
2. **Phase U**（UI 细节）→ 体验提升
3. **Phase V**（归档）

---

## 五、验收标准

- 所有修改通过 `pnpm typecheck:node` + `pnpm typecheck:web` + `pnpm lint`
- WIP 数量从当前 29 个减少至少 8 个
- 不拆分大文件（除非职责确实混杂）
- 所有新增 IPC 完成 4 步同步
- 所有颜色用 var(--color-*) 或 var(--trae-*)
