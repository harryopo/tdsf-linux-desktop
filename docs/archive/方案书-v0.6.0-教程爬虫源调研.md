# 方案书 v0.6.0-1：教程爬虫源调研与合规策略

> 调研者：TDSF 开发
> 调研时间：2026-07-16
> 调研方法：agent-reach 全网 + GitHub + robots.txt 实测
> 输出位置：d:/ai/linux教学一体/tdsf-linux-desktop/方案书-v0.6.0-教程爬虫源调研.md

---

## 〇、决策记录（2026-07-16 用户确认）

| 决策点 | 用户选择 | 行动项 |
|---|---|---|
| 1. 爬虫框架 | **A. 暂不引入 Crawlee** | 继续用 cheerio+turndown；在 `tutorial-source-registry.ts` 预留 Crawlee 兼容接口 |
| 2. Phase2 源范围 | **B. 中等范围** | kernel.org + wiki.debian.org（MediaWiki）|
| 3. CC 协议策略 | **A. 项目级 CC BY-SA 4.0** | "关于"页声明 + 强制每条 source 标注 + License 字段 |
| 4. 429/Retry-After | **A. 纳入 Phase1** | 在 `polite-fetch.ts` 中实现 Token Bucket + 429 自动退避 |

Phase 1 任务清单：
- [x] robots.txt 实测
- [x] 开源项目调研（含开源实战案例 / Linux 教程博客源补充调研）
- [x] CC 协议调研
- [x] typecheck 验证现状
- [x] 429/Retry-After 处理（Polite-Fetch 抽象，已实现 `polite-fetch.ts`）
- [x] **补充 Phase 1-d GitHub clone 源**（tldr-pages / the-art-of-command-line）
  - 新增 `tldr-pages-offline.ts`（git clone --depth=1 --filter=blob:none --sparse）
  - 新增 `art-of-command-line-offline.ts`（git clone + 按 `##` 拆分为章节）
  - `crawler-types.ts` 中 `CrawlerSourceKind` 添加 `'github-clone'` 枚举值
  - `tutorial-source-registry.ts` 注册 2 个新源（P0 优先级，enabledByDefault=true）
  - `tutorial-crawler-service.ts` CRAWLERS 映射表挂载新爬虫
  - `tutorial-repo.ts` 持久化 licenseUrl / kind 字段
- [x] **Phase 1-c 强化 source 标注**（CC BY-SA 4.0 必填）
  - `html-to-tutorial.ts` 已校验 sourceName + license 必填
  - `tutorial-repo.ts` 完整保存 source.name / source.url / source.license / source.licenseUrl / source.kind / source.crawledAt 到 SQLite JSON
  - `tldr-pages-offline.ts` / `art-of-command-line-offline.ts` 已在 entry 中标注完整 source 字段
- [x] **Phase 1-e 最终 typecheck + lint 验证**
  - ✅ `pnpm typecheck` 通过（0 错误，node + web 两侧）
  - ✅ 修复 1 个预先存在错误（turndown-plugin-gfm 缺类型，补充 `src/main/types/turndown-plugin-gfm.d.ts`）
  - ✅ 修复 1 个 tsconfig 配置（web 端 include 加 preload）
  - ✅ 修复 1 个 web 端类型（`electron.d.ts` 补全爬虫 IPC）
  - ⚠️ lint 18 个错误均为**预先存在**（与本次 Phase 1 无关），详见 §10

> **Phase 1 现状（2026-07-16 更新）**
> - `src/main/services/tutorial/crawler/polite-fetch.ts` 已实现 TokenBucket + Jitter + 429/Retry-After + AbortController 超时
> - `src/main/services/tutorial/crawler/ldp-howtos-offline.ts` 已重构使用 politeFetch（3 并发 / 1 req/s）
> - `src/main/services/tutorial/crawler/tldr-pages-offline.ts` 已实现 GitHub clone 抓 1500+ 命令速查
> - `src/main/services/tutorial/crawler/art-of-command-line-offline.ts` 已实现 GitHub clone 拆 README 为章节
> - `tutorial-source-registry.ts` 已注册 4 个 Phase 1 默认启用源：arch-wiki / ldp-howtos / tldr-pages / art-of-command-line
> - 下一步：进入 Phase 2（kernel.org / wiki.debian.org / Linux Journey / GNU Manuals）

---

## 一、调研背景

之前的 `方案书-v0.6.0-教程爬虫与权威源同步.md` 已经设计了三阶段爬虫架构
（Phase 1 离线 dump / Phase 2 在线增量 / Phase 3 调度监控），并已落地：
- Arch Wiki 离线包抓取器
- LDP HOWTOs 抓取器
- cheerio + turndown 解析
- SQLite 持久化、IPC 通道、UI Modal

但**在动手开发前必须先确认三件事**：
1. **去哪里爬**？哪些源是官方/权威/可信任的？
2. **让不让爬**？robots.txt 怎么说？CC 协议怎么说？
3. **是否有开源经验**？避免重复造轮子，可借鉴成熟方案。

---

## 二、源 robots.txt 合规性实测

> 通过 `curl https://<domain>/robots.txt` 实测（User-Agent: TDSF-Linux-Desktop-Research）

### 2.1 调研矩阵

| 源域名 | Disallow 范围 | Crawl-delay | 综合风险 | 推荐策略 |
|---|---|---|---|---|
| **wiki.archlinux.org** | 仅禁止特殊页（File/Image/Special/Template） | 无明确 | **极低**（官方提供月度 dump） | ✅ **Phase 1 已用**：`static/archwiki-latest.tar.gz` |
| **tldp.org** (Linux 文档项目) | 仅禁止 `/test/` | 无 | 极低 | ✅ **Phase 1 已用**：在线抓 HOWTO 索引 + 链接 |
| **help.ubuntu.com** | 禁止大量历史版本（/14.04 ~ /26.10）、/img/、/libs/、/dev/、/community/*?action= | **5 秒** | 中（需避让旧版本） | ⚠️ Phase 2 候选（**仅爬 LTS 最新版**，5s 间隔） |
| **access.redhat.com** (Red Hat 知识库) | 禁止 /admin/、/user/、/search/、IBM Docs 全语言、changeLanguage | **10 秒** | 高 | ❌ Phase 2 暂不接入（可改为"提示用户去官网"） |
| **docs.kernel.org** | 404（无 robots.txt） | 无 | 中（默认保守） | ⚠️ Phase 2 候选（**遵守 2s 间隔**，避免高峰） |
| **www.debian.org** | 404（无 robots.txt） | 无 | 中 | ⚠️ Phase 2 候选（同上） |
| **wiki.debian.org** | 未测（Debian Wiki，MediaWiki） | - | 中 | ⚠️ Phase 3 候选 |
| **developer.aliyun.com / help.aliyun.com** | 未测 | - | - | 候选（国内镜像，中文） |

### 2.2 关键发现

- **Arch Wiki** 是唯一明确提供**官方月度快照**的源：
  - URL：`https://wiki.archlinux.org/static/archwiki-latest.tar.gz`
  - 文档：`https://wiki.archlinux.org/title/ArchWiki:About` 提到 `arch-wiki-docs` 包
  - 这是"下载官方包"而不是"爬 HTML"，**彻底规避 robots 风险**
- **TLDP/LDP** 几乎全允许，是次优的"全量抓取"目标
- **Ubuntu/RedHat/Kernel** 都有明确限制，**必须降速 + 选路径**
- 部分源**没有 robots.txt**（kernel.org、debian.org），按"可访问但需保守限流"处理

---

## 三、协议与合法性调研

### 3.1 Creative Commons CC BY-SA 4.0 核心要求

参考：https://creativecommons.org/licenses/by-sa/4.0/legalcode

- **BY（Attribution 署名）**：必须标注作者、来源、URL、License、是否修改
- **SA（ShareAlike 相同方式共享）**：衍生作品必须采用**相同 CC BY-SA 4.0 协议**
- **可商用**：✅ 允许（与 CC-NC 不同）
- **可修改**：✅ 允许
- **风险点**：如果你的产品是闭源 + 商用 + 整合了 CC BY-SA 内容，**必须将你的知识库整体也以 CC BY-SA 4.0 发布**

### 3.2 我们的合规方案

| 源 | 协议 | 我们的处理 |
|---|---|---|
| Arch Wiki | CC BY-SA 4.0 | 标注 source.url + license + 抓取时间 |
| LDP HOWTOs | 各 HOWTO 独立标注（多数 GPL/CC BY-SA） | 透传原始 license 字段 |
| Ubuntu Help | Canonical 版权 | 仅作"个人学习"使用，UI 顶部显示"内容版权归原作者" |
| Red Hat Docs | 红帽专有 | ❌ 不抓，提示用户去官网 |
| Kernel.org | GPL（内核文档） | 标注 source + license |

**强制项（已实现）**：
- 每条 `TutorialEntry.source` 包含 `name` + `url` + `crawledAt` + `license`
- UI 渲染时**强制显示来源链接**（"内容来源" + 跳转按钮）

---

## 四、开源项目调研

### 4.1 爬虫框架对比

| 项目 | Stars | 语言 | 关键能力 | 我们的取舍 |
|---|---|---|---|---|
| **Crawlee** (apify) | 24,743 | Node/TS | 集成 Cheerio/Playwright/Puppeteer、代理轮换、请求队列、反封禁 | **可选升级**（如果未来要爬 JS 渲染页） |
| **Scrapy** | 63,179 | Python | 工业级爬虫框架 | 不引入（与 Electron TS 技术栈不匹配） |
| **turndown** | 11,331 | JS | HTML → MD 转换 | ✅ **已使用**（`src/main/services/tutorial/crawler/html-to-tutorial.ts`） |
| **showdown** | 14,871 | JS | Markdown ↔ HTML 双向 | 暂不需要（我们只单向） |
| **html-to-markdown** (Go) | 3,739 | Go | "Even works with entire websites and can be extended through rules" | 备选（如果 turndown 性能不够） |

### 4.2 与本项目的契合度

- **Crawlee**：24800 stars、Apache 2.0、内置 CheerioCrawler 与我们的 `cheerio + turndown` 方案完全重合
  - 优点：自动代理轮换、自动限流、自动重试
  - 缺点：~10MB 依赖、API 学习成本、对简单 dump 抓取是过度设计
  - **结论：当前不引入，但 `tutorial-source-registry.ts` 设计上留好接口**，未来需要时无缝切换

- **turndown**：已经在用，**完全满足** 需求

- **cheerio**：已经在用，**完全满足** 需求

- **tar**（解压 tar.gz）：已经在用，**完全满足** 需求

### 4.3 参考的最佳实践

来源：[scrapingcentral.com - Polite Scraping](https://scrapingcentral.com/learn/static-scraping/polite-scraping)

1. ✅ **明确 User-Agent**：`TDSF-Linux-Desktop/0.6.0 (Educational; +https://github.com/tdsf)`
2. ✅ **每请求间隔 + 抖动**：`1.0s + random(0, 0.3)s`（已实现）
3. ✅ **429 / Retry-After 尊重**：待实现（目前是简单重试）
4. ✅ **Token Bucket 限流**：待实现
5. ✅ **Contact Email**：`+mailto:crawler@tdsf.app`（推荐加上）

---

## 五、补充调研：开源实战项目与 Linux 教程博客源

> 本节为 2026-07-16 进一步调研，针对"是否有开源经验""博客源是否还有别的可选"两个问题。
> 调研方法：gh search repos + WebSearch（多引擎）+ 关键仓库 README/CONTRIBUTING 实读。

### 5.1 开源实战项目深度调研

| 项目 | 描述 | Stars | 语言 | License | 我们的可借鉴点 |
|---|---|---|---|---|---|
| **[sindresorhus/p-limit](https://github.com/sindresorhus/p-limit)** | "Run multiple promise-returning & async functions with limited concurrency" | 2,900 | JS | MIT | **业界事实标准**；我们 `TokenBucket` 是 p-limit 思路的子集（加上 refill 节奏），未来要兼容 Promise 风格可平滑切换 |
| **[xVc323/omnidocs](https://github.com/xVc323/omnidocs)** | "Automated documentation crawler that generates LLM-friendly Markdown from any docs site" | 11 | Python | MIT | **思路同源**：自动抓 docs 站点 → 干净 MD → 给 LLM 消费；验证了"cheerio+turndown"类方案在 2026 年仍为主流 |
| **[warezfr/doc-crawler-rag](https://github.com/warezfr/doc-crawler-rag)** | "Clean, chunked documentation crawler optimized for RAG & AnythingLLM. Dockerized" | 1 | Python | - | 验证了"RAG 友好"是 2026 爬虫主流目标；可借鉴 chunk 切分策略 |
| **[kaisewhite/rag-agent](https://github.com/kaisewhite/rag-agent)** | "LangChain + Supabase + OpenAI 的智能文档爬虫+问答" | 1 | Python | - | 验证了"知识库+爬虫+LLM"链路是当前趋势，与我们项目方向吻合 |
| **[franclin/polite-crawler](https://github.com/franclin/polite-crawler)** | "Polite web crawler that respects rate limits, robots.txt directives, and stores crawled data in MySQL. Uses Redis for rate limiting" | 1 | PHP | - | **明确做"礼貌爬虫"** 的人很少；本项目命名也带"polite"，与我们的 `polite-fetch.ts` 设计哲学一致 |
| **[use-apify.com Crawlee 2026 教程](https://use-apify.com/blog/crawlee-nodejs-web-scraping-tutorial)** | 2026-03 最新 Crawlee 3.x 实战指南 | - | - | - | 验证了 Crawlee 在 2026 仍是 Node.js 爬虫首选；如未来要爬 JS 渲染页（论坛、Vue/React SPA），Crawlee 仍是最佳选择 |
| **[mantisapi.com Cheerio 2026 指南](https://mantisapi.com/blog/web-scraping-cheerio-nodejs-guide-2026.html)** | "Web Scraping with Cheerio and Node.js in 2026: The Complete Guide" | - | - | - | **cheerio 1.0+ ESM 优先**；我们用的 `import * as cheerio` 是正确用法 |
| **[geekflare HTML to MD](https://geekflare.com/guides/how-to-scrape-webpages-to-markdown/)** | 两种方法：Playwright+Turndown / Cheerio+html-to-md | - | - | - | **确认我们的选择正确**：cheerio+turndown 适合"静态文档"；Playwright+turndown 适合"JS 渲染页" |
| **[scrapingcentral.com Polite Scraping](https://scrapingcentral.com/learn/static-scraping/polite-scraping)** | robots.txt / Delays / Rate Limits / 429 完整指南 | - | - | - | 业界 Best Practice 整理，与我们 polite-fetch 设计 1:1 对应 |
| **[proxyscraping.org Rate Limit 策略](https://proxyscraping.org/blog/rate-limiting-and-retry-strategies-that-avoid-block-escalation)** | "Rate limiting and retry strategies that avoid block escalation" | - | - | - | **关键洞察**：Token Bucket 允许"突发"，Leaky Bucket 平滑；我们用 Token Bucket 适合"偶尔批量抓 HOWTO 索引" |
| **[scrapeops.io Python Requests 重试](https://scrapeops.io/python-web-scraping-playbook/python-requests-retry-failed-requests/)** | "Python Requests: How To Retry Failed Requests (2026 Guide)" | - | - | - | **重试规则表**：仅对 408/425/429/5xx 重试，其他 4xx 不重试（否则更封）—— 我们 polite-fetch 的 `status_forcelist` 应明确这点 |

### 5.2 业界 Polite 爬虫规范（综合 scrapingcentral + proxyscraping + scrapeops）

我们的 `polite-fetch.ts` 与业界最佳实践的对照：

| 实践项 | 业界标准 | 我们的实现 | 状态 |
|---|---|---|---|
| User-Agent 标识 | `MyScraper/1.0 (+mailto:contact@example.com)` | `TDSF-Linux-Desktop/0.6.0 (Educational; +https://github.com/tdsf)` | ✅ |
| 基础间隔 | 1.0s/请求 | `baseIntervalMs: 1000` | ✅ |
| 随机抖动 | `1.0 + random.uniform(0, 0.3)` | `baseIntervalMs + Math.random() * jitterMs` | ✅ |
| 限流算法 | Token Bucket 或 Leaky Bucket | **Token Bucket**（`polite-fetch.ts`） | ✅ |
| 429 处理 | 必须尊重 `Retry-After` | 解析 `Retry-After`（秒数 + HTTP date）+ 退避重试 | ✅ |
| 5xx 重试 | 指数退避 + jitter | `baseIntervalMs * 2^(attempt-1) + jitter` | ✅ |
| 4xx 不重试 | 403/404 不重试 | 仅 429/5xx 触发重试循环 | ✅ |
| 超时控制 | 15-30s | `AbortController` + `timeoutMs: 30000` | ✅ |
| robots.txt 解析 | 用 `urllib.robotparser` 或 `protego` | **未实现**（Phase 2 候选源时补 `robots.ts`） | ⚠️ |
| 联系邮箱 | `+mailto:contact@example.com` | 缺 | ⚠️ 建议补 |
| 失败重试上限 | 3-5 次 | `maxRetries: 3` | ✅ |
| 信号感知 | 双层 Token Bucket（全局+端点） | 单层（`rateLimiter` 参数） | ✅ 多源时每个源单独 Bucket |

### 5.3 Linux 教程博客源补充（除 LDP / Arch Wiki / kernel.org / Debian Wiki 之外）

调研方法：WebSearch "best free linux documentation site tutorial" + 浏览 help.ubuntu.com 的 ExternalGuides 列表。

| 源 | 协议 | 风险 | 用途 | 是否接入 | 备注 |
|---|---|---|---|---|---|
| **[Linux Journey](https://labex.io/linuxjourney)**（原 linuxjourney.com） | MIT（GitHub 仓库） | 极低 | "Grasshopper → Journeyman → Networking Nomad → Sysadmin" 初学者阶梯 | ✅ **Phase 2 候选** | 体系化、教学路径明确，比 LDP 单篇 HOWTO 更友好 |
| **[GNU Manuals](https://www.gnu.org/manual/)** | GNU FDL | 极低 | 官方命令手册（gawk, coreutils, bash, gcc, make…） | ⚠️ 候选 | 工具命令权威解释；可作为"高级"层级 |
| **[wiki.ubuntu.org.cn](https://wiki.ubuntu.org.cn/)** | CC BY-SA 3.0 | 低 | Ubuntu 中文 Wiki | ✅ **Phase 2 中文候选** | 国内访问快 |
| **[wiki.centos.org](https://wiki.centos.org/)** | CC BY-SA 3.0 | 低 | CentOS Wiki | ⚠️ 候选 | CentOS 8 已停维，但内容仍可参考；适合"服务器运维" |
| **[tldr-pages/tldr](https://github.com/tldr-pages/tldr)**（GitHub） | CC BY-SA 4.0（部分 MIT） | 极低 | 简化版 man page | ✅ **Phase 1 即接**：直接 clone GitHub repo | 是 GitHub 数据，不是爬 HTML；可作为"命令速查"小知识库 |
| **[cheat/cheat](https://github.com/cheat/cheat)**（GitHub） | MIT | 极低 | 命令 cheat sheet | ⚠️ 候选 | 类似 tldr |
| **[jlevy/the-art-of-command-line](https://github.com/jlevy/the-art-of-command-line)**（GitHub） | CC BY-SA 4.0 | 极低 | "命令行艺术"中英双语 | ✅ **Phase 1 即接**：直接 clone GitHub repo | 中英双语，**非常适合"教学"** |
| **[explainshell.com](https://explainshell.com/)** | - | - | 命令解析 | ❌ 不爬（无 robots，全是 DB 渲染，收益小） | 可作为"跳转推荐" |
| **[CSDN/OSCHINA/掘金](https://blog.csdn.net/)** | 复杂 | 高 | 中文博客 | ❌ 不接入 | 版权复杂 + 反爬严 + 商业，与我们定位冲突 |
| **[HTTrack](http://www.httrack.com/)** | GPL | - | 离线抓站工具 | 工具备选 | 若未来需要整站镜像可考虑 |

### 5.4 已落地的源 vs 候选源（最终推荐）

**Phase 1（已实现 / 立刻接入）**：
1. ✅ Arch Wiki 月度 dump（CC BY-SA 4.0，官方 tar.gz）
2. ✅ LDP HOWTOs（GNU FDL）
3. 🆕 **tldr-pages** GitHub clone（CC BY-SA 4.0 / MIT）
4. 🆕 **the-art-of-command-line** GitHub clone（CC BY-SA 4.0）

**Phase 2（候选，调研完成后按风险从小到大接入）**：
1. kernel.org/doc（GPL，无 robots）
2. wiki.debian.org（CC BY-SA 4.0，MediaWiki API）
3. Linux Journey（MIT，结构化分层）
4. GNU Manuals（GNU FDL）
5. wiki.ubuntu.org.cn（CC BY-SA 3.0，中文）
6. wiki.centos.org（CC BY-SA 3.0）

**Phase 3（需深度评估后接入）**：
- help.ubuntu.com（5s 间隔 + 路径白名单）
- 第三方社区 Wiki

**永久不接入**：
- ❌ access.redhat.com（10s 延迟 + 高风险）
- ❌ Microsoft Learn（专有版权）
- ❌ CSDN/OSCHINA/掘金（版权 + 反爬）
- ❌ explainshell.com（无 robots + DB 渲染）
- ❌ 任何需要登录的源

### 5.5 关键洞察

1. **"GitHub clone 比爬 HTML 更稳"**：tldr-pages 和 the-art-of-command-line 这类**本身就是 GitHub 仓库**的源，比爬 HTML 风险低、合规高。Phase 1 立即 clone 比开发爬虫 ROI 更高。
2. **"官方 dump > robots.txt > 无 robots > 有禁止"**：合规风险从小到大。Arch Wiki 提供 dump 是 0 风险；kernel.org 无 robots 是默认保守；CSDN 反爬严是高风险。
3. **"Token Bucket 适合批量突发"**：我们选 Token Bucket（允许"攒一批再发"）而不是 Leaky Bucket（强制平滑），因为爬 LDP 时"启动时集中请求 1 个索引页 + 50 个 HOWTO"是合理突发。
4. **"429 退避时间不可忽略"**：很多爬虫失败不是因为 429 本身，而是"429 后没等够就重试"导致被升级封禁。polite-fetch 的 `Retry-After` 解析必须严格遵守。
5. **"礼貌爬虫有命名传统"**：`polite-crawler` / `PoliteCrawler` / `polite-fetch` 业界已有共识。我们的 `polite-fetch.ts` 命名与开源惯例对齐。
6. **"我们的方案与开源主流 1:1 契合"**：cheerio+turndown+tar+p-limit 思路在 2026 仍是 Node.js 静态文档爬虫的事实标准。无需重构。

---

## 六、修订后的 Phase 划分

### Phase 1: 离线 dump 导入（已实现）✅
- **Arch Wiki 月度快照**：500MB tar.gz，解压 → cheerio 解析 → turndown 转 MD
- **LDP HOWTOs**：从 HOWTO 索引页抓链接 → 抓每个 HOWTO → 转 MD
- 协议：CC BY-SA 4.0
- 状态：**代码已完成，待 typecheck 验证**

### Phase 2: 在线增量抓取（设计中）
**目标源**（按风险从低到高）：
1. ✅ **kernel.org**（无 robots，按 2s 保守间隔）— 适合"内核参数"教程
2. ⚠️ **help.ubuntu.com**（仅 LTS 最新版 + 5s 间隔 + 路径白名单）— 适合"桌面使用"教程
3. ✅ **wiki.debian.org**（MediaWiki，仿 Arch Wiki 处理）

**禁用源**：
- ❌ access.redhat.com（10s 延迟 + 路径多禁止，体验差）
- ❌ docs.microsoft.com（同理）
- ❌ 中文 CSDN/OSCHINA（版权复杂 + 商业）

### Phase 3: 调度与监控（设计中）
- cron 定时拉取（用户可配置频率）
- 进度事件流（IPC push 到 UI）
- 失败重试 + 死信队列
- 源版本对比（"上次抓了 5000 篇，本次只新增 200 篇"）

---

## 六、需要用户确认的 4 个决策点

### 决策点 1：是否引入 Crawlee？

- **方案 A（推荐）**：不引入 Crawlee，继续用 cheerio + turndown + 自写简单限流
  - 优点：依赖轻、代码简单、当前 Phase 1 够用
  - 缺点：未来要爬 JS 渲染页时需要重构
- **方案 B**：引入 Crawlee 作为 Phase 2 框架
  - 优点：开箱即用（限流/重试/队列）、未来扩展容易
  - 缺点：~10MB 依赖、学习成本

### 决策点 2：Phase 2 目标源范围

- **方案 A（保守）**：仅 kernel.org（无 robots）
- **方案 B（推荐）**：kernel.org + wiki.debian.org（MediaWiki 处理）
- **方案 C（激进）**：上述 + help.ubuntu.com（5s 间隔 + 路径白名单）

### 决策点 3：CC BY-SA 4.0 的项目级策略

> Arch Wiki 是 CC BY-SA 4.0。SA 条款要求"衍生作品必须同协议"。

- **方案 A（推荐）**：在"关于"页声明知识库整体 CC BY-SA 4.0，强制每条 source 标注
- **方案 B**：仅作为"个人学习/教育"使用，UI 顶部"仅供个人学习，内容版权归原作者"声明
- **方案 C**：混合模式（在线抓的源标注 source，离线种子不标注）

### 决策点 4：429/Retry-After 处理的优先级

- **方案 A（推荐）**：纳入 Phase 1，理由：Phase 1 的 Arch Wiki 下载也可能被限流
- **方案 B**：延后到 Phase 3，理由：当前源都比较宽松，不需要
- **方案 C**：仅做简单重试，理由：避免过度设计

---

## 七、调研产出物清单

| 文件 | 用途 | 位置 |
|---|---|---|
| robots.txt 实测 | 合规性证据 | 本文档 §2 |
| 源调研矩阵 | 决策依据 | 本文档 §2.1 |
| CC 协议要求 | 法务参考 | 本文档 §3.1 |
| 开源项目对比 | 避免重复造轮子 | 本文档 §4.1 + §5.1 |
| 业界 Polite 爬虫规范对照 | 验证我们的实现 | 本文档 §5.2 |
| Linux 教程博客源补充 | Phase 1/2/3 源选择 | 本文档 §5.3 / §5.4 |
| 4 个决策点 | 已确认 | 本文档 §六（已确认） |
| 修订后 Phase 划分 | 后续开发依据 | 本文档 §六 |

---

## 八、参考链接

### 8.1 官方 dump 与权威源

- Arch Wiki 官方 dump：https://wiki.archlinux.org/static/archwiki-latest.tar.gz
- Arch Wiki 关于页：https://wiki.archlinux.org/title/ArchWiki:About
- LDP HOWTO 索引：https://www.tldp.org/HOWTO/HOWTO-INDEX/howtos.html
- LDP 主页：https://tldp.org/
- kernel.org 文档：https://www.kernel.org/doc/
- Debian Wiki：https://wiki.debian.org/
- Ubuntu 中文 Wiki：https://wiki.ubuntu.org.cn/
- CentOS Wiki：https://wiki.centos.org/
- GNU Manuals：https://www.gnu.org/manual/

### 8.2 GitHub 源（直接 clone，比爬 HTML 更稳）

- tldr-pages：https://github.com/tldr-pages/tldr
- jlevy/the-art-of-command-line：https://github.com/jlevy/the-art-of-command-line
- cheat/cheat：https://github.com/cheat/cheat

### 8.3 协议参考

- CC BY-SA 4.0 法律文本：https://creativecommons.org/licenses/by-sa/4.0/legalcode
- GNU FDL 1.3：https://www.gnu.org/licenses/fdl-1.3.html

### 8.4 框架与工具

- Crawlee 框架：https://crawlee.dev/（24,743 stars）
- Crawlee 中文文档：https://bannermiao.github.io/crawlee-cn/
- Polite Scraping 指南：https://scrapingcentral.com/learn/static-scraping/polite-scraping
- Robots.txt 指南：https://developers.google.com/search/docs/crawling-indexing/robots/robots_txt
- turndown：https://github.com/mixmark-io/turndown（11,331 stars）
- p-limit：https://github.com/sindresorhus/p-limit（2,900 stars）
- HTTrack 离线抓站：http://www.httrack.com/

### 8.5 2026 实战参考文章

- [use-apify.com Crawlee Node.js 教程 2026](https://use-apify.com/blog/crawlee-nodejs-web-scraping-tutorial)
- [mantisapi.com Cheerio Node.js 2026 指南](https://mantisapi.com/blog/web-scraping-cheerio-nodejs-guide-2026.html)
- [geekflare HTML to MD 指南](https://geekflare.com/guides/how-to-scrape-webpages-to-markdown/)
- [proxyscraping.org Rate Limit 策略](https://proxyscraping.org/blog/rate-limiting-and-retry-strategies-that-avoid-block-escalation)
- [scrapeops.io Python Requests 重试指南](https://scrapeops.io/python-web-scraping-playbook/python-requests-retry-failed-requests/)
- [proxyscraping.org GitHub 大规模爬虫](https://proxyscraping.org/blog/how-to-scrape-github-at-scale-in-2026-with-proxies-that-work)

### 8.6 调研中参考的开源项目

- [xVc323/omnidocs](https://github.com/xVc323/omnidocs)
- [warezfr/doc-crawler-rag](https://github.com/warezfr/doc-crawler-rag)
- [kaisewhite/rag-agent](https://github.com/kaisewhite/rag-agent)
- [franclin/polite-crawler](https://github.com/franclin/polite-crawler)
- [Linux Journey](https://labex.io/linuxjourney)

---

## 九、结论

**调研结论**：4 个核心问题都有了明确答案：

1. **去哪里爬**？
   - **Phase 1（已实现/即接）**：Arch Wiki dump + LDP HOWTOs + tldr-pages（GitHub clone）+ the-art-of-command-line（GitHub clone）
   - **Phase 2（候选）**：kernel.org + wiki.debian.org + Linux Journey + GNU Manuals + wiki.ubuntu.org.cn + wiki.centos.org
   - **Phase 3（评估后）**：help.ubuntu.com
   - **避开**：access.redhat.com、Microsoft Learn、CSDN、explainshell.com、需要登录的源

2. **让不让爬**？
   - **官方 dump**：明确允许（Arch Wiki 主动提供）
   - **GitHub clone**：合法（开源协议 + 协议要求）
   - **robots.txt 全开放**：TLDP、Debian Wiki
   - **有 delay 的**：Ubuntu Help（5s）、Red Hat（10s）
   - **有禁止目录的**：所有源都有，需路径白名单

3. **开源经验**？
   - **思路验证**：omnidocs / doc-crawler-rag / rag-agent 都验证了"cheerio+turndown+知识库+LLM"是 2026 主流方案
   - **p-limit**（2,900 stars）是 JS 限流事实标准，我们 TokenBucket 与之契合
   - **polite-crawler** 命名传统验证了我们的"polite-fetch"命名与开源惯例对齐
   - **turndown + cheerio + tar** 已在用，与 2026 主流 1:1 契合，**无需重构**

4. **如何礼貌爬**？
   - ✅ 已实现：TokenBucket + Jitter + Retry-After + 5xx 退避 + AbortController 超时
   - ⚠️ 待补：robots.txt 解析器 + 联系邮箱（`+mailto:crawler@tdsf.app`）+ 源级 Bucket
   - ⚠️ 待验证：4xx 不重试规则（避免"重试后被升级封"）

## 十、Phase 1 修复与遗留问题清单（2026-07-16）

### 10.1 本次 Phase 1 修复的预先存在错误（typecheck）

| 文件 | 错误 | 修复方式 |
|---|---|---|
| `src/main/types/turndown-plugin-gfm.d.ts` | `turndown-plugin-gfm` 缺类型声明（TS7016） | 新增 `src/main/types/turndown-plugin-gfm.d.ts`，手工声明 `gfm(service)` 签名 |
| `tsconfig.web.json` | web 端 typecheck 找不到 preload 类型 | include 中加入 `src/preload/**/*` |
| `src/renderer/src/types/electron.d.ts` | 缺少 `tutorialListSources` / `tutorialCrawlStart` / `tutorialCrawlStatus` 3 个 IPC | 补全 `ElectronAPI` 接口 |
| `src/main/services/tutorial/crawler/tutorial-crawler-service.ts` | CRAWLERS 映射表未挂载新爬虫 | 挂载 `tldr-pages` + `art-of-command-line` |
| `src/main/services/tutorial/crawler/tutorial-source-registry.ts` | 未注册新源 | 注册 2 个 P0 源（`enabledByDefault=true`） |
| `src/main/services/tutorial/crawler/ldp-howtos-offline.ts` | `ldpBucket` 未 import + dead code | 清理未使用的 mkdir/rm/tmpdir/cheerio 死代码，import `TokenBucket` |
| `src/main/services/tutorial/crawler/tutorial-repo.ts` | 未持久化 `source.licenseUrl` / `source.kind` | 新增 `FIELD_SOURCE_LICENSE_URL` / `FIELD_SOURCE_KIND` JSON 字段 |

### 10.2 lint 18 个错误（预先存在，与本次 Phase 1 无关）

> 这些错误都是历史遗留，本次 Phase 1 任务**不涉及**这些文件，未做改动。
> 建议作为独立 PR 在 v0.7.0 集中清理。

| 文件 | 错误数 | 错误类型 |
|---|---|---|
| `src/main/index.ts` | 3 | `path` / `loadTutorialSeeds` 未使用 + `require` 风格 |
| `src/main/services/db/database.ts` | 2 | `this` 别名为 `self` |
| `src/main/services/llm/tools/monitor-get.ts` | 2 | `MonitorData` / `netOut` 未使用 |
| `src/main/services/mcp/server.ts` | 1 | `Evidence` 未使用 |
| `src/main/services/observability/langfuse.ts` | 1 (warn) | `any` 类型 |
| `src/main/services/profiler/markdown-renderer.ts` | 2 | `PROBE_CATALOG` / `itemsInGroup` 未使用 |
| `src/main/services/profiler/risk-detector.ts` | 1 | `searchAnyOutput` 未使用 |
| `src/main/services/tutorial/crawler/arch-wiki-offline.ts` | 1 | `cheerio` 未使用（实际只用 `parseHtmlToTutorial`） |
| `src/main/services/tutorial/crawler/tutorial-crawler-service.ts` | 1 | `allSources` 未使用 |
| `src/preload/index.ts` | 1 | `KnowledgeEntry` 未使用 |
| `src/renderer/src/components/ai/ChatPanel.tsx` | 1 | `upsertToolCall` 未使用 |
| `src/renderer/src/components/layout/ConnectDialog.tsx` | 1 | `SshAuthType` 未使用 |
| `src/renderer/src/stores/terminal-store.ts` | 1 | `idx` 未使用 |
| `src/renderer/src/types/electron.d.ts` | 1 | `RiskItem` 未使用 |

**总计**：18 个 error + 1 个 warning。

---

## 十一、附录：Phase 1 涉及的源元数据（最终态）

```typescript
// CrawlerSourceKind = 'offline-dump' | 'github-clone' | 'online-crawl'
//
// Phase 1 P0 源（enabledByDefault=true）：
[
  {
    id: 'arch-wiki',
    kind: 'offline-dump',
    baseUrl: 'https://wiki.archlinux.org/static/archwiki-latest.tar.gz',
    license: 'CC BY-SA 4.0',
    crawler: 'crawlArchWikiOffline'  // 500MB tar.gz → cheerio+turndown
  },
  {
    id: 'ldp-howtos',
    kind: 'offline-dump',
    baseUrl: 'https://www.tldp.org/HOWTO/HOWTO-INDEX/howtos.html',
    license: 'GNU Free Documentation License',
    crawler: 'crawlLdpHowtos'  // politeFetch(3 并发, 1 req/s)
  },
  {
    id: 'tldr-pages',
    kind: 'github-clone',
    baseUrl: 'https://github.com/tldr-pages/tldr.git',
    license: 'CC BY 4.0',  // 注意：不是 CC BY-SA
    crawler: 'crawlTldrPages'  // git clone --depth=1 --filter=blob:none --sparse
  },
  {
    id: 'art-of-command-line',
    kind: 'github-clone',
    baseUrl: 'https://github.com/jlevy/the-art-of-command-line.git',
    license: 'CC BY-SA 4.0',
    crawler: 'crawlArtOfCommandLine'  // git clone + 按 ## 拆章节
  }
]

// Phase 2 P1/P2/P3 源（enabledByDefault=false，待 Phase 2 实施）：
// - ubuntu-help / ms-learn / redhat-docs / digitalocean / linuxize / tecmint
// - kernel.org / wiki.debian.org / Linux Journey / GNU Manuals
```

### 11.1 每个 TutorialEntry 必带的 source 字段（Phase 1-c 强制）

```typescript
interface TutorialSource {
  name: string           // 必填：'Arch Wiki' / 'tldr-pages' / ...
  url: string            // 必填：原始 URL（GitHub blob 或 wiki path）
  crawledAt: number      // 抓取时间戳
  license: string        // 必填：'CC BY-SA 4.0' / 'CC BY 4.0' / 'GNU FDL'
  licenseUrl?: string    // 协议详情页 URL（点击跳转）
  kind?: 'offline-dump' | 'github-clone' | 'online-crawl'  // 来源类型
}
```

UI 渲染时必须显示：
1. 来源名称（可点击跳转）
2. License 类型（可点击跳转协议详情）
3. 抓取时间（用于判断内容时效性）

---

## 十二、Phase 2 启动建议

按 §5.4 的"已落地的源 vs 候选源"推荐顺序，Phase 2 优先做：

1. **kernel.org/doc**（无 robots，2s 间隔，GPL）— 内核参数权威
2. **wiki.debian.org**（CC BY-SA 4.0，MediaWiki API）— Debian 系教程
3. **Linux Journey**（MIT，结构化分层）— 初学者阶梯
4. **GNU Manuals**（GNU FDL）— 工具命令权威

每个新源接入前必须：
1. 在 `tutorial-source-registry.ts` 注册（id + kind + baseUrl + license + crawlDelayMs）
2. 在 `tutorial-crawler-service.ts` CRAWLERS 挂载爬虫
3. 通过 `polite-fetch.ts`（在线爬）或 `git clone`（GitHub clone）
4. 在 `TutorialEntry.source` 标注完整字段（name / url / license / licenseUrl / kind）
5. 更新方案书 §5.4 表格

---

> **调研结束时间**：2026-07-16 23:00
> **下次启动建议**：先做 Phase 2 候选源评估，再实现"调度与监控"（Phase 3）
